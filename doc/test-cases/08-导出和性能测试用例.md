# 导出和性能测试用例

> 文档版本：v1.2  
> 创建日期：2026-06-03  
> 测试范围：导出任务 + 异步队列 + 导出脱敏 + 导出联调 + 性能联调 + 索引验证 + 缓存验证 + 压测场景  
> 依据文档：`doc/v1.2-完整交付版-AB端任务分配.md`

---

## 1. 文档头部

| 字段 | 说明 |
|------|------|
| 测试类型 | 导出功能 + 性能压测 |
| 测试端口 | 运营端、销售端、教务端、主管端 |
| 后端模块 | exports.service.ts、exports.controller.ts、exports.processor.ts |
| 前端模块 | 导出中心、各端口导出按钮 |
| 依赖服务 | MySQL、Redis（可选，REDIS_URL 未配时降级 setImmediate） |
| 核心接口 | POST /api/exports、GET /api/exports、GET /api/exports/:id、GET /api/exports/:id/download |

---

## 2. 导出类型矩阵表

| 导出类型 | 导出对象 | 运营可见 | 销售可见 | 教务可见 | 主管可见 | 后端状态 |
|----------|----------|----------|----------|----------|----------|----------|
| posts | 作品 | 自己 | - | - | 全局 | ⚠️ 占位 CSV |
| leads | 客资 | 自己 | 自己 | - | 全局 | ✅ 已实现 |
| rankings | 排行榜 | - | - | - | 全局 | ⚠️ 占位 CSV |
| orders | 订单 | - | 自己 | 自己 | 全局 | ✅ 已实现 |
| order_progress | 订单进度 | - | 自己 | 自己 | 全局 | ✅ 已实现 |
| collaboration_records | 协同记录 | 自己 | 自己 | - | 全局 | ✅ 已实现 |
| accounts | 账号 | 自己 | - | - | 全局 | ✅ 已实现 |

---

## 3. 导出接口用例

### 3.1 创建导出任务

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-001 | 销售创建客资导出任务 | 导出 | 销售点击导出客资 | P0 | 销售已登录，有自己负责的客资 | 销售角色 token、leads 类型 | 1. 销售登录<br>2. 进入客资列表<br>3. 点击导出按钮<br>4. 选择导出类型为 leads<br>5. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing' | | |
| TC-EXP-002 | 销售创建订单导出任务 | 导出 | 销售点击导出订单 | P0 | 销售已登录，有自己负责的订单 | 销售角色 token、orders 类型 | 1. 销售登录<br>2. 进入订单列表<br>3. 点击导出按钮<br>4. 选择导出类型为 orders<br>5. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing' | | |
| TC-EXP-003 | 教务创建订单进度导出任务 | 导出 | 教务点击导出订单进度 | P0 | 教务已登录，有自己负责的订单 | 教务角色 token、order_progress 类型 | 1. 教务登录<br>2. 进入订单列表<br>3. 点击导出按钮<br>4. 选择导出类型为 order_progress<br>5. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing' | | |
| TC-EXP-004 | 主管创建全局客资导出任务 | 导出 | 主管导出全局客资 | P0 | 主管已登录，存在客资数据 | 主管角色 token、leads 类型、scope=all | 1. 主管登录<br>2. 进入客资看板<br>3. 设置筛选条件<br>4. 点击导出按钮<br>5. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing'，filterJson 包含筛选条件 | | |
| TC-EXP-005 | 主管创建全局订单导出任务 | 导出 | 主管导出全局订单 | P0 | 主管已登录，存在订单数据 | 主管角色 token、orders 类型、scope=all | 1. 主管登录<br>2. 进入订单看板<br>3. 设置筛选条件<br>4. 点击导出按钮<br>5. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing'，filterJson 包含筛选条件 | | |
| TC-EXP-006 | 主管创建协同记录导出任务 | 导出 | 主管导出协同记录 | P0 | 主管已登录，存在协同记录 | 主管角色 token、collaboration_records 类型 | 1. 主管登录<br>2. 进入协同管理<br>3. 点击导出按钮<br>4. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing' | | |
| TC-EXP-007 | 运营创建自己客资导出任务 | 导出 | 运营导出自己的客资 | P0 | 运营已登录，有自己录入的客资 | 运营角色 token、leads 类型 | 1. 运营登录<br>2. 进入客资看板<br>3. 点击导出按钮<br>4. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing' | | |
| TC-EXP-008 | 运营创建自己账号导出任务 | 导出 | 运营导出自己的账号 | P1 | 运营已登录，有自己负责的账号 | 运营角色 token、accounts 类型 | 1. 运营登录<br>2. 进入账号管理<br>3. 点击导出按钮<br>4. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing' | | |
| TC-EXP-009 | 运营创建作品导出任务 | 导出 | 运营导出自己的作品 | P1 | 运营已登录，有自己录入的作品 | 运营角色 token、posts 类型 | 1. 运营登录<br>2. 进入我的作品<br>3. 点击导出按钮<br>4. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing' | | ⚠️ A 端占位 |
| TC-EXP-010 | 主管创建排行榜导出任务 | 导出 | 主管导出排行榜 | P1 | 主管已登录，排行榜数据存在 | 主管角色 token、rankings 类型 | 1. 主管登录<br>2. 进入排行榜<br>3. 点击导出按钮<br>4. 提交 POST /api/exports | 返回 200，响应包含 id 和 status='processing' | | ⚠️ A 端占位 |
| TC-EXP-011 | 创建导出任务写操作日志 | 导出 | 导出任务创建后写日志 | P0 | 导出任务创建成功 | 操作日志表 operation_logs | 1. 创建任意导出任务<br>2. 查询 operation_logs 表 | 存在 action='export_create' 的记录，target_type='export_task'，detail 包含 exportType 和 rowCount | | |
| TC-EXP-012 | 60秒内重复创建同一类型导出返回已有任务 | 导出 | 防止重复导出 | P1 | 60秒内有未完成的同类型导出任务 | 同用户、同 exportType | 1. 创建导出任务<br>2. 60秒内再次创建相同 exportType 的任务 | 返回已有任务的 id 和状态，不创建新任务 | | 1分钟防抖机制 |

### 3.2 查询导出任务

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-020 | 查询自己的导出任务列表 | 导出 | 用户查看导出中心 | P0 | 用户已登录，存在导出任务 | 用户角色 token | 1. 用户登录<br>2. 进入导出中心<br>3. 调用 GET /api/exports | 返回该用户的导出任务列表，按 createdAt 降序排列 | | |
| TC-EXP-021 | 按类型筛选导出任务列表 | 导出 | 用户筛选特定类型导出 | P1 | 用户已登录，存在多种类型的导出任务 | 用户角色 token、type 参数 | 1. 调用 GET /api/exports?type=leads | 只返回 leads 类型的导出任务 | | |
| TC-EXP-022 | 导出任务列表分页查询 | 导出 | 导出任务数量多时分页 | P1 | 用户已登录，导出任务超过20条 | 用户角色 token、limit=20、offset=0 | 1. 调用 GET /api/exports?limit=20&offset=0 | 返回分页结果，包含 items、total、limit、offset | | |
| TC-EXP-023 | 查询导出任务详情 | 导出 | 查看单个导出任务状态 | P0 | 存在导出任务 | 用户角色 token、任务 ID | 1. 调用 GET /api/exports/:id | 返回任务详情，包含 status、fileUrl、filterJson 等字段 | | |
| TC-EXP-024 | 非创建者无法查看他人导出任务详情 | 导出 | 越权访问防护 | P0 | 存在其他用户创建的导出任务 | 普通用户 token | 1. 使用非创建者的 token 调用 GET /api/exports/:id | 返回 404（统一404避免泄露任务存在性） | | |
| TC-EXP-025 | 主管可查看任意导出任务详情 | 导出 | 主管权限验证 | P0 | 存在普通用户创建的导出任务 | 主管角色 token | 1. 主管调用 GET /api/exports/:id | 返回任务详情，不受 userId 限制 | | |

### 3.3 下载导出文件

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-030 | 下载已完成导出的CSV文件 | 导出 | 用户下载导出文件 | P0 | 存在 status='completed' 的导出任务，fileUrl 不为空 | 用户 token、任务 ID | 1. 调用 GET /api/exports/:id/download | 返回 CSV 文件流，Content-Type: text/csv，Content-Disposition 包含 filename | | |
| TC-EXP-031 | 下载未完成任务返回409 | 导出 | 防止下载未就绪文件 | P0 | 存在 status='pending' 或 'processing' 的任务 | 用户 token、任务 ID | 1. 调用 GET /api/exports/:id/download | 返回 409 Conflict，message 包含任务当前状态 | | |
| TC-EXP-032 | 下载自己创建的任务成功 | 导出 | 任务创建者下载 | P0 | 用户创建了导出任务且已完成 | 用户 token、任务 ID | 1. 调用 GET /api/exports/:id/download | 返回文件下载，状态码 200 | | |
| TC-EXP-033 | 下载他人任务返回404 | 导出 | 越权下载防护 | P0 | 存在其他用户创建的已完成任务 | 非任务创建者 token | 1. 调用 GET /api/exports/:id/download | 返回 404（统一404避免泄露） | | |
| TC-EXP-034 | 主管下载任意导出文件 | 导出 | 主管权限验证 | P0 | 存在普通用户创建的已完成任务 | 主管 token | 1. 主管调用 GET /api/exports/:id/download | 返回文件下载，不受 userId 限制 | | |
| TC-EXP-035 | 下载时写入操作日志 | 导出 | 下载行为审计 | P0 | 下载请求成功 | operation_logs 表 | 1. 执行一次成功的下载<br>2. 查询 operation_logs | 存在 action='export_download' 的记录 | | |
| TC-EXP-036 | 下载不存在任务返回404 | 导出 | 无效ID处理 | P1 | - | 任意有效 token | 1. 调用 GET /api/exports/invalid-id/download | 返回 404 | | |
| TC-EXP-037 | 下载文件不存在返回410 | 导出 | 文件丢失处理 | P1 | 存在已完成任务但 fileUrl 对应文件已删除 | 任务 ID（对应文件已删除） | 1. 调用 GET /api/exports/:id/download | 返回 410 Gone，message: 'file gone' | | |
| TC-EXP-038 | 本地存储模式文件下载 | 导出 | 本地文件模式验证 | P1 | 系统配置为本地存储，任务已完成 | 本地存储模式下的任务 ID | 1. 调用 GET /api/exports/:id/download | 使用 res.sendFile 返回本地文件 | | |
| TC-EXP-039 | OSS存储模式文件下载重定向 | 导出 | OSS文件模式验证 | P1 | 系统配置为 OSS 存储，任务已完成 | OSS 模式下的任务 ID | 1. 调用 GET /api/exports/:id/download | 返回 302 重定向到 OSS 签名 URL | | |

### 3.4 导出类型权限校验

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-050 | 运营无法导出订单类型 | 导出 | 越权导出防护 | P0 | 运营尝试导出 orders 类型 | 运营 token、exportType=orders | 1. 运营调用 POST /api/exports body={exportType:'orders'} | 返回 403 Forbidden | | |
| TC-EXP-051 | 销售无法导出作品类型 | 导出 | 越权导出防护 | P0 | 销售尝试导出 posts 类型 | 销售 token、exportType=posts | 1. 销售调用 POST /api/exports body={exportType:'posts'} | 返回 403 Forbidden | | |
| TC-EXP-052 | 教务无法导出客资类型 | 导出 | 越权导出防护 | P0 | 教务尝试导出 leads 类型 | 教务 token、exportType=leads | 1. 教务调用 POST /api/exports body={exportType:'leads'} | 返回 403 Forbidden | | |
| TC-EXP-053 | 运营无法导出全局数据 | 导出 | 非主管越权导出 | P0 | 运营尝试 scope=all 导出 | 运营 token、filter={scope:'all'} | 1. 运营调用 POST /api/exports filter={scope:'all'} | scope='all' 被服务端删除，只导出自己范围 | | |
| TC-EXP-054 | 销售导出时客户端传来的role被覆盖 | 导出 | 防止客户端伪造权限 | P0 | 销售传递伪造的 role | 销售 token、body={exportType:'leads', filter:{role:'admin'}} | 1. 销售传递 filter.role='admin' 调用导出<br>2. 检查实际生成的 CSV | 生成的 CSV 数据只包含销售自己负责的客资 | | |

---

## 4. 导出状态机用例

### 4.1 状态流转

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-060 | 导出任务创建后状态为processing | 导出 | 任务创建 | P0 | - | - | 1. 调用 POST /api/exports<br>2. 查询任务状态 | status='processing' | | |
| TC-EXP-061 | 导出完成后状态变为completed | 导出 | 导出生成成功 | P0 | 导出数据量小（<100条） | - | 1. 创建导出任务<br>2. 等待完成<br>3. 查询状态 | status='completed'，finishedAt 不为空，fileUrl 有值 | | |
| TC-EXP-062 | 导出失败后状态变为failed | 导出 | 导出生成失败 | P1 | 人为触发导出异常 | - | 1. 创建导出任务时注入异常数据<br>2. 等待处理完成<br>3. 查询状态 | status='failed'，finishedAt 不为空 | | |
| TC-EXP-063 | 导出完成后发送通知 | 导出 | export_done通知 | P0 | 导出任务完成 | notifications 表 | 1. 创建导出任务<br>2. 等待完成<br>3. 查询 notifications 表 | 存在 typeCode='export_done' 或 'export_finished' 的通知，receiverIds 包含任务创建者 | | |

### 4.2 状态查询和轮询

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-070 | 轮询等待导出完成 | 导出 | 前端轮询状态 | P0 | 创建了导出任务 | - | 1. 创建导出任务<br>2. 每秒调用 GET /api/exports/:id<br>3. 直到 status='completed' 或 'failed' | 在 30 秒内完成轮询 | | 小数据30秒内 |
| TC-EXP-071 | 导出中心展示所有状态 | 导出 | 导出中心UI | P0 | 存在各状态的导出任务 | pending、processing、completed、failed 各至少一条 | 1. 打开导出中心<br>2. 查看任务列表 | 各状态任务正确展示，有对应的状态标签 | | |

---

## 5. 异步队列用例

### 5.1 BullMQ 队列验证

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-080 | REDIS_URL配置时使用BullMQ队列 | 导出 | 队列启动 | P0 | REDIS_URL 已配置 | Redis 运行中 | 1. 重启后端服务<br>2. 检查日志 | 日志显示 'exports queue initialized (redis: ...)' | | |
| TC-EXP-081 | REDIS_URL未配置时降级到setImmediate | 导出 | 降级机制 | P0 | REDIS_URL 未配置 | - | 1. 不配置 REDIS_URL<br>2. 创建导出任务<br>3. 检查日志 | 日志显示 'REDIS_URL not set, exports use in-process setImmediate' | | |
| TC-EXP-082 | BullMQ Worker并发处理任务 | 导出 | 并发验证 | P1 | REDIS_URL 已配置，队列中有多个任务 | 5个并发导出任务 | 1. 同时创建5个导出任务<br>2. 监控 Worker 日志 | Worker 并发数<=2，任务交错执行 | | concurrency=2 |
| TC-EXP-083 | BullMQ Worker失败时写status=failed | 导出 | 失败处理 | P1 | REDIS_URL 已配置，任务处理异常 | 人为注入处理异常 | 1. 创建导出任务<br>2. 模拟处理异常<br>3. 查询任务状态 | status='failed'，finishedAt 不为空 | | |
| TC-EXP-084 | BullMQ Queue连接失败时降级 | 导出 | 降级机制 | P1 | REDIS_URL 配置但连接失败 | 错误的 REDIS_URL | 1. 配置无效的 REDIS_URL<br>2. 重启服务<br>3. 创建导出任务 | 降级到 setImmediate，导出任务仍能完成 | | |
| TC-EXP-085 | BullMQ queue.add失败时降级 | 导出 | 降级机制 | P1 | queue.add 抛出异常 | - | 1. 模拟 queue.add 失败<br>2. 创建导出任务 | 降级到 setImmediate，任务通过 setImmediate 执行 | | |
| TC-EXP-086 | 导出任务1分钟防抖 | 导出 | 防抖机制 | P1 | 同用户同类型有进行中任务 | - | 1. 创建导出任务<br>2. 10秒内再次创建同类型 | 返回已有任务 ID，不创建新任务 | | |
| TC-EXP-087 | 导出任务60秒后允许重新创建 | 导出 | 防抖释放 | P1 | 60秒内有未完成任务 | - | 1. 创建导出任务<br>2. 等待61秒<br>3. 再次创建同类型 | 创建新任务（不是因为状态是completed/failed） | | |

### 5.2 导出处理器原理验证

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-090 | Processor调用runExport | 导出 | 队列消费 | P0 | BullMQ Worker 运行中 | - | 1. 创建导出任务<br>2. 检查 Processor 日志 | 日志显示 'export job completed: exportId=...' | | |
| TC-EXP-091 | 导出文件生成到存储服务 | 导出 | 文件生成 | P0 | 存储服务正常 | - | 1. 创建导出任务<br>2. 任务完成后检查 fileUrl | fileUrl 指向存储服务（本地或 OSS） | | |
| TC-EXP-092 | 大数据导出异步不阻塞HTTP | 导出 | 异步验证 | P0 | 导出数据量大 | 10000+条数据 | 1. 创建大数据导出任务<br>2. 立即检查响应时间 | POST /api/exports 响应时间 < 1秒，返回 processing 状态 | | |

---

## 6. 导出脱敏用例

### 6.1 普通员工脱敏

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|----------|------|
| TC-EXP-100 | 运营导出客资联系方式脱敏 | 导出 | 运营导出客资 | P0 | 运营登录，有客资数据 | 运营 token、客资含完整联系方式 | 1. 运营创建 leads 导出<br>2. 下载 CSV<br>3. 检查联系方式列 | 联系方式格式为 138****1234（前3后4，中间***） | | |
| TC-EXP-101 | 销售导出订单客户联系方式脱敏 | 导出 | 销售导出订单 | P0 | 销售登录，有订单数据 | 销售 token、订单关联客资含完整联系方式 | 1. 销售创建 orders 导出<br>2. 下载 CSV<br>3. 检查联系方式列 | 联系方式脱敏显示 | | |
| TC-EXP-102 | 教务导出订单进度联系方式脱敏 | 导出 | 教务导出订单进度 | P0 | 教务登录，有跟进记录 | 教务 token | 1. 教务创建 order_progress 导出<br>2. 下载 CSV<br>3. 检查联系方式列 | 联系方式脱敏显示 | | |
| TC-EXP-103 | 7位以下联系方式脱敏处理 | 导出 | 短号码脱敏 | P1 | 客资联系方式为短号（如 1234567） | 运营 token、7位联系方式 | 1. 运营导出含短号的客资<br>2. 检查 CSV | 脱敏为 1***（保留首字符+***） | | |
| TC-EXP-104 | 空联系方式脱敏处理 | 导出 | 空值脱敏 | P1 | 客资联系方式为空 | 运营 token | 1. 运营导出联系人为空的客资<br>2. 检查 CSV | 导出为空字符串，不报错 | | |

### 6.2 主管完整字段导出

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-110 | 主管导出客资联系方式不脱敏 | 导出 | 主管权限 | P0 | 主管登录，有客资数据 | 主管 token | 1. 主管创建 leads 导出<br>2. 下载 CSV<br>3. 检查联系方式列 | 联系方式完整显示，无脱敏 | | |
| TC-EXP-111 | 主管导出订单联系方式不脱敏 | 导出 | 主管权限 | P0 | 主管登录，有订单数据 | 主管 token | 1. 主管创建 orders 导出<br>2. 下载 CSV<br>3. 检查联系方式列 | 联系方式完整显示 | | |
| TC-EXP-112 | Admin用户导出联系方式不脱敏 | 导出 | Admin权限 | P0 | admin用户登录 | admin token | 1. admin 创建导出<br>2. 下载 CSV | 联系方式完整显示 | | |
| TC-EXP-113 | Owner用户导出联系方式不脱敏 | 导出 | Owner权限 | P0 | owner用户登录 | owner token | 1. owner 创建导出<br>2. 下载 CSV | 联系方式完整显示 | | |

### 6.3 脱敏对比测试

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-120 | 同一客资普通员工vs主管导出对比 | 导出 | 脱敏规则验证 | P0 | 同一条客资，联系方式 13812345678 | 运营 token vs 主管 token | 1. 运营导出该客资<br>2. 主管导出该客资<br>3. 对比两个 CSV | 运营导出：138***5678<br>主管导出：13812345678 | | |

---

## 7. 导出联调用例

### 7.1 运营导出场景

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|----------|------|
| TC-EXP-130 | 运营导出自己的客资完整链路 | 导出 | 运营导出联调 | P0 | 运营录入过客资 | 运营角色 | 1. 运营登录<br>2. 进入客资看板<br>3. 设置筛选条件<br>4. 点击导出<br>5. 进入导出中心<br>6. 下载文件 | 导出任务出现在导出中心，文件包含自己录入的客资 | | ⚠️ 前端按钮待补 |
| TC-EXP-131 | 运营导出自己的作品完整链路 | 导出 | 运营导出联调 | P1 | 运营录入过作品 | 运营角色 | 1. 运营登录<br>2. 进入我的作品<br>3. 点击导出<br>4. 下载文件 | 导出任务成功，文件包含自己的作品 | | ⚠️ A端占位 |
| TC-EXP-132 | 运营只能导出自己录入的客资 | 导出 | 范围验证 | P0 | 运营A和运营B各录入客资 | 运营A token | 1. 运营A导出客资<br>2. 检查CSV | 导出结果只包含运营A录入的客资，不包含运营B的 | | |
| TC-EXP-133 | 运营导出后收到通知 | 导出 | 通知验证 | P1 | 运营创建了导出任务 | 运营角色、notifications表 | 1. 运营创建导出<br>2. 导出完成后<br>3. 检查消息中心 | 有 export_done/export_finished 类型通知 | | |

### 7.2 销售导出场景

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|----------|------|
| TC-EXP-140 | 销售导出自己的客资完整链路 | 导出 | 销售导出联调 | P0 | 销售负责客资 | 销售角色 | 1. 销售登录<br>2. 进入我的客资<br>3. 点击导出<br>4. 下载文件 | 导出任务成功，文件包含自己负责的客资 | | ✅ 已实现 |
| TC-EXP-141 | 销售导出自己的订单完整链路 | 导出 | 销售导出联调 | P0 | 销售有订单 | 销售角色 | 1. 销售登录<br>2. 进入订单跟进<br>3. 点击导出<br>4. 下载文件 | 导出任务成功，文件包含自己的订单 | | ✅ 已实现 |
| TC-EXP-142 | 销售只能导出分配给自己的客资 | 导出 | 范围验证 | P0 | 销售A和销售B各负责不同客资 | 销售A token | 1. 销售A导出客资<br>2. 检查CSV | 导出结果只包含销售A负责的客资 | | |
| TC-EXP-143 | 销售导出后收到通知 | 导出 | 通知验证 | P0 | 销售创建了导出任务 | 销售角色、notifications表 | 1. 销售创建导出<br>2. 导出完成后<br>3. 检查消息中心 | 有 export_done/export_finished 类型通知 | | |

### 7.3 教务导出场景

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|----------|------|
| TC-EXP-150 | 教务导出自己负责的订单完整链路 | 导出 | 教务导出联调 | P0 | 教务负责订单 | 教务角色 | 1. 教务登录<br>2. 进入订单池<br>3. 点击导出<br>4. 下载文件 | 导出任务成功，文件包含自己负责的订单 | | ✅ 已实现 |
| TC-EXP-151 | 教务导出订单进度完整链路 | 导出 | 教务导出联调 | P0 | 教务有跟进记录 | 教务角色 | 1. 教务登录<br>2. 进入导出中心<br>3. 选择订单进度类型<br>4. 导出<br>5. 下载 | 导出任务成功，文件包含订单跟进记录 | | ✅ 已实现 |
| TC-EXP-152 | 教务只能导出自己+池单订单 | 导出 | 范围验证 | P0 | 教务A和教务B各负责不同订单 | 教务A token | 1. 教务A导出订单<br>2. 检查CSV | 导出结果包含教务A负责的订单+池单 | | |
| TC-EXP-153 | 教务导出后收到通知 | 导出 | 通知验证 | P0 | 教务创建了导出任务 | 教务角色、notifications表 | 1. 教务创建导出<br>2. 导出完成后<br>3. 检查消息中心 | 有 export_done 类型通知 | | |

### 7.4 主管导出场景

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|----------|------|
| TC-EXP-160 | 主管导出全局客资完整链路 | 导出 | 主管导出联调 | P0 | 系统存在客资数据 | 主管角色 | 1. 主管登录<br>2. 进入客资看板<br>3. 设置筛选条件（可全选）<br>4. 点击导出<br>5. 下载文件 | 导出任务成功，文件包含所有客资（权限内） | | ✅ 已实现 |
| TC-EXP-161 | 主管导出全局订单完整链路 | 导出 | 主管导出联调 | P0 | 系统存在订单数据 | 主管角色 | 1. 主管登录<br>2. 进入订单看板<br>3. 点击导出<br>4. 下载文件 | 导出任务成功，文件包含所有订单 | | ✅ 已实现 |
| TC-EXP-162 | 主管导出协同记录完整链路 | 导出 | 主管导出联调 | P0 | 系统存在协同记录 | 主管角色 | 1. 主管登录<br>2. 进入协同管理<br>3. 点击导出<br>4. 下载文件 | 导出任务成功，文件包含所有协同记录 | | ✅ 已实现 |
| TC-EXP-163 | 主管导出全局作品完整链路 | 导出 | 主管导出联调 | P1 | 系统存在作品数据 | 主管角色 | 1. 主管登录<br>2. 进入作品看板<br>3. 点击导出<br>4. 下载文件 | 导出任务成功，文件包含所有作品 | | ⚠️ A端占位 |
| TC-EXP-164 | 主管导出排行榜完整链路 | 导出 | 主管导出联调 | P1 | 排行榜数据存在 | 主管角色 | 1. 主管登录<br>2. 进入排行榜<br>3. 点击导出<br>4. 下载文件 | 导出任务成功，文件包含排行榜数据 | | ⚠️ A端占位 |
| TC-EXP-165 | 主管导出后收到通知 | 导出 | 通知验证 | P0 | 主管创建了导出任务 | 主管角色、notifications表 | 1. 主管创建导出<br>2. 导出完成后<br>3. 检查消息中心 | 有 export_done 类型通知 | | |

### 7.5 导出中心联调

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-170 | 各端口导出任务进入导出中心 | 导出 | 导出中心验证 | P0 | 各端口用户创建导出任务 | 运营/销售/教务/主管各角色 | 1. 各角色分别创建导出任务<br>2. 进入各角色的导出中心 | 各角色的导出中心显示自己创建的任务 | | ✅ 已实现 |
| TC-EXP-171 | 导出中心展示任务状态 | 导出 | 导出中心验证 | P0 | 存在各状态的导出任务 | - | 1. 进入导出中心<br>2. 查看任务列表 | 待处理、处理中、成功、失败 状态正确显示 | | |
| TC-EXP-172 | 导出中心支持下载 | 导出 | 导出中心验证 | P0 | 存在已完成的导出任务 | - | 1. 进入导出中心<br>2. 点击已完成任务的下载 | 文件下载成功 | | |

---

## 8. 性能指标基线表

| 指标名称 | 目标值 | VU=10 实测 | VU=50 实测 | VU=100 实测 | 说明 |
|----------|--------|------------|------------|-------------|------|
| 列表分页查询响应时间 | p95 < 1500ms | 561ms ✅ | 2378ms ❌ | 7044ms ❌ | 客资/订单/作品列表 |
| 状态更新响应时间 | p95 < 1000ms | - | - | - | 500ms~1s 目标 |
| 通知实时推送 | p95 < 3000ms | - | - | - | 在线消息3秒内到达 |
| 小数据导出总耗时 | < 30s | - | - | - | <100条数据 |
| 大数据导出 | 异步不阻塞 | - | - | - | 不阻塞页面 |

---

## 9. 性能联调用例

### 9.1 列表分页查询性能

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-001 | 客资列表分页查询1.5秒内返回 | 性能 | 客资列表查询 | P0 | 数据库有客资数据，limit=20 | - | 1. 调用 GET /api/leads?limit=20&offset=0<br>2. 记录响应时间 | 响应时间 < 1500ms，返回200 | | |
| TC-PERF-002 | 订单列表分页查询1.5秒内返回 | 性能 | 订单列表查询 | P0 | 数据库有订单数据，limit=20 | - | 1. 调用 GET /api/orders?limit=20&offset=0<br>2. 记录响应时间 | 响应时间 < 1500ms，返回200 | | |
| TC-PERF-003 | 作品列表分页查询1.5秒内返回 | 性能 | 作品列表查询 | P0 | 数据库有作品数据，limit=20 | - | 1. 调用 GET /api/posts?limit=20&offset=0<br>2. 记录响应时间 | 响应时间 < 1500ms，返回200 | | |
| TC-PERF-004 | 客资列表分页多条件筛选1.5秒内返回 | 性能 | 客资筛选查询 | P0 | 数据库有客资数据 | - | 1. 调用 GET /api/leads?limit=20&status=new&platform=xiaohongshu<br>2. 记录响应时间 | 响应时间 < 1500ms | | |
| TC-PERF-005 | 订单列表分页多条件筛选1.5秒内返回 | 性能 | 订单筛选查询 | P0 | 数据库有订单数据 | - | 1. 调用 GET /api/orders?limit=20&orderStatus=in_progress<br>2. 记录响应时间 | 响应时间 < 1500ms | | |
| TC-PERF-006 | 客资列表深度分页性能 | 性能 | 大偏移量查询 | P1 | 数据库有大量客资 | - | 1. 调用 GET /api/leads?limit=20&offset=1000<br>2. 记录响应时间 | 响应时间 < 2000ms | | |

### 9.2 录入操作性能

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-010 | 录入客资1秒内保存 | 性能 | 客资创建 | P0 | - | 完整的客资字段 | 1. 调用 POST /api/leads<br>2. 记录响应时间 | 响应时间 < 1000ms，返回201 | | |
| TC-PERF-011 | 录入作品1秒内保存 | 性能 | 作品创建 | P0 | - | 完整的作品字段 | 1. 调用 POST /api/posts<br>2. 记录响应时间 | 响应时间 < 1000ms，返回201 | | |
| TC-PERF-012 | 客资创建触发事务完成 | 性能 | 事务验证 | P0 | - | 客资+附件+通知 | 1. 创建带截图的客资<br>2. 检查客资、附件、通知是否同时成功 | 客资、附件、通知同时成功或同时回滚 | | |

### 9.3 状态更新性能

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-020 | 状态更新500毫秒到1秒内完成 | 性能 | 状态变更 | P0 | 存在客资/订单 | - | 1. 调用 PATCH /api/leads/:id/status<br>2. 记录响应时间 | 响应时间在 500ms~1000ms 之间 | | |
| TC-PERF-021 | 订单状态更新乐观锁生效 | 性能 | 并发控制 | P1 | 存在订单 | - | 1. 同时发起两个更新请求<br>2. 检查其中一个被拒绝 | 只有一个成功，另一个返回409 Conflict | | |

### 9.4 通知实时性能

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-030 | 在线消息3秒内到达 | 性能 | WebSocket推送 | P0 | 用户在线（WebSocket连接） | - | 1. 用户A创建触发通知的操作<br>2. 用户B在线<br>3. 记录通知到达时间 | 通知在3秒内出现在用户B界面 | | ✅ 已实现 |
| TC-PERF-031 | 离线用户60秒兜底轮询 | 性能 | 轮询验证 | P1 | 用户离线 | - | 1. 用户离线<br>2. 触发通知<br>3. 等待轮询间隔<br>4. 用户重新上线 | 离线期间的通知通过轮询获取 | | |

### 9.5 导出性能

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-040 | 小数据导出30秒内完成 | 性能 | 小数据导出 | P0 | 数据量 < 100条 | - | 1. 创建导出任务（数据量<100）<br>2. 轮询状态直到完成<br>3. 记录总耗时 | 总耗时 < 30000ms | | |
| TC-PERF-041 | 大数据导出异步不阻塞页面 | 性能 | 大数据导出 | P0 | 数据量 > 5000条 | - | 1. 创建大数据导出任务<br>2. 立即检查响应状态 | POST 立即返回 processing，不阻塞 | | |
| TC-PERF-042 | 大数据导出生成后通知用户 | 性能 | 异步通知 | P0 | 大数据导出完成 | - | 1. 创建大数据导出<br>2. 等待完成<br>3. 检查通知 | 用户收到 export_done 通知 | | |
| TC-PERF-043 | 导出接口响应时间 < 1秒 | 性能 | 导出创建 | P0 | - | - | 1. 调用 POST /api/exports<br>2. 记录响应时间 | 响应时间 < 1000ms | | |

### 9.6 VU=10 压测场景

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-050 | VU=10列表分页p95<1.5s | 性能 | 压测 | P0 | 种子数据充足 | VU=10, duration=10m | 1. 运行 k6 脚本 phase-a/01-list-pagination.js<br>2. 检查阈值 | http_req_duration p95 < 1500ms，错误率 < 1% | | ✅ 达标561ms |
| TC-PERF-051 | VU=10状态更新p95<1s | 性能 | 压测 | P1 | - | VU=10, duration=5m | 1. 运行 k6 脚本 phase-a/02-status-update.js<br>2. 检查阈值 | http_req_duration p95 < 1000ms | | |
| TC-PERF-052 | VU=10导出异步p95<30s | 性能 | 压测 | P1 | - | VU=10, duration=5m | 1. 运行 k6 脚本 phase-b/07-export-async.js<br>2. 检查阈值 | 总耗时 p95 < 30000ms | | |

### 9.7 VU=50 压测场景

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|---------|------|
| TC-PERF-060 | VU=50列表分页p95<1.5s | 性能 | 压测 | P1 | 种子数据充足 | VU=50, duration=10m | 1. 运行 k6 脚本 K6_VUS=50<br>2. 检查阈值 | http_req_duration p95 < 1500ms | | ❌ 2378ms超 |
| TC-PERF-061 | VU=50混合负载p95<2s | 性能 | 压测 | P1 | - | VU=50, duration=10m | 1. 运行 k6 脚本 phase-a/04-mixed-workload.js K6_VUS=50<br>2. 检查阈值 | http_req_duration p95 < 2000ms | | |
| TC-PERF-062 | VU=50压测Node单线程瓶颈验证 | 性能 | 瓶颈分析 | P1 | VU=50压测数据 | - | 1. 运行 VU=50 压测<br>2. 分析耗时分布 | 主耗时在 Node 事件循环，非 SQL 慢 | | |

### 9.8 VU=100 压测场景

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|---------|------|
| TC-PERF-070 | VU=100列表分页p95<1.5s | 性能 | 压测 | P1 | 种子数据充足 | VU=100, duration=10m | 1. 运行 k6 脚本 K6_VUS=100<br>2. 检查阈值 | http_req_duration p95 < 1500ms | | ❌ 7044ms超 |
| TC-PERF-071 | VU=100混合负载压测 | 性能 | 压测 | P1 | - | VU=100, duration=10m | 1. 运行 k6 脚本 phase-a/04-mixed-workload.js K6_VUS=100<br>2. 检查阈值 | http_req_duration p95 < 3000ms | | |
| TC-PERF-072 | 50-100人同时在线基础压测 | 性能 | 压测 | P1 | 种子数据充足 | VU=50~100 | 1. 运行完整压测套件<br>2. 检查错误率 | 错误率 < 2%，无崩溃 | | |

---

## 10. 索引验证用例

### 10.1 leads 表索引

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-080 | leads表employee_id索引验证 | 性能 | 索引验证 | P0 | M3/M11/M13/M14/M15/M16/M17迁移已执行 | - | 1. 执行 EXPLAIN SELECT * FROM leads WHERE employee_id=?<br>2. 检查 type 和 key 列 | type=ref 或 eq_ref，key 使用索引 | | |
| TC-PERF-081 | leads表assigned_sales_user_id索引验证 | 性能 | 索引验证 | P0 | - | - | 1. 执行 EXPLAIN SELECT * FROM leads WHERE assigned_sales_user_id=?<br>2. 检查索引使用 | 使用索引，type=ref | | |
| TC-PERF-082 | leads表status索引验证 | 性能 | 索引验证 | P1 | - | - | 1. 执行 EXPLAIN SELECT * FROM leads WHERE status=?<br>2. 检查索引使用 | 使用索引 | | |
| TC-PERF-083 | leads表复合索引验证(员工+状态) | 性能 | 索引验证 | P1 | - | - | 1. 执行 EXPLAIN SELECT * FROM leads WHERE employee_id=? AND status=?<br>2. 检查索引使用 | 使用复合索引 | | |

### 10.2 orders 表索引

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-090 | orders表sales_user_id索引验证 | 性能 | 索引验证 | P0 | - | - | 1. 执行 EXPLAIN SELECT * FROM orders WHERE sales_user_id=?<br>2. 检查索引使用 | 使用索引，type=ref | | |
| TC-PERF-091 | orders表academic_user_id索引验证 | 性能 | 索引验证 | P0 | - | - | 1. 执行 EXPLAIN SELECT * FROM orders WHERE academic_user_id=?<br>2. 检查索引使用 | 使用索引，type=ref | | |
| TC-PERF-092 | orders表order_status索引验证 | 性能 | 索引验证 | P1 | - | - | 1. 执行 EXPLAIN SELECT * FROM orders WHERE order_status=?<br>2. 检查索引使用 | 使用索引 | | |
| TC-PERF-093 | orders表created_at索引验证 | 性能 | 索引验证 | P1 | - | - | 1. 执行 EXPLAIN SELECT * FROM orders ORDER BY created_at DESC LIMIT 20<br>2. 检查索引使用 | 使用索引，Extra 不包含 filesort | | |

### 10.3 posts 表索引

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|---------|------|
| TC-PERF-100 | posts表employee_id索引验证 | 性能 | 索引验证 | P0 | - | - | 1. 执行 EXPLAIN SELECT * FROM posts WHERE employee_id=?<br>2. 检查索引使用 | 使用索引，type=ref | | |
| TC-PERF-101 | posts表account_id索引验证 | 性能 | 索引验证 | P1 | - | - | 1. 执行 EXPLAIN SELECT * FROM posts WHERE account_id=?<br>2. 检查索引使用 | 使用索引 | | |
| TC-PERF-102 | posts表published_at索引验证 | 性能 | 索引验证 | P1 | - | - | 1. 执行 EXPLAIN SELECT * FROM posts ORDER BY published_at DESC LIMIT 20<br>2. 检查索引使用 | 使用索引，无 filesort | | |

### 10.4 accounts 表索引

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-110 | accounts表employee_id索引验证 | 性能 | 索引验证 | P0 | - | - | 1. 执行 EXPLAIN SELECT * FROM accounts WHERE employee_id=?<br>2. 检查索引使用 | 使用索引 | | |
| TC-PERF-111 | accounts表platform索引验证 | 性能 | 索引验证 | P1 | - | - | 1. 执行 EXPLAIN SELECT * FROM accounts WHERE platform=?<br>2. 检查索引使用 | 使用索引 | | |

### 10.5 notifications 表索引

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-120 | notifications表receiver_id索引验证 | 性能 | 索引验证 | P0 | - | - | 1. 执行 EXPLAIN SELECT * FROM notifications WHERE receiver_id=?<br>2. 检查索引使用 | 使用索引 | | |
| TC-PERF-121 | notifications表is_read索引验证 | 性能 | 索引验证 | P1 | - | - | 1. 执行 EXPLAIN SELECT * FROM notifications WHERE receiver_id=? AND is_read=?<br>2. 检查索引使用 | 使用复合索引 | | |

---

## 11. 缓存验证用例

### 11.1 Dashboard缓存验证

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-130 | CacheService 5分钟TTL验证 | 性能 | 缓存验证 | P0 | dashboard.service.ts 已配置缓存 | - | 1. 首次调用 dashboard API<br>2. 5分钟内再次调用<br>3. 检查响应时间 | 第二次响应时间显著低于首次（命中缓存） | | commit 45e9513 |
| TC-PERF-131 | Dashboard缓存key隔离 | 性能 | 缓存验证 | P1 | - | 不同用户/参数 | 1. 用户A调用 dashboard<br>2. 用户B调用 dashboard<br>3. 检查缓存key | 缓存key包含用户标识，不同用户不混用缓存 | | |
| TC-PERF-132 | Dashboard数据变更后缓存失效 | 性能 | 缓存失效 | P1 | 缓存已建立 | - | 1. 调用 dashboard API 建立缓存<br>2. 修改底层数据<br>3. 等待TTL或手动失效<br>4. 再次调用 | 返回最新数据 | | |

### 11.2 缓存性能影响

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|------|------|
| TC-PERF-140 | 缓存命中时响应时间 < 100ms | 性能 | 缓存性能 | P0 | 缓存已建立 | - | 1. 调用 dashboard API（第二次）<br>2. 记录响应时间 | 响应时间 < 100ms | | |
| TC-PERF-141 | 缓存未命中时正常返回数据 | 性能 | 缓存降级 | P1 | 缓存服务异常 | - | 1. 模拟缓存不可用<br>2. 调用 dashboard API | 正常返回数据库数据，不报错 | | |

---

## 12. 压测场景用例

### 12.1 基础压测脚本验证

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-150 | 阶段A-列表分页压测脚本可执行 | 性能 | 脚本验证 | P0 | k6 已安装 | - | 1. cd load-test/k6/phase-a<br>2. k6 run 01-list-pagination.js | 脚本正常执行，输出压测报告 | | |
| TC-PERF-151 | 阶段A-状态更新压测脚本可执行 | 性能 | 脚本验证 | P1 | k6 已安装 | - | 1. k6 run 02-status-update.js<br>2. 检查输出 | 脚本正常执行 | | |
| TC-PERF-152 | 阶段A-通知实时压测脚本可执行 | 性能 | 脚本验证 | P1 | k6 已安装 | - | 1. k6 run 03-notification-realtime.js<br>2. 检查输出 | 脚本正常执行 | | |
| TC-PERF-153 | 阶段B-导出异步压测脚本可执行 | 性能 | 脚本验证 | P0 | k6 已安装 | - | 1. k6 run phase-b/07-export-async.js<br>2. 检查输出 | 脚本正常执行，p95 < 30s | | |
| TC-PERF-154 | Artillery混合负载脚本可执行 | 性能 | 脚本验证 | P1 | artillery 已安装 | - | 1. cd load-test/artillery<br>2. artillery run mixed-workload.yml<br>3. 检查输出 | 脚本正常执行 | | |

### 12.2 压测结果记录

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-160 | VU=10压测结果记录 | 性能 | 压测 | P0 | 种子数据 | VU=10, duration=10m | 1. 运行各阶段压测脚本 VU=10<br>2. 记录 p95/p99/avg/max<br>3. 保存到 load-test/results/ | 记录完整压测数据 | | |
| TC-PERF-161 | VU=50压测结果记录 | 性能 | 压测 | P1 | 种子数据 | VU=50, duration=10m | 1. 运行各阶段压测脚本 VU=50<br>2. 记录性能数据 | 记录完整压测数据 | | |
| TC-PERF-162 | VU=100压测结果记录 | 性能 | 压测 | P1 | 种子数据 | VU=100, duration=10m | 1. 运行各阶段压测脚本 VU=100<br>2. 记录性能数据 | 记录完整压测数据 | | |
| TC-PERF-163 | 压测报告生成 | 性能 | 报告生成 | P1 | 压测已完成 | - | 1. 运行所有压测脚本<br>2. 生成综合压测报告 | 报告包含各指标对比表 | | |

### 12.3 性能瓶颈分析

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-PERF-170 | Node单线程瓶颈验证 | 性能 | 瓶颈分析 | P1 | VU=50/100 压测数据 | - | 1. 分析压测耗时分布<br>2. 检查CPU使用情况 | Node事件循环是主要瓶颈，非SQL慢 | | |
| TC-PERF-171 | MySQL连接池使用分析 | 性能 | 连接池 | P1 | - | - | 1. 监控MySQL连接数<br>2. 检查连接池配置 | connectionLimit=10配置合理 | | |

---

## 13. 导出失败恢复用例

### 13.1 失败处理

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-180 | 导出处理异常时状态变为failed | 导出 | 失败恢复 | P0 | 导出处理异常 | 人为注入异常 | 1. 创建导出任务<br>2. 模拟处理异常<br>3. 查询任务状态 | status='failed' | | |
| TC-EXP-181 | 导出失败后可以重新创建 | 导出 | 失败恢复 | P0 | 有failed状态任务 | - | 1. 查询到failed任务<br>2. 重新创建同类型导出 | 创建新任务成功 | | |
| TC-EXP-182 | BullMQ job失败后主动写failed | 导出 | 失败恢复 | P1 | BullMQ Worker处理失败 | - | 1. 模拟Worker处理异常<br>2. 检查数据库 | status='failed'，由 processor 主动写入 | | |
| TC-EXP-183 | setImmediate兜底失败时写failed | 导出 | 失败恢复 | P1 | setImmediate执行失败 | - | 1. 模拟runExport异常<br>2. 检查数据库 | status='failed' | | |

### 13.2 文件清理

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-EXP-190 | 下载不存在文件返回410 | 导出 | 文件清理 | P1 | 文件已删除但数据库有fileUrl | - | 1. 删除物理文件<br>2. 调用下载接口 | 返回 410 Gone | | |

---

## 14. A端占位功能测试用例（待实现）

> 以下功能标注为 ⚠️ A端占位或待补，属于 P2 优先级

| 用例编号 | 用例标题 | 类型 | 触发场景 | 优先级 | 前置条件 | 测试数据 | 测试步骤 | 预期结果 | 实际结果 | 备注 |
|----------|----------|------|----------|--------|----------|----------|----------|----------|---------|------|
| TC-P2-001 | 运营端导出按钮接入 | 导出 | A端占位 | P2 | A端导出按钮未实现 | - | 1. 运营登录<br>2. 进入客资看板<br>3. 检查导出按钮 | 导出按钮存在且可用 | | ⚠️ 待A端实现 |
| TC-P2-002 | posts导出后端CSV生成 | 导出 | A端占位 | P2 | posts类型导出调用 | - | 1. 主管调用 POST /api/exports exportType=posts<br>2. 检查生成的CSV | CSV包含作品数据 | | ⚠️ 占位CSV待实现 |
| TC-P2-003 | rankings导出后端CSV生成 | 导出 | A端占位 | P2 | rankings类型导出调用 | - | 1. 主管调用 POST /api/exports exportType=rankings<br>2. 检查生成的CSV | CSV包含排行榜数据 | | ⚠️ 占位CSV待实现 |
| TC-P2-004 | 50-100人同时在线压测报告 | 性能 | 压测 | P2 | 压测脚本已就位 | - | 1. 运行完整压测套件 VU=50~100<br>2. 生成压测报告 | 压测报告已生成，识别瓶颈点 | | ⚠️ 未出报告 |

---

## 15. 用例统计汇总

| 类别 | 用例数量 | P0数量 | P1数量 | P2数量 | 备注 |
|------|----------|--------|--------|--------|------|
| 导出接口用例 | 55 | 25 | 30 | 0 | |
| 异步队列用例 | 13 | 6 | 7 | 0 | |
| 导出脱敏用例 | 15 | 9 | 6 | 0 | |
| 导出联调用例 | 25 | 18 | 7 | 0 | |
| 性能联调用例 | 33 | 15 | 18 | 0 | |
| 索引验证用例 | 15 | 8 | 7 | 0 | |
| 缓存验证用例 | 5 | 3 | 2 | 0 | |
| 压测场景用例 | 15 | 4 | 8 | 3 | |
| 失败恢复用例 | 5 | 2 | 3 | 0 | |
| A端占位用例 | 4 | 0 | 0 | 4 | |
| **总计** | **185** | **90** | **88** | **7** | |

---

## 附录A：压测脚本位置

| 脚本 | 路径 | 用途 |
|------|------|------|
| 列表分页压测 | load-test/k6/phase-a/01-list-pagination.js | VU=5-10, p95<1.5s |
| 状态更新压测 | load-test/k6/phase-a/02-status-update.js | p95<1s |
| 通知实时压测 | load-test/k6/phase-a/03-notification-realtime.js | p95<3s |
| 混合负载压测 | load-test/k6/phase-a/04-mixed-workload.js | 综合场景 |
| 导出异步压测 | load-test/k6/phase-b/07-export-async.js | p95<30s |
| Artillery混合负载 | load-test/artillery/mixed-workload.yml | artillery脚本 |

---

## 附录B：阈值配置（thresholds.js）

```javascript
// 列表分页
LIST_PAGINATION_THRESHOLD = {
  'http_req_duration': { p95: 1500 },    // 1.5秒
  'http_req_failed': { rate: 0.01 },      // 1%
};

// 状态更新
STATUS_UPDATE_THRESHOLD = {
  'http_req_duration': { p95: 1000 },    // 1秒
  'http_req_failed': { rate: 0.01 },
};

// 通知实时
NOTIFICATION_REALTIME_THRESHOLD = {
  'http_req_duration': { p95: 3000 },   // 3秒
  'http_req_failed': { rate: 0.01 },
};

// 导出异步
EXPORT_ASYNC_THRESHOLD = {
  'http_req_duration': { p95: 30000 },  // 30秒
  'http_req_failed': { rate: 0.01 },
};

// 混合负载
MIXED_WORKLOAD_THRESHOLD = {
  'http_req_duration': { p95: 2000 },   // 2秒
  'http_req_failed': { rate: 0.02 },
};
```

---

## 附录C：环境变量配置

| 变量 | 说明 | 默认值 |
|------|------|--------|
| REDIS_URL | BullMQ连接URL | 未配置时降级setImmediate |
| BASE_URL | 压测目标URL | http://localhost:3000 |
| K6_VUS | 压测虚拟用户数 | 脚本内默认值 |
| K6_DURATION | 压测持续时间 | 脚本内默认值 |

---

## 附录D：关键代码位置

| 文件 | 行号 | 说明 |
|------|------|------|
| exports.service.ts | 155-179 | BullMQ队列初始化和降级逻辑 |
| exports.service.ts | 207-259 | create方法，防抖机制 |
| exports.service.ts | 486-493 | maskContact脱敏规则 |
| exports.service.ts | 1023-1092 | resolveDownload权限/状态校验 |
| exports.controller.ts | 50-114 | create控制器，权限过滤 |
| exports.controller.ts | 164-195 | download控制器 |
| exports.processor.ts | 32-75 | BullMQ Worker初始化 |
| thresholds.js | - | 性能阈值配置 |
