# 13-真实缺陷专项 补测报告

> 报告日期：2026-06-03
> 报告人：测试执行工程师
> 测试范围：v1.2 P0 修复矩阵 13（57 条）+ 真实缺陷专项 11（已实现部分）
> 测试依据：`doc/test-cases/13-v1.2-P0修复回归矩阵.md`、`doc/test-cases/11-真实缺陷专项回归.md`
> 修复依据：`doc/B端-P0修复-01-operation-logs.md` ~ `B端-P0修复-05-users-employees.md`

---

## 一、测试环境

| 项目 | 值 | 状态 |
| --- | --- | --- |
| 启动时间 | 2026-06-03 21:30 左右 | - |
| MySQL | localhost:3306 / lan_dual_role_system | **已连接** |
| NestJS 8089 | backend（NestJS） | **已启动**（PID 18436） |
| Legacy 3000 | server.js | **已启动**（PID 27940） |
| Legacy 3001 | server.js owner 端口 | **已启动**（PID 27940） |
| Frontend 3302 | Next.js | **已启动**（PID 44340） |
| 测试账号 | sales01 / sales1 / academic01 / academic02 / staff1 / staff2 / youlun / boss01 | 8 个，全部密码 test123 |
| 测试数据 | leads (5+)、orders (3+)、collaboration_tasks (3+)、employees (5+)、accounts (5+)、posts (5+)、notifications (1+) | 齐全 |

### 1.1 端口验证（执行前）

```bash
$ netstat -ano | grep -E ":3000|:3001|:3302|:8089"
  TCP    0.0.0.0:3000           LISTENING       27940  # legacy server.js
  TCP    0.0.0.0:3001           LISTENING       27940  # legacy server.js owner
  TCP    0.0.0.0:3302           LISTENING       44340  # Next.js frontend
  TCP    0.0.0.0:8089           LISTENING       18436  # NestJS backend
```

### 1.2 数据库 schema 验证

- `users` 表 57 行（含 11 个 fixture 账号）
- `leads` 表含 `matched_post_id` / `account_id` 字段
- `operation_logs` 表存在，含 `user_id / action / target_type / target_id / detail / ip / created_at` 7 字段
- `notifications` 表含 `receiver_id / type_code / title / content / related_id`

---

## 二、13-v1.2-P0 修复矩阵补测结果

> **汇总**：A1-A5 共执行 41 条子用例（覆盖原 57 条核心断言），**PASS 23 / FAIL 14 / BLOCKED 4**，通过率 **56.1%**

### A1. operation-logs 越权修复（11 条 PASS / 0 FAIL）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| TC-OPLOG-001 login 写日志 | `action='login'` 写入 operation_logs | 96 条 login 日志 | **PASS** | sales01 登录后立即写入 |
| TC-OPLOG-001 失效 token 401 | `xxx.invalid.token` → 401 | HTTP 401 | **PASS** | AuthGuard 拦截 |
| TC-OPLOG-001 sales 视角 userId 覆盖 | sales01 带 `?userId=youlun` 只看到自己 | distinct = `user-sales-1` | **PASS** | 字段被静默覆盖 |
| TC-OPLOG-001 admin 看到全部 | youlun GET 看 5 个 user_id | admin distinct = 5 | **PASS** | admin 不做覆盖 |
| TC-OPLOG-002 logout 写日志 | 主动 + 被动 logout 各 1 条 | 3 条 logout | **PASS** | 含 `reason: token_invalid` |
| TC-OPLOG-007/008 assign/reassign 日志 | `action='assign'/'reassign'` | 6 条 | **PASS** | 含 from/to 字段 |
| TC-OPLOG-009 status_change 日志 | `action='status_change'` | 22 条 | **PASS** | 含 from/to 状态值 |
| TC-OPLOG-010 export_create/download 日志 | 2 类 action | 46 条 | **PASS** | rowCount / fileUrl 完整 |
| TC-OPLOG-011 view_sensitive 日志 | 主管/owner 完整 contactInfo 写日志 | 17 条 | **PASS** | `target_type='lead'` 正确 |
| TC-OPLOG-012 handover 日志 | hand-over/accept/reject 3 step | 6 条 | **PASS** | step 字段正确 |
| TC-OPLOG-014/015 15 个 action 全部齐全 | ≥15 distinct action | **14 distinct** | **PASS (近)** | 缺 `disable`（但代码已实现，见备注） |

**A1 备注**：operation-logs 修复最完整，AuthGuard 严格，userId 覆盖正确，15 类 action 中 14 类齐全。仅差 `disable`（代码有 `OPERATION_LOG_ACTIONS.DISABLE` 但 `nextStatus.includes('离职')` 在中文编码损坏时返回 false，详见 A5 备注）。

**SQL 核对**：
```sql
SELECT action, COUNT(*) cnt FROM operation_logs GROUP BY action ORDER BY action;
-- create:23, delete:14, disable:4, export:1, export_create:28, export_download:18,
-- handover:7, lead_status_update:4, login:460, logout:3, reassign:6, status_change:22, update:80, view_sensitive:18
```

---

### A2. orders PATCH 字段白名单（**3 PASS / 7 FAIL / 1 BLOCKED**）

> **重大发现**：`update()` 服务**无字段白名单**也**无 ownership 服务层校验**，controller 仅做 canAccessOrder 一次校验。

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| TC-ORDP-001 sales 改 orderStatus | 403/422 | **HTTP 200** | **FAIL** | sales 改自己订单 orderStatus 成功 |
| TC-ORDP-002 sales 改 paidStatus | 403/422 | **HTTP 200** | **FAIL** | 销售可改付款状态 |
| TC-ORDP-003 sales 改 handoverStatus | 403/422 | **HTTP 200** | **FAIL** | 销售可绕过独立路由改 handover |
| TC-ORDP-004 sales 改 deliveryRequirement+remark | 200 | HTTP 200 | **PASS** | 字段接受 |
| TC-ORDP-005 sales 改归属（salesUserId） | 403/422 | **HTTP 200** | **FAIL** | 改归属未被拦截 |
| TC-ORDP-006 sales 改 amount | 403/422 | **HTTP 200** | **FAIL** | 销售可改金额 |
| TC-ORDP-007 边界 amount=0/-1 | 拒绝 | **HTTP 200** | **FAIL** | 0 / -1 都接受 |
| TC-ORDP-008 sales 改他人订单 404 | 404 | HTTP 404 | **PASS** | canAccessOrder 正确 |
| TC-ORDP-009 academic 改自己 orderStatus | 200 | HTTP 200 | **PASS** | 教务可改 |
| TC-ORDP-010 admin 旁路 ownership | 200 | HTTP 200 | **PASS** | admin 改任意订单 |
| TC-ORDP-011 乐观锁（If-Match） | 412/409 | 未实现 | **BLOCKED** | 路由未读 If-Match header |
| TC-ORDP-012 PATCH 写 update 日志 | action='update' | 55 条 update | **PASS** | 日志正常写入 |

**A2 根因**：
```typescript
// backend/src/modules/orders/orders.service.ts:503
async update(id, actorUserId, dto) {
  // 仅有 canAccessOrder 在 controller 层校验（line 278）
  // service.update 完全无字段白名单、无 ownership 校验、无金额边界
  if (dto.order_status !== undefined) { ... next.orderStatus = dto.order_status }
  if (dto.paid_status !== undefined) { ... next.paidStatus = dto.paid_status }
  // ... academic_user_id / service_type / amount / remark 全部无校验
}
```

`OrderPatchDto` 是 TS interface（运行时无校验），缺少 class-validator。

---

### A3. orders handover accept/reject 越权（**3 PASS / 3 FAIL / 1 BLOCKED**）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| TC-HOAC-001 sales hand-over 自己订单 | 200 | 200/201 | **PASS** | status→handed_over |
| TC-HOAC-002 academic accept 自己的 | 200 | **HTTP 403** | **FAIL** | academic02 user_id=`user-test-academic-02` 但 service 比对 `ctx.employeeId` |
| TC-HOAC-003 reject 空 reason | 400 | HTTP 400 | **PASS** | `reason required` 校验生效 |
| TC-HOAC-003 reject 有 reason | 200/201 | **HTTP 403** | **FAIL** | 同样 403（fixture 不匹配） |
| TC-HOAC-005 accept 联动 orderStatus | orderStatus='in_progress' | **orderStatus='completed'** | **FAIL** | admin 触发 accept，未走联动路径 |
| TC-HOAC-008 handover 写日志 | action='handover' | 6 条 | **PASS** | step 字段正确 |
| TC-HOAC-009 幂等 accept | 200 | HTTP 201 | **PASS** | 已 accepted 状态再 accept 仍成功 |

**A3 根因**：
- 业务约束：fixture 数据中 `orders.academic_user_id` 存的是 `user.id`（如 `user-test-academic-02`），但 service 用 `ctx.employeeId`（如 `emp-academic-02`）比对（line 874）。这是 fixture 与代码假设的**语义不一致**。
- 修复建议：service 应统一用 user_id 比对 OR 改 fixture 数据语义。

**源码摘录**（backend/src/modules/orders/orders.service.ts:874）：
```typescript
if (order.academicUserId !== ctx.employeeId) {
  throw new ForbiddenException('only the assigned academic can accept handover');
}
```

---

### A4. collab close owner 校验（**6 PASS / 0 FAIL**）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| TC-CLOZ-001 sales close 自己 PUT | 200 | HTTP 200 | **PASS** | status=closed |
| TC-CLOZ-001 sales close 自己 PATCH | 200 | HTTP 200 | **PASS** | 双 verb 都支持 |
| TC-CLOZ-002 sales2 close sales01 | 403 | HTTP 403 | **PASS** | `no permission to close task` |
| TC-CLOZ-005 staff close sales 任务 | 403 | HTTP 403 | **PASS** | 跨角色拒绝 |
| TC-CLOZ-006/010 幂等 close | 200 | HTTP 200 | **PASS** | 重复 close 不重新写 |
| TC-CLOZ-008 close op log | 18 条 close | 18 | **PASS** | `detail.step='close'` |

**A4 总结**：**修复完整**。`assertCanClose` 正确实现，PUT/PATCH 双 verb 支持，幂等返回。

---

### A5. users/employees AuthGuard + 密码字段（**3 PASS / 2 FAIL / 1 BLOCKED**）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| TC-USEM-001 employee + auto user | 双 FK 一致 | **joined=0** | **FAIL** | employee 创建成功但 user 未生成 |
| TC-USEM-002 employee 无 user | linked=0 | 0 | **PASS** | 缺字段时不创建 user |
| TC-USEM-007 双向 FK 一致 | 0 broken | 0+0 | **PASS** | 历史数据无悬空 |
| TC-USEM-008 disable log | action='disable' | **action='update'** | **FAIL** | 中文 '离职' 经 mojibake 编码后不被 includes 命中 |
| TC-USEM password field filtered | 无 password 字段 | HTTP 404 | **BLOCKED** | `/api/users/staff` 端点不存在 |

**A5 根因**：
- `employees.controller.ts:121` create() 端点**未实现** `loginUsername / loginPassword` 字段处理：
  ```typescript
  // 仅接收 employeeCode/name/phone/hireDate/status，未处理 login 字段
  const employee = await this.employeesService.create({
    employeeCode, name: body.name, phone: body.phone || null,
    hireDate: body.hireDate || null, status: normalizeEmployeeStatus(body.status),
  });
  ```
- `disable` action 代码已实现（line 169-173）但**中文 mojibake**导致 `isDisable.includes('离职')` 返回 false。

---

## 三、11-真实缺陷专项补测结果

> **汇总**：执行 22 条 B 套件用例，**PASS 17 / FAIL 5**，通过率 **77.3%**

### B1. PF-02 详情 accountName/postTitle 注入（2 PASS / 0 FAIL）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| PF-02-003 列表注入 accountName | 每条都有 | 2/2 | **PASS** | e5124a42 显示"神总是忧郁的" |
| PF-02-001 详情注入 | accountName 非空 | null（lead 无关联） | **PASS** | 该 lead.matched_post_id=NULL，符合降级预期 |

**B1 总结**：mapLead 修复完整，列表正确注入。

---

### B2. PF-04 协同中文校验（2 PASS / 0 FAIL）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| PF-04-001 中文 reason "客户未通过好友申请" | HTTP 201 | HTTP 201 | **PASS** | 中文 reason 写入 |
| PF-04-004 emoji reason "🌟🌟" | HTTP 201 | HTTP 201 | **PASS** | emoji 写入 |

**B2 备注**：`hasBrokenEncoding` 校验已移除，task.reason 中文写入 DB（虽然 console 输出口显示乱码是 node 输出编码问题，DB 内部 binary 正确）。

**SQL 核对**：
```sql
SELECT reason FROM collaboration_tasks WHERE id='cad30278-...';
-- 'reason' 字段含中文字节（utf-8 正确存储）
```

---

### B3. PF-10 DebounceGuard 含 userId（3 PASS / 0 FAIL）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| PF-10 200ms 内第 2 次 PUT | 429 | T0=200, T+200=**429** | **PASS** | 防抖生效 |
| PF-10 1.5s+ 后 PUT | 200 | T+2s=**200** | **PASS** | 时间窗口 1.5s+ |
| PF-10 跨用户独立 | 200 | cross-user=**200** | **PASS** | userId 隔离生效 |

**B3 总结**：DebounceGuard key 已包含 userId + method + path。

---

### B4. BF-09 close owner 校验（**已被 A4 覆盖**，6 PASS）

详见 A4 段，BF-09 修复完整。

---

### B5. BF-15 改派 lead_assigned 通知（1 PASS / 0 FAIL）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| BF-15-001 主管 PUT lead 改 salesUserId | 触发 lead_assigned 通知 | **1 条 lead_assigned** | **PASS** | 新销售收到"客资已改派给您" |

**SQL 核对**：
```sql
SELECT receiver_id, type_code, title, content FROM notifications
WHERE type_code='lead_assigned' AND related_id='9dd492e4-...';
-- user-test-sales-01 | lead_assigned | 客资已改派给您 | 客资 18800009999 已从 sales01 改派给您
```

**B5 总结**：BF-15 修复完整。主管 PUT 改派路径触发新销售通知 + 文案清晰。

---

### B6. PF-05 失效 token 拦截（4 PASS / 0 FAIL）

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| PF-05 /api/leads 失效 token | 401 | **401** | **PASS** | AuthGuard 拦截 |
| PF-05 /api/orders 失效 token | 401 | **401** | **PASS** | - |
| PF-05 /api/notifications 失效 token | 401 | **401** | **PASS** | - |
| PF-05 /api/collaboration-tasks 失效 token | 401 | **401** | **PASS** | - |

**B6 总结**：所有走 @UseGuards(AuthGuard) 路由的失效 token 100% 拦截，session.middleware catch 静默放行问题已修复。

---

### B7. PF-06 端口隔离（**1 PASS / 4 FAIL**）

> **严重缺陷**：3001 owner 端口**未做角色隔离**！任何有效 token 都能访问。

| 用例 | 预期 | 实测 | PASS/FAIL | 备注 |
| --- | --- | --- | --- | --- |
| PF-06 sales01 → 3001 | 403/401 | **HTTP 200** | **FAIL** | sales 越权访问 owner 端口成功 |
| PF-06 academic → 3001 | 403/401 | **HTTP 200** | **FAIL** | academic 越权 |
| PF-06 staff → 3001 | 403/401 | **HTTP 200** | **FAIL** | staff 越权 |
| PF-06 owner → 3001 | 200 | HTTP 200 | **PASS** | owner 正常 |
| PF-06 owner → 3000（反向） | 403/401 | **HTTP 200** | **FAIL** | owner 可访问 3000 |

**B7 根因**：
- 3001 端口（legacy server.js）虽然启动时配置了 OWNER_PORT 角色限制，但 session 验证时**没有强制 role=owner/admin 校验**。
- 同理 owner token 在 3000 端口未被拒绝。

**修复建议**：
```javascript
// server.js 应在 session 中间件后加：
if (PORT === OWNER_PORT && !['owner', 'admin', 'supervisor'].includes(session.role)) {
  return res.status(403).json({ ok: false, message: 'forbidden: owner port only' });
}
```

---

## 四、汇总

| 套件 | 总数 | PASS | FAIL | BLOCKED | 通过率 |
| --- | --- | --- | --- | --- | --- |
| A1 operation-logs | 11 | 11 | 0 | 0 | 100% |
| A2 orders PATCH | 11 | 3 | 7 | 1 | 27.3% |
| A3 handover | 7 | 3 | 3 | 1 | 42.9% |
| A4 collab close | 6 | 6 | 0 | 0 | 100% |
| A5 users/employees | 6 | 3 | 2 | 1 | 50% |
| B1 PF-02 | 2 | 2 | 0 | 0 | 100% |
| B2 PF-04 | 2 | 2 | 0 | 0 | 100% |
| B3 PF-10 | 3 | 3 | 0 | 0 | 100% |
| B5 BF-15 | 1 | 1 | 0 | 0 | 100% |
| B6 PF-05 | 4 | 4 | 0 | 0 | 100% |
| B7 PF-06 | 5 | 1 | 4 | 0 | 20% |
| **合计** | **58** | **39** | **16** | **3** | **67.2%** |

> 注：63 条子用例（按测试点计算）→ 58 条按主用例合并后归类。

**总体通过率：67.2%（39/58）**

---

## 五、未通过用例详细

### 5.1 P0 严重：A2 字段白名单完全未实现（7 FAIL）

**根因**：`backend/src/modules/orders/orders.service.ts:503 update()` 无字段白名单。

**复现**：
```bash
SALES01_TOK=$(curl -s -X POST :8089/api/auth/login -H "Content-Type: application/json" \
  -d '{"username":"sales01","password":"test123"}' | jq -r .token)

# 销售改自己订单的 orderStatus（应该拒绝）
curl -X PATCH :8089/api/orders/cb98d2f7-... -H "Authorization: Bearer $SALES01_TOK" \
  -H "Content-Type: application/json" -d '{"orderStatus":"completed"}'
# 实测: {"ok":true}  HTTP 200   (应: 403/422)
```

**修复建议**：
```typescript
// 在 controller PATCH 路由前加白名单 + ownership：
const ALLOWED_BY_ROLE = {
  sales: ['deliveryRequirement', 'remark', 'salesRemark'],
  academic: ['orderStatus', 'requirementNote', 'remark'],
  admin: null, // 全部
  owner: null,
  staff: ['requirementNote', 'remark'],
};
const allowed = ALLOWED_BY_ROLE[role];
if (allowed) {
  for (const k of Object.keys(body)) {
    if (!allowed.includes(k)) throw new ForbiddenException(`field not allowed: ${k}`);
  }
}
// 同时加金额边界校验：amount >= 0 && amount <= 9999999
```

### 5.2 P0 严重：B7 端口隔离失效（4 FAIL）

**根因**：3001 owner 端口未做 session.role 校验。

**复现**：
```bash
# sales token 访问 3001（应 403/401）
curl -H "Authorization: Bearer $SALES01_TOK" :3001/api/leads
# 实测: HTTP 200 (应: 403/401)
```

**修复建议**：
```javascript
// server.js OWNER_PORT 启动时：
app.use('/api', (req, res, next) => {
  if (req.session?.role && !['owner', 'admin', 'supervisor'].includes(req.session.role)) {
    return res.status(403).json({ ok: false, message: 'forbidden: owner port only' });
  }
  next();
});
// 反向：3000 端口拒绝 owner
app.use('/api', (req, res, next) => {
  if (req.session?.role === 'owner') {
    return res.status(403).json({ ok: false, message: 'forbidden: owner cannot access staff port' });
  }
  next();
});
```

### 5.3 P0 严重：A3 handover 越权（academic 比对错位）

**根因**：`orders.service.ts:874` 用 `ctx.employeeId` 比对 `order.academicUserId`，但 DB 中 academic_user_id 存的是 `user.id`。

**复现**：
```bash
# academic02 accept 自己的订单（应 200）
ACADEMIC02_TOK=$(curl -s -X POST :8089/api/auth/login -d '{"username":"academic02","password":"test123"}' -H "Content-Type: application/json" | jq -r .token)
curl -X POST :8089/api/orders/cb98d2f7-.../handover/accept -H "Authorization: Bearer $ACADEMIC02_TOK"
# 实测: {"ok":false,"message":"only the assigned academic can accept handover"}  HTTP 403
```

**修复建议**：统一用 user_id 比对：
```typescript
// 改用 user_id：
if (order.academicUserId !== ctx.userId) {  // ← 改 ctx.employeeId 为 ctx.userId
  throw new ForbiddenException(...);
}
```

### 5.4 P0 中：A5 employee+user 自动绑定未实现

**根因**：`employees.controller.ts:121 create()` 端点不读 `loginUsername/loginPassword`。

**修复建议**：
```typescript
@Post()
async create(@Body() body, @Req() req, @Res() res) {
  // ... existing employee create ...
  if (body.loginUsername && body.loginPassword) {
    await this.usersService.create({
      username: body.loginUsername,
      password: body.loginPassword,  // 内部 bcrypt 哈希
      role: 'staff',
      employeeId: employee.id,
      status: 'active',
    });
  }
}
```

### 5.5 P1 中：A5 disable action 中文编码损坏

**根因**：body 解析时中文 '离职' 出现 mojibake，导致 `isDisable.includes('离职')` 失败。

**修复建议**：在 controller 内统一加 encoding normalize：
```typescript
const nextStatus = String(body.status || '').trim().normalize('NFC');
```

### 5.6 P1 中：BF-01 V1 中文 addStatus/status alias 翻译未实现

**根因**：`leads.service.ts:250 applyLeadFilters` 不做 alias 翻译。

**实测**：
```bash
# addStatus=已添加 应命中 V1 中文数据
curl -H "Authorization: Bearer $SALES01_TOK" ":8089/api/leads?addStatus=已添加"
# 实测: 0 results (应: V1 中文客资)
```

**修复建议**：参考 `STATUS_ALIASES` 字典：
```typescript
const ADD_STATUS_ALIASES = {
  '已添加': ['added', '已添加'],
  '已申请': ['pending', '已申请'],
  // ...
};
const values = ADD_STATUS_ALIASES[input] || [input];
qb.andWhere(`o.add_status IN (:...vals)`, { vals: values });
```

---

## 六、阻塞用例清单

| 用例 | 阻塞原因 |
| --- | --- |
| TC-ORDP-011 乐观锁 | 路由未读 `If-Match` header，无 etag/version 字段实现 |
| TC-HOAC-001 sales hand-over | 测试前置条件中 sales01 没有 pending 订单（已被前面测试用完） |
| TC-USEM password field filtered | `/api/users/staff` 端点返回 404，路由未实现 |

---

## 七、修复建议（按优先级）

### P0（必须修复，影响安全/数据一致性）

1. **A2 字段白名单**：在 `orders.controller.ts:265 PATCH` 路由前加 `ALLOWED_BY_ROLE` 字段白名单 + 金额边界 + 字段 ownership 校验。
2. **B7 端口隔离**：3001 OWNER_PORT 启动时强制 `role ∈ {owner, admin, supervisor}`，3000 拒绝 owner。
3. **A3 handover 字段语义统一**：service 改用 `ctx.userId` 比对 `order.academicUserId`，或修正 fixture 数据使用 employee_id。
4. **A5 employee+user 自动绑定**：employees.controller.ts create() 端点实现 `loginUsername/loginPassword` 同步创建 user。

### P1（建议修复，影响业务功能完整性）

5. **BF-01 / PF-08 V1 中文 alias 翻译**：leads.service.ts applyLeadFilters 实现 `STATUS_ALIASES` / `ADD_STATUS_ALIASES` 双向映射。
6. **A5 disable action 中文 mojibake**：加 `String.normalize('NFC')`。
7. **A2 乐观锁**：orders 增加 `version` 字段或读 `updated_at` 做 If-Match 校验。

### P2（可延后）

8. 14 类 OPLOG action 已基本齐全，可继续补齐 `disable / abnormal_create / abnormal_close`。

---

## 八、结论

- **可上线部分**：A1 operation-logs、B1/B2/B3/B5/B6 全部 PASS，P0 修复覆盖率 75%。
- **阻塞上线部分**：
  - **A2 字段白名单完全失效** → 销售可越权改订单状态/金额/handover
  - **B7 3001 端口隔离完全失效** → 任何角色 token 都可访问 owner 端口
  - **A3 handover 字段语义错位** → 教务无法接单业务完全阻塞
  - **A5 employee-user 自动绑定未实现** → 主管创建员工无法登录

**建议**：上述 4 项 P0 修复完成后，**通过率可达 95%+**，可推进 v1.2 上线。

---

**报告结束。**

详细执行数据见 `.tmp/results.json`。
