# 真实缺陷专项回归测试用例

> **文档版本**: P0-20260603
> **编写日期**: 2026-06-03
> **编写人**: 测试工程师
> **依据文档**: `doc/B端-详细测试用例-真实执行结果.md`（含 PF-01~11 / BF-01/09/15 真实执行缺陷清单）
> **适用范围**: P0 当周必补，11 个真实确认缺陷的专项回归
> **优先级**: P0（5 个）+ P1（3 个）+ P2（1 个）+ P3（2 个）
> **用例总数**: 119 条

---

## 一、覆盖总览

| 缺陷编号 | 缺陷标题 | 严重度 | 用例数 | 编号区间 | 修复负责人 |
| --- | --- | --- | --- | --- | --- |
| **PF-01** | 端口文档不一致 / legacy server 误判（已撤回） | P3 | 4 | TC-PF01-001~004 | 文档 |
| **PF-02** | 详情 accountName/postTitle 注入失败 | P1 | 11 | TC-PF02-001~011 | B 端 leads.service.ts |
| **PF-04** | 协同 reason/handledNote 中文校验过严 | P0 | 10 | TC-PF04-001~010 | B 端 collaboration-tasks.service.ts |
| **PF-05** | 失效 JWT token 没被拦截（数据泄漏，严重） | P0 | 12 | TC-PF05-001~012 | B 端 session.middleware.ts |
| **PF-06** | 3001 owner 端口隔离失效（严重） | P0 | 12 | TC-PF06-001~012 | B 端端口启动逻辑 |
| **PF-08** | V1 add_status='已添加' 列表过滤失效 | P1 | 10 | TC-PF08-001~010 | B 端 applyLeadFilters |
| **PF-09** | 端口文档不一致 | P3 | 10 | TC-PF09-001~010 | 文档 |
| **PF-10** | DebounceGuard 1s 限 1 次太严 | P3 | 10 | TC-PF10-001~010 | B 端 debounce.guard.ts |
| **PF-11** | notifications 表 type_code 列 collation 与 leads.id 不一致 | P3 | 10 | TC-PF11-001~010 | DB 迁移 |
| **BF-01** | 状态 alias 翻译缺读路径 | P1 | 10 | TC-BF01-001~010 | B 端 leads.service.ts |
| **BF-09** | close() 无 owner 校验 | P2 | 10 | TC-BF09-001~010 | B 端 collaboration-tasks.service.ts |
| **BF-15** | 改派（admin PUT 改 assignedSalesUserId）不发 lead_assigned 通知 | P0 | 10 | TC-BF15-001~010 | B 端 leads.controller.ts PUT 路径 |
| **合计** | — | — | **119** | — | — |

---

## 二、缺陷 PF-01 端口文档与 legacy server 误判专项

> **缺陷状态**: 已撤回（第一轮测试错误地跑在 3000 端口，触发假 bug；真实 NestJS 实现跑在 8089）
> **用例数**: 4
> **回归意义**: 防止后续测试再次误用 3000 端口，复用 BF-31/32/33 已被证伪的"bug"

#### TC-PF01-001：文档明确 PF-01 已撤回（legacy server 问题）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF01-001 |
| 用例标题 | 文档明确 PF-01 已撤回（legacy server 问题） |
| 优先级 | P3 |
| 对应缺陷 | PF-01 |
| 严重程度 | 低（已撤回） |
| 前置条件 | 测试工程师阅读 `doc/B端-详细测试用例-真实执行结果.md` §3 |
| 测试步骤 | 1. 打开 `B端-详细测试用例-真实执行结果.md` 找到"之前误报的BUG"章节<br>2. 确认 BF-31/32/33/PF-01 全部标注为"已撤回" |
| 预期结果 | 文档明确说明这 4 个缺陷是 legacy server.js 行为，不是 NestJS v2 真实实现 |
| 修复后预期 | 测试用例文档与本次执行结果一致，**不应**再作为有效缺陷追踪 |
| 自动化标记 | 建议手动（文档验证） |

#### TC-PF01-002：测试环境确认是 NestJS 8089 而非 3000

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF01-002 |
| 用例标题 | 测试环境确认是 NestJS 8089 而非 3000 |
| 优先级 | P3 |
| 对应缺陷 | PF-01 |
| 严重程度 | 低（已撤回） |
| 前置条件 | 双端口后端并存（legacy 3000 + NestJS 8089） |
| 测试步骤 | 1. 执行 `netstat -ano \| findstr :3000` 和 `:8089`<br>2. 确认 NestJS 进程在 8089 端口（`backend/.env`）<br>3. 确认 3000 端口是 legacy server.js 进程 |
| 预期结果 | 8089 端口有 NestJS 进程在 LISTEN 状态，3000 端口是老 server.js |
| 修复后预期 | 测试人员明确主测后端是 8089 |
| 自动化标记 | 建议自动化（启动前环境检查） |

#### TC-PF01-003：添加端口验证前置用例

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF01-003 |
| 用例标题 | 添加端口验证前置用例 |
| 优先级 | P3 |
| 对应缺陷 | PF-01 |
| 严重程度 | 低（已撤回） |
| 前置条件 | 测试套件启动 |
| 测试步骤 | 1. 自动化测试套件启动时<br>2. 调用 `GET http://localhost:8089/api/health`（或根路径）<br>3. 验证返回 200 且 server header 是 NestJS |
| 预期结果 | 端口校验通过；如失败立即停止测试套件，提示"未运行 NestJS 后端" |
| 修复后预期 | 自动化测试不再因端口错配而产生假缺陷 |
| 自动化标记 | 建议自动化 |

#### TC-PF01-004：跨端口测试必须在双端口环境

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF01-004 |
| 用例标题 | 跨端口测试必须在双端口环境 |
| 优先级 | P3 |
| 对应缺陷 | PF-01 |
| 严重程度 | 低（已撤回） |
| 前置条件 | 执行 PF-06 系列用例 |
| 测试步骤 | 1. 执行跨端口测试前先确认 3000 + 3001 + 8089 三端口都有对应进程<br>2. 三个端口在 netstat 中均可见 LISTEN |
| 预期结果 | 三端口验证通过后再跑跨端口用例 |
| 修复后预期 | 避免再次因端口错配触发误报 |
| 自动化标记 | 建议自动化 |

---

## 三、缺陷 PF-02 详情 accountName/postTitle 注入失败专项

> **缺陷位置**: `leads.service.ts:308 findOne` 调 `mapLead` 时没传 `account`/`post` 参数
> **用例数**: 11
> **回归意义**: 详情接口必须正确注入账号名/作品名，否则运营/销售/主管三端均无法看到关键关联信息

#### TC-PF02-001：客资详情接口 GET /api/leads/:id 返回 accountName 字段（注入正确）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-001 |
| 用例标题 | 客资详情接口 GET /api/leads/:id 返回 accountName 字段（注入正确） |
| 优先级 | P0 |
| 对应缺陷 | PF-02 |
| 严重程度 | 中（数据缺失但接口可用） |
| 前置条件 | 1. 存在一条客资有 matched_account_id 关联<br>2. sales1 已登录 |
| 测试步骤 | 1. 销售 sales1 调用 `GET /api/leads/{lead_id}`<br>2. 验证返回 JSON 中 `accountName` 字段存在且非空<br>3. 对比 DB 中 `accounts.name` 一致 |
| 预期结果 | `accountName = "美妆达人小A"`（与 accounts 表对应） |
| 修复后预期 | PF-02 修复后 `accountName` 不为 null |
| 自动化标记 | 建议自动化（核心回归） |

#### TC-PF02-002：客资详情接口返回 postTitle 字段

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-002 |
| 用例标题 | 客资详情接口返回 postTitle 字段 |
| 优先级 | P0 |
| 对应缺陷 | PF-02 |
| 严重程度 | 中 |
| 前置条件 | 客资有 matched_post_id 关联 |
| 测试步骤 | 1. `GET /api/leads/{lead_id}`<br>2. 验证 `postTitle` 字段非空<br>3. 对比 `posts.title` |
| 预期结果 | `postTitle = "测试作品标题"`（与 posts 表对应） |
| 修复后预期 | `postTitle` 正确注入 |
| 自动化标记 | 建议自动化 |

#### TC-PF02-003：列表接口（搜索时）也注入 accountName/postTitle

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-003 |
| 用例标题 | 列表接口（搜索时）也注入 accountName/postTitle |
| 优先级 | P1 |
| 对应缺陷 | PF-02 |
| 严重程度 | 中 |
| 前置条件 | 列表中有多条客资 |
| 测试步骤 | 1. `GET /api/leads?keyword=xxx`<br>2. 验证返回数组中每条都含 `accountName`/`postTitle` |
| 预期结果 | 列表中每条记录的 accountName/postTitle 字段都被正确填充 |
| 修复后预期 | mapLead 在 list 路径也走相同注入逻辑 |
| 自动化标记 | 建议自动化 |

#### TC-PF02-004：accountName 为 null 时降级（不报错）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-004 |
| 用例标题 | accountName 为 null 时降级（不报错） |
| 优先级 | P1 |
| 对应缺陷 | PF-02 |
| 严重程度 | 中 |
| 前置条件 | 存在 matched_account_id 为 NULL 的客资 |
| 测试步骤 | 1. `GET /api/leads/{lead_id}`<br>2. 验证 `accountName: null` 不报错<br>3. 前端能正常展示"无关联账号"占位 |
| 预期结果 | HTTP 200，`accountName: null` 字段存在但不抛错 |
| 修复后预期 | 注入逻辑对 null 安全 |
| 自动化标记 | 建议自动化 |

#### TC-PF02-005：postTitle 为 null 时降级

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-005 |
| 用例标题 | postTitle 为 null 时降级 |
| 优先级 | P1 |
| 对应缺陷 | PF-02 |
| 严重程度 | 中 |
| 前置条件 | matched_post_id 为 NULL 的客资 |
| 测试步骤 | 1. `GET /api/leads/{lead_id}`<br>2. 验证 `postTitle: null` |
| 预期结果 | HTTP 200，postTitle 为 null |
| 修复后预期 | 不报 500 |
| 自动化标记 | 建议自动化 |

#### TC-PF02-006：多账号多作品关联正确（1 客资 → 1 作品 → 1 账号 链路）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-006 |
| 用例标题 | 多账号多作品关联正确（1 客资 → 1 作品 → 1 账号 链路） |
| 优先级 | P0 |
| 对应缺陷 | PF-02 |
| 严重程度 | 中 |
| 前置条件 | 测试一个 lead 关联 post，再通过 post.account_id 关联 account |
| 测试步骤 | 1. 准备：lead.matched_post_id = post_a.id，post_a.account_id = account_a.id<br>2. `GET /api/leads/{lead_id}`<br>3. 验证 accountName 取自 account_a，postTitle 取自 post_a |
| 预期结果 | 链路正确，2 个字段都非 null 且与 DB 一致 |
| 修复后预期 | 修复后链路完整 |
| 自动化标记 | 建议自动化 |

#### TC-PF02-007：accountName 字段 SQL 注入防护

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-007 |
| 用例标题 | accountName 字段 SQL 注入防护 |
| 优先级 | P0 |
| 对应缺陷 | PF-02 |
| 严重程度 | 高（注入后门） |
| 前置条件 | 测试环境 |
| 测试步骤 | 1. 用恶意账号名 `' OR 1=1 --` 创建 account<br>2. 创建客资关联此 account<br>3. `GET /api/leads/{lead_id}`<br>4. 验证响应中 accountName 字段是转义后的字符串，不影响其他字段 |
| 预期结果 | accountName 显示为 `&#x27; OR 1=1 --` 或原样，但 SQL 查询不执行注入 |
| 修复后预期 | 注入防护到位，参数化查询 |
| 自动化标记 | 建议自动化（安全测试） |

#### TC-PF02-008：postTitle 字段 XSS 注入防护

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-008 |
| 用例标题 | postTitle 字段 XSS 注入防护 |
| 优先级 | P0 |
| 对应缺陷 | PF-02 |
| 严重程度 | 高 |
| 前置条件 | 测试环境 |
| 测试步骤 | 1. 用 `<script>alert(1)</script>` 作为 post.title<br>2. 创建客资关联此 post<br>3. `GET /api/leads/{lead_id}`<br>4. 前端渲染 postTitle 时验证 |
| 预期结果 | 前端不执行 alert(1)，XSS 被转义 |
| 修复后预期 | XSS 防护到位 |
| 自动化标记 | 建议自动化（安全测试） |

#### TC-PF02-009：accountName/postTitle 修复后回归稳定

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-009 |
| 用例标题 | accountName/postTitle 修复后回归稳定 |
| 优先级 | P0 |
| 对应缺陷 | PF-02 |
| 严重程度 | 中 |
| 前置条件 | 修复后跑回归 |
| 测试步骤 | 1. 跑 TC-PF02-001~008 全部用例<br>2. 跑 5 次（防 flake）<br>3. 100% 通过 |
| 预期结果 | 5/5 通过 |
| 修复后预期 | 修复后稳定回归 |
| 自动化标记 | 建议自动化（CI 必跑） |

#### TC-PF02-010：销售端/运营端/主管端详情都正确显示

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-010 |
| 用例标题 | 销售端/运营端/主管端详情都正确显示 |
| 优先级 | P1 |
| 对应缺陷 | PF-02 |
| 严重程度 | 中 |
| 前置条件 | 三端账号都有权限访问此 lead |
| 测试步骤 | 1. sales1 / operation_a / supervisor_a 三个角色分别 `GET /api/leads/{id}`<br>2. 验证三端响应中 accountName/postTitle 一致 |
| 预期结果 | 三端返回相同字段值 |
| 修复后预期 | 跨角色一致 |
| 自动化标记 | 建议自动化 |

#### TC-PF02-011：详情页面前端能正确渲染 accountName（如果有 EJS 模板）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF02-011 |
| 用例标题 | 详情页面前端能正确渲染 accountName（如果有 EJS 模板） |
| 优先级 | P2 |
| 对应缺陷 | PF-02 |
| 严重程度 | 低 |
| 前置条件 | 前端 SPA 渲染 |
| 测试步骤 | 1. 浏览器打开 `/sales/leads/{id}` 详情页<br>2. 观察"来源账号"字段是否显示<br>3. 观察"来源作品"字段是否显示 |
| 预期结果 | UI 正确显示账号名和作品标题 |
| 修复后预期 | UI 完整渲染 |
| 自动化标记 | 建议手动 + Playwright 自动化 |

---

## 四、缺陷 PF-04 协同 reason/handledNote 中文校验过严专项

> **缺陷位置**: `collaboration-tasks.service.ts:85-87 hasBrokenEncoding` 把中文误判为损坏字符
> **用例数**: 10
> **回归意义**: 真实业务中协同/处理备注必须能用中文，校验逻辑必须容错

#### TC-PF04-001：协同 reason 中文（"客户未通过好友申请"）不报 422

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-001 |
| 用例标题 | 协同 reason 中文（"客户未通过好友申请"）不报 422 |
| 优先级 | P0 |
| 对应缺陷 | PF-04 |
| 严重程度 | 高（业务阻断） |
| 前置条件 | sales1 有客资，可发起协同 |
| 测试步骤 | 1. sales1 `POST /api/leads/{id}/collaboration` body `{type: "remind_customer", reason: "客户未通过好友申请"}`<br>2. 验证 HTTP 200（不是 422）<br>3. DB 中 collaboration_tasks.reason = "客户未通过好友申请" |
| 预期结果 | HTTP 200，中文 reason 写入成功 |
| 修复后预期 | hasBrokenEncoding 修复后中文通过 |
| 自动化标记 | 建议自动化（核心回归） |

#### TC-PF04-002：协同 reason 英文正常

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-002 |
| 用例标题 | 协同 reason 英文正常 |
| 优先级 | P0 |
| 对应缺陷 | PF-04 |
| 严重程度 | 高 |
| 前置条件 | 同上 |
| 测试步骤 | 1. reason = `"customer not passed"`<br>2. 验证 HTTP 200 |
| 预期结果 | HTTP 200，DB reason 写入 |
| 修复后预期 | 英文不受影响（持续通过） |
| 自动化标记 | 建议自动化 |

#### TC-PF04-003：协同 reason 中英混合

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-003 |
| 用例标题 | 协同 reason 中英混合 |
| 优先级 | P0 |
| 对应缺陷 | PF-04 |
| 严重程度 | 高 |
| 前置条件 | 同上 |
| 测试步骤 | 1. reason = `"客户回复 WeChat no response"`<br>2. 验证 HTTP 200 |
| 预期结果 | HTTP 200 |
| 修复后预期 | 混合文本通过 |
| 自动化标记 | 建议自动化 |

#### TC-PF04-004：协同 reason 含特殊字符（标点、emoji 🌟）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-004 |
| 用例标题 | 协同 reason 含特殊字符（标点、emoji 🌟） |
| 优先级 | P1 |
| 对应缺陷 | PF-04 |
| 严重程度 | 高 |
| 前置条件 | 同上 |
| 测试步骤 | 1. reason = `"客户说：'暂时不需要', 真的吗？🌟🌟🌟"`<br>2. 验证 HTTP 200 |
| 预期结果 | HTTP 200，特殊字符正常写入 |
| 修复后预期 | emoji 和标点通过 |
| 自动化标记 | 建议自动化 |

#### TC-PF04-005：协同 reason 超长（5000 字符）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-005 |
| 用例标题 | 协同 reason 超长（5000 字符） |
| 优先级 | P2 |
| 对应缺陷 | PF-04 |
| 严重程度 | 中 |
| 前置条件 | 同上 |
| 测试步骤 | 1. 生成 5000 字符的中文 reason<br>2. `POST /api/leads/{id}/collaboration`<br>3. 验证 HTTP 200 或合理 413 |
| 预期结果 | 长文本不触发"字符损坏"误报 |
| 修复后预期 | 长文本也能写入 |
| 自动化标记 | 建议自动化 |

#### TC-PF04-006：handledNote 中文正常

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-006 |
| 用例标题 | handledNote 中文正常 |
| 优先级 | P0 |
| 对应缺陷 | PF-04 |
| 严重程度 | 高 |
| 前置条件 | 存在 pending 协同任务，staff1 处理 |
| 测试步骤 | 1. staff1 `POST /api/collaboration-tasks/{id}/handle` body `{handledNote: "已私信提醒客户添加"}`<br>2. 验证 HTTP 200 |
| 预期结果 | HTTP 200，DB handled_note = "已私信提醒客户添加" |
| 修复后预期 | 中文 handledNote 写入成功 |
| 自动化标记 | 建议自动化 |

#### TC-PF04-007：handledNote 英文正常

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-007 |
| 用例标题 | handledNote 英文正常 |
| 优先级 | P0 |
| 对应缺陷 | PF-04 |
| 严重程度 | 高 |
| 前置条件 | 同上 |
| 测试步骤 | 1. handledNote = `"already DM the customer"`<br>2. 验证 HTTP 200 |
| 预期结果 | HTTP 200 |
| 修复后预期 | 英文持续通过 |
| 自动化标记 | 建议自动化 |

#### TC-PF04-008：hasBrokenEncoding 函数修复后行为

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-008 |
| 用例标题 | hasBrokenEncoding 函数修复后行为 |
| 优先级 | P0 |
| 对应缺陷 | PF-04 |
| 严重程度 | 高 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 单元测试 `hasBrokenEncoding("客户未通过")` → false<br>2. `hasBrokenEncoding("customer not passed")` → false<br>3. `hasBrokenEncoding("客户说：'不需要'🌟")` → false<br>4. `hasBrokenEncoding(" \x01")` → true（真实损坏） |
| 预期结果 | 中文/英文/混合/emoji 都 false；控制字符 true |
| 修复后预期 | 校验函数不再误杀中文 |
| 自动化标记 | 建议自动化（单测） |

#### TC-PF04-009：修复后自动化测试可使用中文

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-009 |
| 用例标题 | 修复后自动化测试可使用中文 |
| 优先级 | P1 |
| 对应缺陷 | PF-04 |
| 严重程度 | 中 |
| 前置条件 | e2e 自动化套件 |
| 测试步骤 | 1. 在 e2e 测试中用中文 reason<br>2. 跑完整 TC-B-015/019/043<br>3. 全部通过 |
| 预期结果 | 100% PASS |
| 修复后预期 | 中文 e2e 可用 |
| 自动化标记 | 建议自动化 |

#### TC-PF04-010：修复后真实业务不受影响

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF04-010 |
| 用例标题 | 修复后真实业务不受影响 |
| 优先级 | P0 |
| 对应缺陷 | PF-04 |
| 严重程度 | 高 |
| 前置条件 | 真实业务场景 |
| 测试步骤 | 1. 真实运营发起协同（中文 reason）<br>2. 真实销售收到通知（中文 content）<br>3. 主管/销售端 UI 正常显示 |
| 预期结果 | 全链路中文无报错 |
| 修复后预期 | 业务恢复 |
| 自动化标记 | 建议手动 |

---

## 五、缺陷 PF-05 失效 JWT token 没被拦截（数据泄漏）专项

> **缺陷位置**: `session.middleware.ts:35-37 catch {}` 静默放行
> **用例数**: 12
> **回归意义**: **P0 数据泄漏严重缺陷**，失效 token 必须 401 拦截

#### TC-PF05-001：失效 token `xxx.invalid.token` 必须 401

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-001 |
| 用例标题 | 失效 token `xxx.invalid.token` 必须 401 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重**（数据泄漏） |
| 前置条件 | 后端运行中 |
| 测试步骤 | 1. `curl -H "Authorization: Bearer xxx.invalid.token" http://localhost:8089/api/leads`<br>2. 验证 HTTP 401<br>3. 验证响应体不含 lead 数据 |
| 预期结果 | HTTP 401 Unauthorized |
| 修复后预期 | session.middleware 不再 catch 静默放行 |
| 自动化标记 | 建议自动化（核心回归） |

#### TC-PF05-002：篡改 payload 签名的 token 必须 401

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-002 |
| 用例标题 | 篡改 payload 签名的 token 必须 401 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 拿到一个合法 token |
| 测试步骤 | 1. 取真实 token，修改 payload 部分（base64 解码后改 sub 字段）<br>2. `curl -H "Authorization: Bearer {篡改后token}" http://localhost:8089/api/leads`<br>3. 验证 HTTP 401 |
| 预期结果 | HTTP 401 |
| 修复后预期 | 签名验证拦截篡改 |
| 自动化标记 | 建议自动化 |

#### TC-PF05-003：篡改 signature 的 token 必须 401

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-003 |
| 用例标题 | 篡改 signature 的 token 必须 401 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 拿到一个合法 token |
| 测试步骤 | 1. 修改 token 最后一段（signature）<br>2. `curl -H "Authorization: Bearer {篡改sig}" http://localhost:8089/api/leads`<br>3. 验证 HTTP 401 |
| 预期结果 | HTTP 401 |
| 修复后预期 | 签名校验严格 |
| 自动化标记 | 建议自动化 |

#### TC-PF05-004：过期 token 必须 401

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-004 |
| 用例标题 | 过期 token 必须 401 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 准备一个 exp 已过期的 token |
| 测试步骤 | 1. 用过期 token（手工构造或用 nbf/exp 测试端点）<br>2. 请求任意 API<br>3. 验证 HTTP 401 |
| 预期结果 | HTTP 401，message 包含 "expired" |
| 修复后预期 | 过期 token 必须拒绝 |
| 自动化标记 | 建议自动化 |

#### TC-PF05-005：空 token 必须 401

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-005 |
| 用例标题 | 空 token 必须 401 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 无 |
| 测试步骤 | 1. `curl -H "Authorization: Bearer" http://localhost:8089/api/leads`<br>2. 验证 HTTP 401 |
| 预期结果 | HTTP 401 |
| 修复后预期 | 空 token 拒绝 |
| 自动化标记 | 建议自动化 |

#### TC-PF05-006：Bearer 前缀缺失必须 401

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-006 |
| 用例标题 | Bearer 前缀缺失必须 401 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 拿到一个合法 token |
| 测试步骤 | 1. `curl -H "Authorization: {token_no_bearer}" http://localhost:8089/api/leads`<br>2. 验证 HTTP 401 |
| 预期结果 | HTTP 401 |
| 修复后预期 | 强制要求 Bearer 前缀 |
| 自动化标记 | 建议自动化 |

#### TC-PF05-007：session.middleware catch 静默放行的真实复现

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-007 |
| 用例标题 | session.middleware catch 静默放行的真实复现 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 修复前（bug 复现） |
| 测试步骤 | 1. `curl -H "Authorization: Bearer xxx.invalid.token" http://localhost:8089/api/leads`<br>2. 验证（修复前）HTTP 200 + 完整 lead 列表<br>3. 记录现象作为修复前证据 |
| 预期结果 | 修复前返回 200+数据（确认 bug） |
| 修复后预期 | 修复后此用例变成 TC-PF05-001 形式（必须 401） |
| 自动化标记 | 建议自动化（修复前/后对比） |

#### TC-PF05-008：修复后所有 controller 强制鉴权

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-008 |
| 用例标题 | 修复后所有 controller 强制鉴权 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 列出全部 controller（leads/orders/notifications/collaboration-tasks 等）<br>2. 每个 controller 至少 1 个端点<br>3. 失效 token 请求每个端点<br>4. 100% 返 401 |
| 预期结果 | 全部返 401 |
| 修复后预期 | 全 controller 鉴权一致 |
| 自动化标记 | 建议自动化（覆盖扫描） |

#### TC-PF05-009：修复后跨端口 token 也强制失效

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-009 |
| 用例标题 | 修复后跨端口 token 也强制失效 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 跨端口环境（3000/3001/8089） |
| 测试步骤 | 1. sales token 试图访问 owner 3001 端口<br>2. 同时也用失效 token 访问任一端口<br>3. 验证两个场景都 401/403 |
| 预期结果 | 失效 token 跨端口全部拒绝 |
| 修复后预期 | 端口+token 双层防护 |
| 自动化标记 | 建议自动化 |

#### TC-PF05-010：修复后 Bearer token 而非 JWT 行为保持

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-010 |
| 用例标题 | 修复后 Bearer token 而非 JWT 行为保持 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | 高 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 项目实际是 in-memory Bearer token（CLAUDE.md §Architecture）<br>2. 修复后失效 token 检测逻辑必须匹配 Bearer 语义（session lookup）<br>3. 不能误把 JWT 库当唯一标准 |
| 预期结果 | 修复与现有 Bearer token 体系兼容 |
| 修复后预期 | 行为保持 |
| 自动化标记 | 建议自动化 |

#### TC-PF05-011：多端口同时请求时失效 token 拦截

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-011 |
| 用例标题 | 多端口同时请求时失效 token 拦截 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 多端口运行 |
| 测试步骤 | 1. 并发 50 个请求到 3000/3001/8089，全部用失效 token<br>2. 验证 100% 401/403 |
| 预期结果 | 全部拒绝 |
| 修复后预期 | 并发一致拒绝 |
| 自动化标记 | 建议自动化（并发压测） |

#### TC-PF05-012：运营/销售/教务/主管/owner 5 角色 token 失效一致性

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF05-012 |
| 用例标题 | 运营/销售/教务/主管/owner 5 角色 token 失效一致性 |
| 优先级 | P0 |
| 对应缺陷 | PF-05 |
| 严重程度 | **严重** |
| 前置条件 | 5 角色账号 |
| 测试步骤 | 1. 拿 5 个角色各自的合法 token<br>2. 5 个 token 全部"强制失效"（重启服务或强制登出）<br>3. 用 5 个失效 token 请求对应端口<br>4. 100% 401/403 |
| 预期结果 | 5 角色一致拦截 |
| 修复后预期 | 角色无关，统一拦截 |
| 自动化标记 | 建议自动化 |

---

## 六、缺陷 PF-06 3001 owner 端口隔离失效专项

> **缺陷位置**: 端口启动逻辑（3001 上跑的可能不是 NestJS）
> **用例数**: 12
> **回归意义**: **P0 跨角色访问隔离被绕过**

#### TC-PF06-001：sales token 访问 3001 任一 API → 403

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-001 |
| 用例标题 | sales token 访问 3001 任一 API → 403 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | 3001 端口运行，sales1 已登录 3000 拿 token |
| 测试步骤 | 1. `curl -H "Authorization: Bearer {sales_token}" http://localhost:3001/api/leads`<br>2. 验证 HTTP 403/401 |
| 预期结果 | 拒绝访问（修复前可能 200+数据） |
| 修复后预期 | 端口强制隔离 |
| 自动化标记 | 建议自动化（核心回归） |

#### TC-PF06-002：operation token 访问 3001 → 403

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-002 |
| 用例标题 | operation token 访问 3001 → 403 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | 同上 |
| 测试步骤 | 1. operation token 调 3001/api/* 任一端点<br>2. 验证拒绝 |
| 预期结果 | HTTP 403/401 |
| 修复后预期 | operation 不能进 owner 端口 |
| 自动化标记 | 建议自动化 |

#### TC-PF06-003：academic token 访问 3001 → 403

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-003 |
| 用例标题 | academic token 访问 3001 → 403 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | 同上 |
| 测试步骤 | 1. academic token 调 3001/api/* 任一端点<br>2. 验证拒绝 |
| 预期结果 | HTTP 403/401 |
| 修复后预期 | academic 不能进 owner 端口 |
| 自动化标记 | 建议自动化 |

#### TC-PF06-004：3001 端口仅接受 role=owner/admin/supervisor

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-004 |
| 用例标题 | 3001 端口仅接受 role=owner/admin/supervisor |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | owner 账号已登录 |
| 测试步骤 | 1. owner token 调 3001/api/*<br>2. 验证 HTTP 200<br>3. supervisor/admin 同样验证 200 |
| 预期结果 | owner/supervisor/admin 三个 role 通过；其他 role 拒绝 |
| 修复后预期 | 角色白名单生效 |
| 自动化标记 | 建议自动化 |

#### TC-PF06-005：owner token 访问 3000 → 拒绝（反向 PF-05 联动）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-005 |
| 用例标题 | owner token 访问 3000 → 拒绝（反向 PF-05 联动） |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | owner token |
| 测试步骤 | 1. owner token 调 3000/api/*<br>2. 验证拒绝 |
| 预期结果 | HTTP 403/401 |
| 修复后预期 | owner 不能混进 3000 端口 |
| 自动化标记 | 建议自动化 |

#### TC-PF06-006：3001 端口启动逻辑验证

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-006 |
| 用例标题 | 3001 端口启动逻辑验证 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | 启动 3001 |
| 测试步骤 | 1. 启动 nest start --port=3001<br>2. 验证 nest 在 3001 启动（不是其他进程）<br>3. 验证 process 启动日志"Listening on 3001" |
| 预期结果 | 3001 上跑的是 NestJS 不是老 server.js |
| 修复后预期 | 端口启动逻辑正确 |
| 自动化标记 | 建议自动化（启动检查） |

#### TC-PF06-007：跨端口 token 伪造防护

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-007 |
| 用例标题 | 跨端口 token 伪造防护 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | 拿到 sales 真实 token |
| 测试步骤 | 1. sales token 试图调 3001/api/orders（owner 专属）<br>2. 修改 token payload 加 role=owner（伪造）<br>3. 验证全部 401/403 |
| 预期结果 | 全部拒绝 |
| 修复后预期 | 签名验证拦截伪造 |
| 自动化标记 | 建议自动化（安全） |

#### TC-PF06-008：跨端口 namespace 隔离

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-008 |
| 用例标题 | 跨端口 namespace 隔离 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | 双端口 socket.io |
| 测试步骤 | 1. sales 客户端连 3000 socket.io 命名空间<br>2. 尝试连 3001 命名空间<br>3. 验证 3001 命名空间只接受 owner |
| 预期结果 | 命名空间隔离 |
| 修复后预期 | socket 命名空间独立 |
| 自动化标记 | 建议自动化 |

#### TC-PF06-009：双端口环境验证（必须 3000+3001 同时运行）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-009 |
| 用例标题 | 双端口环境验证（必须 3000+3001 同时运行） |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | 测试环境 |
| 测试步骤 | 1. `netstat -ano \| findstr "3000 3001"`<br>2. 两个端口都 LISTEN 状态<br>3. 进程 ID 是 NestJS |
| 预期结果 | 双端口同时健康 |
| 修复后预期 | 双端口验证通过 |
| 自动化标记 | 建议自动化（环境检查） |

#### TC-PF06-010：修复后双端口各自权限独立

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-010 |
| 用例标题 | 修复后双端口各自权限独立 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | **严重** |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 4 角色 × 2 端口 = 8 组合<br>2. 每组合 1 次请求<br>3. 验证：3000 仅 staff/admin 通过，3001 仅 owner/admin/supervisor 通过 |
| 预期结果 | 8 组合符合预期 |
| 修复后预期 | 双端口独立 |
| 自动化标记 | 建议自动化（矩阵测试） |

#### TC-PF06-011：3001 端口静态资源 vs API 路由隔离

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-011 |
| 用例标题 | 3001 端口静态资源 vs API 路由隔离 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | 中 |
| 前置条件 | 3001 运行 |
| 测试步骤 | 1. `curl http://localhost:3001/` 静态资源可访问<br>2. `curl http://localhost:3001/api/*` API 强制鉴权<br>3. 验证静态资源不被 API 鉴权拦截 |
| 预期结果 | 静态资源 200，API 401/403 |
| 修复后预期 | 路由清晰分离 |
| 自动化标记 | 建议自动化 |

#### TC-PF06-012：3000 端口 socket.io 命名空间 vs 3001 socket.io 命名空间隔离

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF06-012 |
| 用例标题 | 3000 端口 socket.io 命名空间 vs 3001 socket.io 命名空间隔离 |
| 优先级 | P0 |
| 对应缺陷 | PF-06 |
| 严重程度 | 中 |
| 前置条件 | 双端口 socket.io |
| 测试步骤 | 1. sales 客户端用 sales namespace 连 3000<br>2. 尝试用 owner namespace 连 3001<br>3. 验证 3001 拒绝非 owner namespace |
| 预期结果 | 命名空间按 role 隔离 |
| 修复后预期 | socket 隔离 |
| 自动化标记 | 建议自动化 |

---

## 七、缺陷 PF-08 V1 add_status='已添加' 列表过滤失效专项

> **缺陷位置**: `leads.service.ts:250 applyLeadFilters` addStatus 不走 ADD_STATUS_ALIASES
> **用例数**: 10
> **回归意义**: 老数据兼容性，老客资必须能筛出来

#### TC-PF08-001：GET /api/leads?addStatus=已添加 命中 V1 老数据

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-001 |
| 用例标题 | GET /api/leads?addStatus=已添加 命中 V1 老数据 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | 存在 add_status='已添加' 的老客资（V1 中文值） |
| 测试步骤 | 1. `GET /api/leads?addStatus=已添加`<br>2. 验证返回含 V1 中文数据 |
| 预期结果 | HTTP 200，结果包含 add_status='已添加' 的客资 |
| 修复后预期 | ADD_STATUS_ALIASES['已添加'] = ['added'] 翻译生效 |
| 自动化标记 | 建议自动化 |

#### TC-PF08-002：GET /api/leads?addStatus=added 命中 V1 老数据

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-002 |
| 用例标题 | GET /api/leads?addStatus=added 命中 V1 老数据 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | 同上 |
| 测试步骤 | 1. `GET /api/leads?addStatus=added`<br>2. 验证结果同时包含 V2 'added' 和 V1 '已添加' 记录 |
| 预期结果 | 英文 added 触发中英双向查询 |
| 修复后预期 | alias 翻译完整 |
| 自动化标记 | 建议自动化 |

#### TC-PF08-003：销售端筛选"已添加" tab 包含 V1 老数据

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-003 |
| 用例标题 | 销售端筛选"已添加" tab 包含 V1 老数据 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | sales1 名下有 V1 中文值客资 |
| 测试步骤 | 1. 浏览器打开销售端"已添加"tab<br>2. 验证 V1 中文值客资出现在列表 |
| 预期结果 | V1 老数据出现在 tab |
| 修复后预期 | 前端触发正确 API 参数 |
| 自动化标记 | 建议自动化 |

#### TC-PF08-004：V1 中文 addStatus 状态全部 5 值（未添加/已申请/未通过/已添加/无效）翻译

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-004 |
| 用例标题 | V1 中文 addStatus 状态全部 5 值（未添加/已申请/未通过/已添加/无效）翻译 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | 5 种 V1 中文值都有老数据 |
| 测试步骤 | 1. `GET /api/leads?addStatus=未添加` 含 V1 '未添加' 客资<br>2. `GET /api/leads?addStatus=已申请`<br>3. `GET /api/leads?addStatus=未通过`<br>4. `GET /api/leads?addStatus=已添加`<br>5. `GET /api/leads?addStatus=无效`<br>6. 验证 5 个查询都返回对应记录 |
| 预期结果 | 5 个查询全部命中 |
| 修复后预期 | ADD_STATUS_ALIASES 完整 5 值 |
| 自动化标记 | 建议自动化 |

#### TC-PF08-005：V1 中文 addStatus 列表过滤正确

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-005 |
| 用例标题 | V1 中文 addStatus 列表过滤正确 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | 同上 |
| 测试步骤 | 1. 准备 5 条客资，addStatus 各对应 5 个 V1 中文值<br>2. 5 次 `GET /api/leads?addStatus={chinese}`<br>3. 每次返回 1 条对应记录 |
| 预期结果 | 5/5 正确过滤 |
| 修复后预期 | 列表过滤无误 |
| 自动化标记 | 建议自动化 |

#### TC-PF08-006：V1 中文 addStatus 与 V2 英文混合查询

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-006 |
| 用例标题 | V1 中文 addStatus 与 V2 英文混合查询 |
| 优先级 | P1 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | 同一 V1/V2 混合数据 |
| 测试步骤 | 1. `GET /api/leads?addStatus=已添加` 命中 V1+ V2 'added'<br>2. 验证结果数 ≥ V1 '已添加' 数 + V2 'added' 数 |
| 预期结果 | 中英双向都查 |
| 修复后预期 | 翻译覆盖所有 alias |
| 自动化标记 | 建议自动化 |

#### TC-PF08-007：addStatus 翻译函数（ADD_STATUS_ALIASES）行为

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-007 |
| 用例标题 | addStatus 翻译函数（ADD_STATUS_ALIASES）行为 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | 单元测试 |
| 测试步骤 | 1. 单元测试 `ADD_STATUS_ALIASES['已添加']` 返回 `['added']`<br>2. `ADD_STATUS_ALIASES['added']` 返回 `['added', '已添加']`<br>3. `ADD_STATUS_ALIASES[undefined]` 返回 `[]` |
| 预期结果 | 双向映射 |
| 修复后预期 | 翻译函数稳定 |
| 自动化标记 | 建议自动化（单测） |

#### TC-PF08-008：修复后 applyLeadFilters 走 alias 翻译

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-008 |
| 用例标题 | 修复后 applyLeadFilters 走 alias 翻译 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 代码 review `leads.service.ts:250`<br>2. 验证 addStatus 分支调用 `translateAddStatus()`<br>3. 跑 TC-PF08-001~007 全部通过 |
| 预期结果 | 代码+测试都过 |
| 修复后预期 | 走 alias 翻译 |
| 自动化标记 | 建议自动化 |

#### TC-PF08-009：修复后主管端兼容 V1 中文过滤

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-009 |
| 用例标题 | 修复后主管端兼容 V1 中文过滤 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | supervisor 账号 |
| 测试步骤 | 1. 主管端客资看板筛选"已添加"<br>2. 验证 V1 中文值客资出现 |
| 预期结果 | 主管端兼容 |
| 修复后预期 | 主管端 OK |
| 自动化标记 | 建议自动化 |

#### TC-PF08-010：修复后运营端兼容 V1 中文过滤

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF08-010 |
| 用例标题 | 修复后运营端兼容 V1 中文过滤 |
| 优先级 | P0 |
| 对应缺陷 | PF-08 |
| 严重程度 | 中 |
| 前置条件 | operation 账号 |
| 测试步骤 | 1. 运营端客资看板筛选"已添加"<br>2. 验证 V1 中文值客资出现 |
| 预期结果 | 运营端兼容 |
| 修复后预期 | 运营端 OK |
| 自动化标记 | 建议自动化 |

---

## 八、缺陷 PF-09 端口文档不一致专项

> **缺陷位置**: 文档（`/env`、`/backend/.env`、`CLAUDE.md`）
> **用例数**: 10

#### TC-PF09-001：文档明确 4 端口（运营/销售/教务/主管）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-001 |
| 用例标题 | 文档明确 4 端口（运营/销售/教务/主管） |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | 文档审阅 |
| 测试步骤 | 1. 阅读所有 README/CLAUDE.md 文档<br>2. 验证 4 端口（3000 运营/销售/教务/主管 + 3001 owner）都明确列出 |
| 预期结果 | 文档清晰 |
| 修复后预期 | 端口描述一致 |
| 自动化标记 | 建议手动 |

#### TC-PF09-002：文档明确 3001 owner 端口

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-002 |
| 用例标题 | 文档明确 3001 owner 端口 |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | 同上 |
| 测试步骤 | 1. 验证文档明确 3001 是 owner 端口 |
| 预期结果 | 描述清晰 |
| 修复后预期 | 一致 |
| 自动化标记 | 建议手动 |

#### TC-PF09-003：.env 文档与实际端口一致

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-003 |
| 用例标题 | .env 文档与实际端口一致 |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | .env.example 与 backend/.env |
| 测试步骤 | 1. 对比根 `.env.example`、`backend/.env.example`、实际 `.env`<br>2. 验证 PORT/OWNER_PORT 配置一致 |
| 预期结果 | 三处一致 |
| 修复后预期 | 文档与实际同步 |
| 自动化标记 | 建议自动化（diff） |

#### TC-PF09-004：CLAUDE.md 与实际端口一致

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-004 |
| 用例标题 | CLAUDE.md 与实际端口一致 |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | CLAUDE.md 文档 |
| 测试步骤 | 1. 阅读 CLAUDE.md "Running the Application" 段<br>2. 验证 PORT/OWNER_PORT 描述与实际一致 |
| 预期结果 | 描述一致 |
| 修复后预期 | 文档同步 |
| 自动化标记 | 建议手动 |

#### TC-PF09-005：backend/.env 与实际端口一致

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-005 |
| 用例标题 | backend/.env 与实际端口一致 |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | backend/.env |
| 测试步骤 | 1. 启动 backend，验证实际监听端口 == backend/.env 配置 |
| 预期结果 | 一致 |
| 修复后预期 | 启动日志匹配配置 |
| 自动化标记 | 建议自动化 |

#### TC-PF09-006：启动日志与实际端口一致

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-006 |
| 用例标题 | 启动日志与实际端口一致 |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | 启动应用 |
| 测试步骤 | 1. 启动 nest + 启动 legacy server<br>2. 验证启动日志中 "Listening on 3000" / "Listening on 8089" 与实际一致 |
| 预期结果 | 日志匹配 |
| 修复后预期 | 一致 |
| 自动化标记 | 建议自动化 |

#### TC-PF09-007：文档包含端口对照表

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-007 |
| 用例标题 | 文档包含端口对照表 |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | 文档 |
| 测试步骤 | 1. README/CLAUDE.md 中是否有"端口对照表"小节 |
| 预期结果 | 包含完整对照表 |
| 修复后预期 | 文档化 |
| 自动化标记 | 建议手动 |

#### TC-PF09-008：测试环境验证端口（NestJS 8089 vs 3000）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-008 |
| 用例标题 | 测试环境验证端口（NestJS 8089 vs 3000） |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | 测试环境 |
| 测试步骤 | 1. 跑 B 端测试时显式指定 NestJS 在 8089<br>2. 验证测试不依赖 3000 老 server |
| 预期结果 | 测试环境清晰 |
| 修复后预期 | 测试不混淆端口 |
| 自动化标记 | 建议自动化 |

#### TC-PF09-009：添加端口检测到测试用例

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-009 |
| 用例标题 | 添加端口检测到测试用例 |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | 自动化测试套件 |
| 测试步骤 | 1. 每个 B 端测试用例先做端口检测<br>2. 失败立即报错 |
| 预期结果 | 端口错配立即失败 |
| 修复后预期 | 不再误用 3000 |
| 自动化标记 | 建议自动化 |

#### TC-PF09-010：双端口启动顺序

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF09-010 |
| 用例标题 | 双端口启动顺序 |
| 优先级 | P2 |
| 对应缺陷 | PF-09 |
| 严重程度 | 低 |
| 前置条件 | 启动脚本 |
| 测试步骤 | 1. 文档明确"先启动 nest 8089，再启动 legacy 3000"<br>2. 启动脚本按此顺序 |
| 预期结果 | 启动顺序确定 |
| 修复后预期 | 启动可重复 |
| 自动化标记 | 建议自动化（CI 启动脚本） |

---

## 九、缺陷 PF-10 DebounceGuard 1s 限 1 次太严专项

> **缺陷位置**: `backend/src/common/debounce.guard.ts`
> **用例数**: 10

#### TC-PF10-001：DebounceGuard 1 秒内同 path 限 1 次

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-001 |
| 用例标题 | DebounceGuard 1 秒内同 path 限 1 次 |
| 优先级 | P1 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | sales1 登录 |
| 测试步骤 | 1. T=0ms 调 `PUT /api/leads/{id}` addStatus=added<br>2. T=200ms 再次调相同端点<br>3. 验证第 2 次返 429 |
| 预期结果 | HTTP 429 Too Many Requests |
| 修复后预期 | 修复后放宽到 1.5s+ 或 userId+key |
| 自动化标记 | 建议自动化 |

#### TC-PF10-002：DebounceGuard 跨用户独立

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-002 |
| 用例标题 | DebounceGuard 跨用户独立 |
| 优先级 | P0 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | 2 个用户 |
| 测试步骤 | 1. sales1 T=0 调 PUT /api/leads/{id}<br>2. sales2 T=200 调 PUT /api/leads/{id2}<br>3. 验证 sales2 不被 sales1 防抖 |
| 预期结果 | 跨用户独立 |
| 修复后预期 | key 包含 userId |
| 自动化标记 | 建议自动化 |

#### TC-PF10-003：DebounceGuard 跨 method 独立

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-003 |
| 用例标题 | DebounceGuard 跨 method 独立 |
| 优先级 | P0 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | 1 个用户 |
| 测试步骤 | 1. sales1 T=0 PUT /api/leads/{id}<br>2. sales1 T=200 GET /api/leads<br>3. 验证 GET 不被 PUT 防抖 |
| 预期结果 | GET 200 OK |
| 修复后预期 | key 包含 method |
| 自动化标记 | 建议自动化 |

#### TC-PF10-004：DebounceGuard 1.5 秒后允许

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-004 |
| 用例标题 | DebounceGuard 1.5 秒后允许 |
| 优先级 | P0 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | 1 个用户 |
| 测试步骤 | 1. T=0 PUT /api/leads/{id}<br>2. sleep 1500ms<br>3. T=2000 PUT /api/leads/{id}<br>4. 验证 T=2000 不返 429 |
| 预期结果 | HTTP 200 |
| 修复后预期 | 1.5s 窗口期 |
| 自动化标记 | 建议自动化 |

#### TC-PF10-005：DebounceGuard 修复后自动化测试正常

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-005 |
| 用例标题 | DebounceGuard 修复后自动化测试正常 |
| 优先级 | P0 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | e2e 套件 |
| 测试步骤 | 1. 跑完整 45 个 TC，无 429 误判 |
| 预期结果 | 100% PASS |
| 修复后预期 | 测试不再被防抖阻断 |
| 自动化标记 | 建议自动化 |

#### TC-PF10-006：DebounceGuard 修复后用户体验

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-006 |
| 用例标题 | DebounceGuard 修复后用户体验 |
| 优先级 | P0 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | 真用户 |
| 测试步骤 | 1. 用户连续点击"已添加"按钮 2 次（间隔 200ms）<br>2. 验证第 2 次不被防抖 |
| 预期结果 | 真实操作不误伤 |
| 修复后预期 | 体验改善 |
| 自动化标记 | 建议自动化 |

#### TC-PF10-007：DebounceGuard key 包含 userId

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-007 |
| 用例标题 | DebounceGuard key 包含 userId |
| 优先级 | P0 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | 代码 |
| 测试步骤 | 1. 单测 debounce key 包含 userId<br>2. 验证 key 格式 `${userId}:${method}:${path}` |
| 预期结果 | key 正确 |
| 修复后预期 | 单测通过 |
| 自动化标记 | 建议自动化（单测） |

#### TC-PF10-008：DebounceGuard 修复后保持防抖

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-008 |
| 用例标题 | DebounceGuard 修复后保持防抖 |
| 优先级 | P1 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | 1 个用户 |
| 测试步骤 | 1. T=0 PUT /api/leads/{id}<br>2. T=100 再次 PUT /api/leads/{id}<br>3. 验证第 2 次仍防抖 |
| 预期结果 | 100ms 内仍防抖 |
| 修复后预期 | 防抖功能保留 |
| 自动化标记 | 建议自动化 |

#### TC-PF10-009：DebounceGuard 修复后不影响并发用户

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-009 |
| 用例标题 | DebounceGuard 修复后不影响并发用户 |
| 优先级 | P0 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | 50 用户 |
| 测试步骤 | 1. 50 用户同时 PUT 各自 lead<br>2. 验证 100% 200 |
| 预期结果 | 并发不互锁 |
| 修复后预期 | userId 隔离 |
| 自动化标记 | 建议自动化（并发） |

#### TC-PF10-010：DebounceGuard 修复后端到端流畅

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF10-010 |
| 用例标题 | DebounceGuard 修复后端到端流畅 |
| 优先级 | P0 |
| 对应缺陷 | PF-10 |
| 严重程度 | 中 |
| 前置条件 | e2e 套件 |
| 测试步骤 | 1. 跑完整 8 步主链路（分配→跟进→协同→成交）<br>2. 每步间隔 1s<br>3. 100% PASS |
| 预期结果 | e2e 流畅 |
| 修复后预期 | 链路全过 |
| 自动化标记 | 建议自动化 |

---

## 十、缺陷 PF-11 notifications 表 type_code 列 collation 与 leads.id 不一致专项

> **缺陷位置**: DB 迁移
> **用例数**: 10

#### TC-PF11-001：notifications 表与 leads 表 join 查询不报 collation 错误

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-001 |
| 用例标题 | notifications 表与 leads 表 join 查询不报 collation 错误 |
| 优先级 | P0 |
| 对应缺陷 | PF-11 |
| 严重程度 | 高 |
| 前置条件 | DB 修复前/后 |
| 测试步骤 | 1. `SELECT n.*, l.code FROM notifications n INNER JOIN leads l ON n.lead_id = l.id LIMIT 1`<br>2. 验证（修复前）报错 `Illegal mix of collations`<br>3. 验证（修复后）正常返回 |
| 预期结果 | 修复后无 collation 错误 |
| 修复后预期 | join 正常 |
| 自动化标记 | 建议自动化 |

#### TC-PF11-002：批量 DELETE 不报 collation 错误

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-002 |
| 用例标题 | 批量 DELETE 不报 collation 错误 |
| 优先级 | P0 |
| 对应缺陷 | PF-11 |
| 严重程度 | 高 |
| 前置条件 | DB |
| 测试步骤 | 1. `DELETE FROM notifications WHERE type_code IN ('lead_assigned', 'customer_added')`<br>2. 验证（修复前）部分失败<br>3. 验证（修复后）成功 |
| 预期结果 | 修复后批量正常 |
| 修复后预期 | DELETE 顺利 |
| 自动化标记 | 建议自动化 |

#### TC-PF11-003：跨表 JOIN 操作正常

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-003 |
| 用例标题 | 跨表 JOIN 操作正常 |
| 优先级 | P0 |
| 对应缺陷 | PF-11 |
| 严重程度 | 高 |
| 前置条件 | DB |
| 测试步骤 | 1. 跑全部跨表 join SQL（notifications × leads, notifications × users, leads × posts, leads × accounts）<br>2. 验证无 collation 报错 |
| 预期结果 | 全部 join 成功 |
| 修复后预期 | 跨表 OK |
| 自动化标记 | 建议自动化 |

#### TC-PF11-004：中文 type_code 标签查询

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-004 |
| 用例标题 | 中文 type_code 标签查询 |
| 优先级 | P1 |
| 对应缺陷 | PF-11 |
| 严重程度 | 中 |
| 前置条件 | 存在中文 type_code 通知 |
| 测试步骤 | 1. `SELECT * FROM notifications WHERE type_code = '客户已添加'`<br>2. 验证返回 |
| 预期结果 | 修复后中文查询正常 |
| 修复后预期 | 中文 tag 可查 |
| 自动化标记 | 建议自动化 |

#### TC-PF11-005：utf8mb4_unicode_ci 与 utf8mb4_0900_ai_ci 兼容

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-005 |
| 用例标题 | utf8mb4_unicode_ci 与 utf8mb4_0900_ai_ci 兼容 |
| 优先级 | P0 |
| 对应缺陷 | PF-11 |
| 严重程度 | 高 |
| 前置条件 | DB |
| 测试步骤 | 1. `SHOW VARIABLES LIKE 'collation%'`<br>2. 验证修复后 server collation 与每张表一致 |
| 预期结果 | 全部统一为 utf8mb4_0900_ai_ci |
| 修复后预期 | 一致 |
| 自动化标记 | 建议自动化 |

#### TC-PF11-006：数据库迁移修复 collation

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-006 |
| 用例标题 | 数据库迁移修复 collation |
| 优先级 | P0 |
| 对应缺陷 | PF-11 |
| 严重程度 | 高 |
| 前置条件 | 迁移脚本 |
| 测试步骤 | 1. 跑迁移脚本<br>2. 验证 `ALTER TABLE notifications CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci`<br>3. 验证迁移幂等 |
| 预期结果 | 迁移成功 |
| 修复后预期 | 一致 |
| 自动化标记 | 建议自动化（CI 迁移） |

#### TC-PF11-007：修复后批量操作性能

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-007 |
| 用例标题 | 修复后批量操作性能 |
| 优先级 | P1 |
| 对应缺陷 | PF-11 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. `DELETE FROM notifications WHERE created_at < '2025-01-01'`<br>2. 验证耗时 < 1s |
| 预期结果 | 性能正常 |
| 修复后预期 | 修复不破坏性能 |
| 自动化标记 | 建议自动化 |

#### TC-PF11-008：修复后查询性能

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-008 |
| 用例标题 | 修复后查询性能 |
| 优先级 | P1 |
| 对应缺陷 | PF-11 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. `EXPLAIN SELECT * FROM notifications WHERE type_code = 'lead_assigned'`<br>2. 验证使用索引 |
| 预期结果 | 索引命中 |
| 修复后预期 | 性能不降 |
| 自动化标记 | 建议自动化 |

#### TC-PF11-009：修复后索引使用

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-009 |
| 用例标题 | 修复后索引使用 |
| 优先级 | P1 |
| 对应缺陷 | PF-11 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. `EXPLAIN` 多条 join 查询<br>2. 验证 index 仍使用 |
| 预期结果 | 索引有效 |
| 修复后预期 | 索引保持 |
| 自动化标记 | 建议自动化 |

#### TC-PF11-010：修复后端到端流程

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-PF11-010 |
| 用例标题 | 修复后端到端流程 |
| 优先级 | P0 |
| 对应缺陷 | PF-11 |
| 严重程度 | 高 |
| 前置条件 | 修复后 e2e |
| 测试步骤 | 1. 跑分配→通知→跟进→成交完整 e2e<br>2. 验证无 collation 报错 |
| 预期结果 | e2e 全过 |
| 修复后预期 | 业务恢复 |
| 自动化标记 | 建议自动化 |

---

## 十一、缺陷 BF-01 状态 alias 翻译缺读路径专项

> **缺陷位置**: `leads.service.ts:249 applyLeadFilters` 不做 alias 翻译
> **用例数**: 10
> **回归意义**: 状态筛选同时支持中英文

#### TC-BF01-001：GET /api/leads?status=跟进中 命中（修复后）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-001 |
| 用例标题 | GET /api/leads?status=跟进中 命中（修复后） |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | V1 '跟进中' 客资存在 |
| 测试步骤 | 1. `GET /api/leads?status=跟进中`<br>2. 验证返回含 V1 客资 |
| 预期结果 | HTTP 200 + V1 客资 |
| 修复后预期 | STATUS_ALIASES['跟进中'] = ['in_followup'] 生效 |
| 自动化标记 | 建议自动化 |

#### TC-BF01-002：GET /api/leads?status=in_followup 命中

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-002 |
| 用例标题 | GET /api/leads?status=in_followup 命中 |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | 存在 in_followup 客资 |
| 测试步骤 | 1. `GET /api/leads?status=in_followup`<br>2. 验证返回 |
| 预期结果 | HTTP 200 + 客资 |
| 修复后预期 | 英文 OK |
| 自动化标记 | 建议自动化 |

#### TC-BF01-003：8 个 status 状态全部 alias 翻译

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-003 |
| 用例标题 | 8 个 status 状态全部 alias 翻译 |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | 8 个 status 都有客资 |
| 测试步骤 | 1. 8 次 `GET /api/leads?status={8个值}`<br>2. 每次都返回对应客资 |
| 预期结果 | 8/8 通过 |
| 修复后预期 | 翻译完整 |
| 自动化标记 | 建议自动化 |

#### TC-BF01-004：5 个 addStatus 全部 alias 翻译

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-004 |
| 用例标题 | 5 个 addStatus 全部 alias 翻译 |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | 5 个 addStatus 都有客资 |
| 测试步骤 | 1. 5 次 `GET /api/leads?addStatus={5个值}`<br>2. 每次都返回 |
| 预期结果 | 5/5 通过 |
| 修复后预期 | addStatus 翻译完整 |
| 自动化标记 | 建议自动化 |

#### TC-BF01-005：7 个 processStatus 全部 alias 翻译

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-005 |
| 用例标题 | 7 个 processStatus 全部 alias 翻译 |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | 7 个 processStatus 都有客资 |
| 测试步骤 | 1. 7 次 `GET /api/leads?processStatus={7个值}`<br>2. 每次都返回 |
| 预期结果 | 7/7 通过 |
| 修复后预期 | processStatus 翻译完整 |
| 自动化标记 | 建议自动化 |

#### TC-BF01-006：修复后 applyLeadFilters 走 STATUS_ALIASES

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-006 |
| 用例标题 | 修复后 applyLeadFilters 走 STATUS_ALIASES |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 代码 review `leads.service.ts:249`<br>2. 验证 status/addStatus/processStatus 三个分支都调 translate |
| 预期结果 | 三分支都翻译 |
| 修复后预期 | 走 alias |
| 自动化标记 | 建议自动化（单测） |

#### TC-BF01-007：销售端筛选"跟进中" tab 包含 V1 数据

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-007 |
| 用例标题 | 销售端筛选"跟进中" tab 包含 V1 数据 |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | sales1 名下有 V1 客资 |
| 测试步骤 | 1. 浏览器销售端"跟进中"tab<br>2. 验证 V1 客资出现 |
| 预期结果 | V1 客资出现 |
| 修复后预期 | 前端 OK |
| 自动化标记 | 建议自动化 |

#### TC-BF01-008：主管端筛选兼容 V1 中文

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-008 |
| 用例标题 | 主管端筛选兼容 V1 中文 |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | supervisor 账号 |
| 测试步骤 | 1. 主管端"跟进中"筛选<br>2. 验证 V1 中文客资出现 |
| 预期结果 | 主管端兼容 |
| 修复后预期 | 主管端 OK |
| 自动化标记 | 建议自动化 |

#### TC-BF01-009：修复后写入路径不破坏

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-009 |
| 用例标题 | 修复后写入路径不破坏 |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. `PUT /api/leads/{id}` addStatus=added 写入 V2 英文<br>2. `GET /api/leads?addStatus=added` 包含刚写入<br>3. `GET /api/leads?addStatus=已添加` 包含历史 V1 |
| 预期结果 | 写入不破坏读路径 |
| 修复后预期 | 写入读路径一致 |
| 自动化标记 | 建议自动化 |

#### TC-BF01-010：修复后读路径完整

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF01-010 |
| 用例标题 | 修复后读路径完整 |
| 优先级 | P0 |
| 对应缺陷 | BF-01 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 跑 TC-BF01-001~009 全部用例<br>2. 100% PASS |
| 预期结果 | 读路径全通 |
| 修复后预期 | 读路径完整 |
| 自动化标记 | 建议自动化 |

---

## 十二、缺陷 BF-09 close() 无 owner 校验专项

> **缺陷位置**: `collaboration-tasks.service.ts` close 方法
> **用例数**: 10

#### TC-BF09-001：销售 close 自己发起的协同（成功）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-001 |
| 用例标题 | 销售 close 自己发起的协同（成功） |
| 优先级 | P0 |
| 对应缺陷 | BF-09 |
| 严重程度 | 中 |
| 前置条件 | sales1 自己发起的 pending 协同 |
| 测试步骤 | 1. sales1 `POST /api/collaboration-tasks/{id}/close`<br>2. 验证 HTTP 200 |
| 预期结果 | HTTP 200，status=closed |
| 修复后预期 | owner 校验通过 |
| 自动化标记 | 建议自动化 |

#### TC-BF09-002：销售 close 他人发起的协同（403）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-002 |
| 用例标题 | 销售 close 他人发起的协同（403） |
| 优先级 | P0 |
| 对应缺陷 | BF-09 |
| 严重程度 | 中 |
| 前置条件 | sales1 看 sales2 的协同 |
| 测试步骤 | 1. sales1 `POST /api/collaboration-tasks/{sales2_task_id}/close`<br>2. 验证 HTTP 403 |
| 预期结果 | HTTP 403 Forbidden |
| 修复后预期 | 修复后拒绝 |
| 自动化标记 | 建议自动化 |

#### TC-BF09-003：运营 close 销售的协同（403）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-003 |
| 用例标题 | 运营 close 销售的协同（403） |
| 优先级 | P0 |
| 对应缺陷 | BF-09 |
| 严重程度 | 中 |
| 前置条件 | sales1 协同存在 |
| 测试步骤 | 1. staff1 `POST /api/collaboration-tasks/{sales1_task_id}/close`<br>2. 验证 HTTP 403 |
| 预期结果 | HTTP 403 |
| 修复后预期 | 修复后运营不能 close 销售协同 |
| 自动化标记 | 建议自动化 |

#### TC-BF09-004：close() 加 actor 校验

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-004 |
| 用例标题 | close() 加 actor 校验 |
| 优先级 | P0 |
| 对应缺陷 | BF-09 |
| 严重程度 | 中 |
| 前置条件 | 代码 |
| 测试步骤 | 1. 单测 close() 函数<br>2. 验证 actor=requester 才能 close |
| 预期结果 | 单测通过 |
| 修复后预期 | 校验逻辑就位 |
| 自动化标记 | 建议自动化（单测） |

#### TC-BF09-005：修复后 close 与 handle 权限分离

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-005 |
| 用例标题 | 修复后 close 与 handle 权限分离 |
| 优先级 | P0 |
| 对应缺陷 | BF-09 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. sales1 自己发起的协同可以 close<br>2. 同一个协同，运营（handler）不能 close<br>3. 运营能 handle 但不能 close |
| 预期结果 | 权限分离 |
| 修复后预期 | close/handle 独立 |
| 自动化标记 | 建议自动化 |

#### TC-BF09-006：修复后 close 二次调用幂等

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-006 |
| 用例标题 | 修复后 close 二次调用幂等 |
| 优先级 | P1 |
| 对应缺陷 | BF-09 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. sales1 close 自己协同 2 次<br>2. 验证 2 次都成功，DB 状态保持 closed |
| 预期结果 | 幂等 |
| 修复后预期 | 幂等保持 |
| 自动化标记 | 建议自动化 |

#### TC-BF09-007：修复后 close 操作日志

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-007 |
| 用例标题 | 修复后 close 操作日志 |
| 优先级 | P1 |
| 对应缺陷 | BF-09 |
| 严重程度 | 低 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. close 协同后查 operation_logs<br>2. 验证有 close 记录 |
| 预期结果 | 日志写入 |
| 修复后预期 | 日志 OK |
| 自动化标记 | 建议自动化 |

#### TC-BF09-008：修复后 close 通知

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-008 |
| 用例标题 | 修复后 close 通知 |
| 优先级 | P1 |
| 对应缺陷 | BF-09 |
| 严重程度 | 低 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. close 协同<br>2. 验证运营收到 `collab_closed` 通知 |
| 预期结果 | 通知发出 |
| 修复后预期 | 通知 OK |
| 自动化标记 | 建议自动化 |

#### TC-BF09-009：修复后 close 状态机

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-009 |
| 用例标题 | 修复后 close 状态机 |
| 优先级 | P0 |
| 对应缺陷 | BF-09 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. pending → close 正常<br>2. handled → close 报 422<br>3. closed → close 幂等 |
| 预期结果 | 状态机正确 |
| 修复后预期 | 状态机 OK |
| 自动化标记 | 建议自动化 |

#### TC-BF09-010：修复后端到端验证

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF09-010 |
| 用例标题 | 修复后端到端验证 |
| 优先级 | P0 |
| 对应缺陷 | BF-09 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 跑完整协同 e2e：sales 发起 → staff handle → sales close<br>2. 100% PASS |
| 预期结果 | e2e 全过 |
| 修复后预期 | 业务恢复 |
| 自动化标记 | 建议自动化 |

---

## 十三、缺陷 BF-15 改派（admin PUT 改 assignedSalesUserId）不发 lead_assigned 通知专项

> **缺陷位置**: `leads.controller.ts:295-350 PUT` 路径不触发通知
> **用例数**: 10
> **回归意义**: P0 业务缺陷，主管改派后新销售不知道

#### TC-BF15-001：主管改派 → 新销售收到 lead_assigned 通知（v1.2 修复后）

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-001 |
| 用例标题 | 主管改派 → 新销售收到 lead_assigned 通知（v1.2 修复后） |
| 优先级 | P0 |
| 对应缺陷 | BF-15 |
| 严重程度 | 高 |
| 前置条件 | admin2 已登录，存在 lead 分配给 sales1 |
| 测试步骤 | 1. admin2 `PUT /api/leads/{id}` body `{assignedSalesUserId: "sales2_user_id"}`<br>2. 验证 sales2 收到 lead_assigned 通知 |
| 预期结果 | sales2 notifications 表新增 lead_assigned |
| 修复后预期 | PUT 路径触发通知 |
| 自动化标记 | 建议自动化（核心回归） |

#### TC-BF15-002：主管改派 → 旧销售收到通知

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-002 |
| 用例标题 | 主管改派 → 旧销售收到通知 |
| 优先级 | P0 |
| 对应缺陷 | BF-15 |
| 严重程度 | 高 |
| 前置条件 | 同上 |
| 测试步骤 | 1. 改派 sales1 → sales2<br>2. 验证 sales1 收到 lead_unassigned/lead_reassigned 通知 |
| 预期结果 | 旧销售有通知 |
| 修复后预期 | 双向通知 |
| 自动化标记 | 建议自动化 |

#### TC-BF15-003：改派 title 为"客资已改派给您"

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-003 |
| 用例标题 | 改派 title 为"客资已改派给您" |
| 优先级 | P0 |
| 对应缺陷 | BF-15 |
| 严重程度 | 中 |
| 前置条件 | 同上 |
| 测试步骤 | 1. 验证 sales2 收到通知 title="客资已改派给您" |
| 预期结果 | title 正确 |
| 修复后预期 | 通知文案清晰 |
| 自动化标记 | 建议自动化 |

#### TC-BF15-004：改派 content 含原销售名

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-004 |
| 用例标题 | 改派 content 含原销售名 |
| 优先级 | P0 |
| 对应缺陷 | BF-15 |
| 严重程度 | 中 |
| 前置条件 | 同上 |
| 测试步骤 | 1. 验证 content 含 "原销售：sales1 姓名" |
| 预期结果 | content 完整 |
| 修复后预期 | 信息完整 |
| 自动化标记 | 建议自动化 |

#### TC-BF15-005：主管改派走 PUT /api/leads/:id 路径

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-005 |
| 用例标题 | 主管改派走 PUT /api/leads/:id 路径 |
| 优先级 | P0 |
| 对应缺陷 | BF-15 |
| 严重程度 | 高 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 代码 review `leads.controller.ts:295-350`<br>2. 验证 PUT 路径调用 notificationService.create(lead_assigned) |
| 预期结果 | 代码正确 |
| 修复后预期 | 代码+测试同步 |
| 自动化标记 | 建议自动化（code review） |

#### TC-BF15-006：主管改派走 service.update 路径

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-006 |
| 用例标题 | 主管改派走 service.update 路径 |
| 优先级 | P0 |
| 对应缺陷 | BF-15 |
| 严重程度 | 高 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 验证 service.update 检测到 assignedSalesUserId 变化<br>2. 触发 notification |
| 预期结果 | service 层通知 |
| 修复后预期 | service 层正确 |
| 自动化标记 | 建议自动化 |

#### TC-BF15-007：主管改派多销售同时收通知

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-007 |
| 用例标题 | 主管改派多销售同时收通知 |
| 优先级 | P1 |
| 对应缺陷 | BF-15 |
| 严重程度 | 中 |
| 前置条件 | 多销售 |
| 测试步骤 | 1. 改派触发 2 条通知（sales2 lead_assigned + sales1 reassigned）<br>2. 验证 2 条都在 notifications 表 |
| 预期结果 | 双通知 |
| 修复后预期 | 双发 |
| 自动化标记 | 建议自动化 |

#### TC-BF15-008：主管改派在多端口同步

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-008 |
| 用例标题 | 主管改派在多端口同步 |
| 优先级 | P1 |
| 对应缺陷 | BF-15 |
| 严重程度 | 中 |
| 前置条件 | 多端口环境 |
| 测试步骤 | 1. admin2 在 3000 端口改派<br>2. sales2 在 3000 端口 WebSocket 立即收到 |
| 预期结果 | socket 推送 |
| 修复后预期 | socket OK |
| 自动化标记 | 建议自动化 |

#### TC-BF15-009：改派操作日志

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-009 |
| 用例标题 | 改派操作日志 |
| 优先级 | P1 |
| 对应缺陷 | BF-15 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 改派后查 operation_logs<br>2. 验证有 reassign 记录 |
| 预期结果 | 日志写入 |
| 修复后预期 | 日志 OK |
| 自动化标记 | 建议自动化 |

#### TC-BF15-010：改派后状态机正确

| 字段 | 内容 |
| --- | --- |
| 用例编号 | TC-BF15-010 |
| 用例标题 | 改派后状态机正确 |
| 优先级 | P0 |
| 对应缺陷 | BF-15 |
| 严重程度 | 中 |
| 前置条件 | 修复后 |
| 测试步骤 | 1. 改派后 lead.status 保持不变（in_followup）<br>2. lead.assigned_sales_user_id 更新为新销售<br>3. follow_records 写一条"被改派"记录 |
| 预期结果 | 状态机+日志完整 |
| 修复后预期 | 业务恢复 |
| 自动化标记 | 建议自动化 |

---

## 十四、回归执行指南

### 14.1 执行顺序

```text
1. PF-01 系列（端口文档，已撤回）— 文档对齐 5min
2. PF-09 系列（端口文档不一致）— 文档对齐 10min
3. PF-10 系列（DebounceGuard）— 自动化回归 15min
4. PF-11 系列（DB collation）— 自动化回归 20min
5. BF-09 系列（close 权限）— 单元+集成 15min
6. PF-04 系列（中文校验）— e2e 20min
7. PF-02 系列（详情注入）— e2e 20min
8. BF-01 系列（状态 alias）— e2e 25min
9. PF-08 系列（V1 中文 addStatus）— e2e 20min
10. BF-15 系列（改派通知）— e2e 25min
11. PF-05 系列（token 失效）— 安全扫描 30min
12. PF-06 系列（端口隔离）— 矩阵测试 30min
```

### 14.2 通过判定

| 缺陷 | 通过条件 | 阻塞条件 |
| --- | --- | --- |
| PF-01 | 文档已撤回标记 | 仍被当有效缺陷 |
| PF-02 | TC-PF02-001~010 全过 | 仍为 null |
| PF-04 | TC-PF04-001~008 全过 | 中文仍 422 |
| PF-05 | TC-PF05-001~012 全过 | 失效 token 仍 200 |
| PF-06 | TC-PF06-001~012 全过 | sales 进 3001 仍 200 |
| PF-08 | TC-PF08-001~010 全过 | V1 中文 0 命中 |
| PF-09 | 文档与实际一致 | 仍描述错端口 |
| PF-10 | TC-PF10-001~010 全过 | 自动化仍被防抖 |
| PF-11 | TC-PF11-001~010 全过 | join 仍 collation 错 |
| BF-01 | TC-BF01-001~010 全过 | 中文 status 仍 0 命中 |
| BF-09 | TC-BF09-001~010 全过 | 跨角色仍可 close |
| BF-15 | TC-BF15-001~010 全过 | 改派仍无通知 |

---

## 十五、关联文档

- `doc/B端-详细测试用例-真实执行结果.md`（缺陷源文档）
- `doc/CLAUDE.md`（端口与架构说明）
- `doc/v1.2-完整交付版-AB端任务分配.md`（开发任务分配）
- `doc/test-cases/06-权限和数据安全测试用例.md`（安全相关基础用例）
- `doc/test-cases/07-通知和WebSocket测试用例.md`（通知相关基础用例）
- `doc/test-cases/11-当前问题专项.md`（配套的当前问题专项文档）

---

> 文档结束。本专项回归共 **119 条** 用例，覆盖真实确认的 **11 个缺陷**（PF-01~11, BF-01/09/15），P0 缺陷 5 个（PF-04/05/06, BF-15）+ P1 缺陷 3 个（PF-02/08, BF-01）+ P2 缺陷 1 个（BF-09）+ P3 文档/性能 2 个（PF-09/10/11, PF-01）。
