# v1.2 P1 修复回归矩阵

> 文档生成时间：2026-06-03
> 依据：
> - `doc/B端-P1修复-导出+字符集.md`（E-P1-01/03/05 + D-P1-01/02）
> - `doc/B端-P1修复-性能+数据.md`（P-P1-01/02/03）
> - `doc/B端-P1修复-权限+Fixture.md`（R-P1-01/02/03 + F-P1-01~05）
> - `doc/B端-P1修复-状态机+文档.md`（S-P1-01/02/03/04 + C-P1-01/02）
> - `doc/B端-P1修复-通知1-5.md`（N-P1-01~05）
> - `doc/B端-P1修复-通知6-10.md`（N-P1-06~10）
> - `doc/v1.2-b端-redis适配说明.md`（BullMQ / AOF / key 过期）
> - `doc/B端-v1.2-收尾批次回归测试用例.md`（25→60+ 收尾用例）
> 用途：v1.2 P1 修复项 + Redis 适配 + 收尾批次的完整回归矩阵

---

## 一、覆盖总览

| 修复项 | 编号 | 标题 | 用例数 | 优先级 |
| --- | --- | --- | --- | --- |
| B1 | TC-CHRS-001 ~ 010 | 导出+字符集 P1 修复 | 10 | P1 |
| B2 | TC-PERFV-001 ~ 010 | 性能+数据 P1 修复 | 10 | P1 |
| B3 | TC-PERF-001 ~ 010 | 权限+Fixture P1 修复 | 10 | P1 |
| B4 | TC-STMV-001 ~ 010 | 状态机+文档 P1 修复 | 10 | P1 |
| B5 | TC-NOT15-001 ~ 010 | 通知 1-5 P1 修复 | 10 | P1 |
| B6 | TC-NOT610-001 ~ 010 | 通知 6-10 P1 修复 | 10 | P1 |
| C1 | TC-RDS-001 ~ 018 | Redis 适配专项回归 | 18 | P1 |
| D1 | TC-LEAD-01 ~ 020 | 收尾批次回归用例（25→60+） | 20 | P1 |
| **合计** | | | **108** | |

---

## 二、修复项 B1：导出+字符集 P1 修复

**对应修复文档**：`doc/B端-P1修复-导出+字符集.md`
**修复要点**：
- E-P1-01：delete / disable / view_sensitive 注入点（6 controller + 2 module）
- E-P1-03：导出 1 分钟防抖（userId+exportType 60s 窗口）
- E-P1-05：exports 复合索引 (user_id, created_at) + (user_id, export_type, created_at)
- D-P1-01：leads 字符集脏数据核查（无脏数据，无需 backfill）
- D-P1-02：孤儿 lead 软删除（UPDATE 待 DBA 确认）

### TC-CHRS-001：CSV UTF-8 BOM 输出 Excel 不乱码

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1（导出+字符集） |
| 修复前行为 | CSV 无 BOM，Excel 打开中文乱码 |
| 修复后行为 | CSV 头部加 UTF-8 BOM（EF BB BF），Excel 正确识别 |
| 自动化建议 | 必须自动化 |

**前置条件**：sales01 登录 + 至少 5 个中文昵称 lead

**测试步骤**：
1. sales01 调 `POST /api/exports` body=`{exportType:'leads'}`
2. 等待完成
3. 下载 CSV 文件
4. 用十六进制查看文件头 3 字节

**预期结果**：
- 文件头 3 字节为 `EF BB BF`
- Excel 打开中文列名（"昵称"、"联系方式"）不乱码
- 中文 nickname 列正确显示

**验证命令**：
```bash
xxd -l 16 export.csv | head -1
# 期望：00000000: efbb bf......(以 efbbbf 开头)
```

---

### TC-CHRS-002：日文/emoji 字符正确显示

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | 日文/emoji 乱码或被替换为 ? |
| 修复后行为 | 4 字节 UTF-8 字符正确写入 CSV |
| 自动化建议 | 建议自动化 |

**前置条件**：leads 表有日文 / emoji nickname（如「テスト」「😀客户」）

**测试步骤**：
1. 准备测试数据：INSERT 1 条 lead nickname='テストユーザー😀'
2. 导出 leads CSV
3. 验证文件内容

**预期结果**：
- CSV 中 nickname 字段原样保留日文 + emoji
- 字符长度正确（UTF-8 字节数）
- 不会被替换为 `?`

---

### TC-CHRS-003：繁体中文导出

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | 繁简转换或乱码 |
| 修复后行为 | 繁体原样保留 |
| 自动化建议 | 建议自动化 |

**前置条件**：leads 表 nickname 含繁体字

**测试步骤**：
1. 准备 nickname='繁體中文測試'
2. 导出
3. 验证

**预期结果**：
- CSV 中"繁體中文測試"原样保留
- 不会自动转简体

---

### TC-CHRS-004：韩文/阿拉伯文导出

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | 多字节字符乱码 |
| 修复后行为 | 韩文/阿拉伯文正确显示 |
| 自动化建议 | 建议自动化 |

**前置条件**：leads 表 nickname 含韩文/阿拉伯文

**测试步骤**：
1. 准备 nickname='테스트用户'（韩文+中文混合）
2. 准备 nickname='اختبار'（阿拉伯文）
3. 导出
4. 验证

**预期结果**：
- 混合字符原样保留
- 无字符丢失或乱码

---

### TC-CHRS-005：CSV 含特殊字符（逗号、引号、换行）的转义

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | 特殊字符破坏 CSV 格式 |
| 修复后行为 | 按 RFC 4180 转义（双引号包裹 + 双引号转义） |
| 自动化建议 | 必须自动化 |

**前置条件**：lead nickname 含 `, " \n` 字符

**测试步骤**：
1. 准备 nickname='客户, "VIP"\n' 含逗号 + 引号 + 换行
2. 准备 remark 含单引号 `'` 和 `"`
3. 导出
4. 验证

**预期结果**：
- nickname 列用 `"客户, ""VIP""\n"` 转义
- 解析时不丢字段
- 换行在引号内不破坏行结构

---

### TC-CHRS-006：XLSX 文件含中文表头

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | XLSX 中文表头乱码 |
| 修复后行为 | XLSX 表头正确显示 |
| 自动化建议 | 建议自动化 |

**前置条件**：导出类型支持 XLSX（部分类型）

**测试步骤**：
1. admin 导出 leads 为 XLSX（若支持）
2. 用 Excel 或 WPS 打开
3. 验证表头

**预期结果**：
- 表头："编号 / 昵称 / 联系方式 / 跟进状态 / 创建时间"
- 中文不乱码

---

### TC-CHRS-007：JSON 导出格式

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | JSON 中文转 \uXXXX |
| 修复后行为 | JSON 中文原样输出（ensure_ascii=False） |
| 自动化建议 | 建议自动化 |

**前置条件**：导出支持 JSON 类型

**测试步骤**：
1. 导出 JSON
2. 验证 JSON 字符串

**预期结果**：
- JSON 包含 "昵称": "测试客户" 而非 "昵称": "测试客户"
- 字段值可读

---

### TC-CHRS-008：文件名中文不乱码

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | 文件名中文 HTTP header 乱码 |
| 修复后行为 | Content-Disposition 用 RFC 5987 编码 |
| 自动化建议 | 必须自动化 |

**前置条件**：导出任务完成

**测试步骤**：
1. 触发导出，文件名含中文（如 `客资导出_2026-06-03.csv`）
2. 抓 HTTP 响应头

**预期结果**：
- `Content-Disposition: attachment; filename="leads_20260603.csv"; filename*=UTF-8''%E5%AE%A2%E8%B5%84%E5%AF%BC%E5%87%BA_2026-06-03.csv`
- 浏览器下载时正确显示中文文件名

---

### TC-CHRS-009：Excel 公式注入防护（=`等）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | lead nickname 含 `=cmd|...` 可能被 Excel 当公式执行 |
| 修复后行为 | 以 `=`/`+`/`-`/`@` 开头的字段值加 `'` 前缀或转义 |
| 自动化建议 | 必须自动化 |

**前置条件**：leads 表 nickname 含 `=SUM(A1:A2)` / `+1+1` / `@VLOOKUP`

**测试步骤**：
1. 准备 3 条 lead，nickname 分别为 `=SUM(1,2)`、`+1+1`、`@cmd`
2. 导出
3. 打开 CSV 在 Excel 中

**预期结果**：
- CSV 中这些字段值前加 `'` 或被引号包裹
- Excel 打开显示为文本，不执行
- 不弹安全警告

---

### TC-CHRS-010：CSV 大小写敏感性

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B1 |
| 修复前行为 | CSV 大小写影响解析 |
| 修复后行为 | CSV 字段名统一为小写或与 API 一致 |
| 自动化建议 | 建议自动化 |

**前置条件**：导出 CSV

**测试步骤**：
1. 导出 leads
2. 检查 CSV 第一行表头大小写

**预期结果**：
- 表头统一为 `id,nickname,contact_info,...`（snake_case）
- 不混用 CamelCase / PascalCase

---

## 三、修复项 B2：性能+数据 P1 修复

**对应修复文档**：`doc/B端-P1修复-性能+数据.md`
**修复要点**：
- P-P1-01：post_metrics 表 + entity（M17 migration）
- P-P1-02：BullMQ 异步队列（exports.processor.ts Worker + exports.service.ts 异步改造）
- P-P1-03：dashboard 缓存（5 分钟 TTL）

### TC-PERFV-001：VU=50 列表分页 p95<1.5s 修复后实测

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 |
| 修复前行为 | VU=50 p95>1.5s |
| 修复后行为 | 加索引 + 缓存后 VU=50 p95<1.5s |
| 自动化建议 | 必须自动化（k6） |

**前置条件**：后端已启动，索引已建（M17），cache 已开启

**测试步骤**：
1. k6 跑 `/api/leads?limit=20&offset=0` 50 并发 30 秒
2. 统计 p95 / p99

**预期结果**：
- p95 < 1500ms
- p99 < 2500ms
- 错误率 < 0.1%

---

### TC-PERFV-002：VU=100 列表分页 p95<1.5s 修复后实测

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 |
| 修复前行为 | VU=100 p95>2s |
| 修复后行为 | VU=100 p95<1.5s |
| 自动化建议 | 必须自动化（k6） |

**前置条件**：后端已启动，索引 + 缓存已生效

**测试步骤**：
1. k6 跑 `/api/leads?limit=20&offset=0` 100 并发 30 秒

**预期结果**：
- p95 < 1500ms
- p99 < 3000ms
- 错误率 < 0.1%

---

### TC-PERFV-003：索引使用率（EXPLAIN）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 + D1-收尾 M17 |
| 修复前行为 | 索引未生效，type=ALL |
| 修复后行为 | 9 个 M17 索引全部生效 |
| 自动化建议 | 必须自动化（SQL 验证） |

**前置条件**：M17 已执行

**测试步骤**：
1. 跑 9 个 EXPLAIN（详见 TC-INDEX-04）
2. 校验 type=ref/range 不为 ALL

**预期结果**：
- 9 个查询全部走索引
- type 不为 ALL

---

### TC-PERFV-004：慢 SQL 日志

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 |
| 修复前行为 | 慢 SQL 无日志 |
| 修复后行为 | 慢 SQL 写入日志（>1s） |
| 自动化建议 | 建议自动化 |

**前置条件**：开启 slow_query_log

**测试步骤**：
1. 跑一个 >1s 的查询
2. 查 MySQL slow query log

**预期结果**：
- slow log 出现该查询
- Query_time > 1

---

### TC-PERFV-005：10000+ 数据量导出

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 + BullMQ 异步队列 |
| 修复前行为 | 10000+ 数据量导出阻塞 HTTP 响应 |
| 修复后行为 | 异步导出，HTTP 立即返回 task_id |
| 自动化建议 | 必须自动化 |

**前置条件**：leads 表 10000+ 条数据；REDIS_URL 已配置

**测试步骤**：
1. admin 调 `POST /api/exports` body=`{exportType:'leads', scope:'all'}`
2. 记录 HTTP 响应时间 T1
3. 轮询 GET /api/exports/:id 直至 completed
4. 下载 CSV 验证行数

**预期结果**：
- T1 < 500ms（HTTP 立即返回）
- 异步处理 30 秒内完成
- CSV 行数 = 10000+
- 后端进程 CPU 不持续 100%

---

### TC-PERFV-006：批量导入 1000+ 性能

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 |
| 修复前行为 | 1000+ 导入串行处理慢 |
| 修复后行为 | 批量 INSERT 优化（VALUES 多行） |
| 自动化建议 | 建议自动化 |

**前置条件**：admin + 1000+ leads CSV

**测试步骤**：
1. admin 调 `POST /api/imports/leads` 上传 CSV
2. 记录耗时 T1

**预期结果**：
- 1000+ 行导入 < 30 秒
- 错误行有详细错误信息
- DB 实际插入数 = 成功行数

---

### TC-PERFV-007：缓存命中 < 100ms

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 + P-P1-03 dashboard 缓存 |
| 修复前行为 | dashboard summary 每次实时查 |
| 修复后行为 | 5 分钟内缓存命中，< 100ms 返回 |
| 自动化建议 | 必须自动化 |

**前置条件**：已触发一次 GET /api/dashboard/summary 写入缓存

**测试步骤**：
1. 第一次 GET /api/dashboard/summary 记录 T1
2. 5 分钟内第二次 GET 记录 T2

**预期结果**：
- T1 > T2（第一次走 DB，第二次走缓存）
- T2 < 100ms
- 响应内容相同

---

### TC-PERFV-008：缓存未命中降级

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 |
| 修复前行为 | 缓存未命中时阻塞或失败 |
| 修复后行为 | 缓存未命中降级查 DB，不报错 |
| 自动化建议 | 必须自动化 |

**前置条件**：已清空缓存

**测试步骤**：
1. `cacheService.deletePattern('dashboard:*')`
2. 立即 GET /api/dashboard/summary

**预期结果**：
- HTTP 200，正常返回
- 缓存被重新填充
- 不报错或超时

---

### TC-PERFV-009：Redis 切换 cluster

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 + C1（Redis 适配） |
| 修复前行为 | 单机 Redis 故障全挂 |
| 修复后行为 | Cluster 模式兼容（详见 C1） |
| 自动化建议 | 建议自动化 |

**前置条件**：REDIS_URL 指向 Cluster 模式

**测试步骤**：
1. 配置 REDIS_URL=redis-cluster://...
2. 重启后端
3. 跑导出任务

**预期结果**：
- 队列初始化成功
- 导出任务正常处理

---

### TC-PERFV-010：Node 单线程瓶颈

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B2 |
| 修复前行为 | Node 单线程，同步导出阻塞事件循环 |
| 修复后行为 | 异步队列 + Worker 隔离 |
| 自动化建议 | 建议自动化 |

**前置条件**：大导出任务运行中

**测试步骤**：
1. 触发 1 个大导出（10000+ 行）
2. 同步触发 /api/dashboard/summary
3. 记录 summary 响应时间

**预期结果**：
- summary 响应 < 500ms（不受导出阻塞）
- 后端进程事件循环不被卡死

---

## 四、修复项 B3：权限+Fixture P1 修复

**对应修复文档**：`doc/B端-P1修复-权限+Fixture.md`
**修复要点**：
- R-P1-01：登录失败计数 + 锁定（users.failed_login_count + last_failed_at，5 次锁定）
- R-P1-02：bcrypt 密码哈希（新写密码 bcrypt 哈希，存量明文保留）
- R-P1-03：session 字段兼容性（getSessionUserId helper 统一 65 处引用）
- F-P1-01 ~ 05：5 个 fixture 增补

### TC-PERF-001：canAccessLead 销售/运营/admin/owner 各角色

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 + R-P1-03（getSessionUserId helper） |
| 修复前行为 | canAccessLead 各角色行为依赖 inline 校验，字段名漂移 |
| 修复后行为 | 统一 helper + 一致矩阵 |
| 自动化建议 | 必须自动化 |

**前置条件**：4 个角色账号 + 多个 lead

**测试步骤**：
1. sales01 调 `GET /api/leads/:id-self`（分配给 sales01）→ 200
2. sales01 调 `GET /api/leads/:id-other`（分配给 sales02）→ 404
3. staff01 调 `GET /api/leads/:id-staff`（自己录入）→ 200
4. staff01 调 `GET /api/leads/:id-other-staff`（其他运营）→ 404
5. admin 调任意 lead → 200
6. owner 调任意 lead → 200

**预期结果**：
- 矩阵 4×2=8 全部符合预期
- 字段名 session.userId/user.sub/user.id 全部走 getSessionUserId 兼容

---

### TC-PERF-002：canAccessOrder 销售/教务/admin/owner 各角色

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 |
| 修复前行为 | 字段名漂移 |
| 修复后行为 | 4 角色 × 4 归属矩阵 |
| 自动化建议 | 必须自动化 |

**前置条件**：4 角色 + 4 归属订单

**测试步骤**：
1. sales01 调自己订单 → 200
2. sales01 调他人订单 → 404
3. academic02 调自己订单 → 200
4. academic02 调他人订单 → 404
5. academic01 调池单 → 200
6. admin 调任意订单 → 200
7. owner 调任意订单 → 200

**预期结果**：
- 7 种组合全部符合预期
- session.userId/user.sub/user.id 兼容

---

### TC-PERF-003：service 层过滤（applyLeadScope 等）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 |
| 修复前行为 | controller 过滤不一致 |
| 修复后行为 | service.applyLeadScope / applyOrdersScope 集中过滤 |
| 自动化建议 | 必须自动化 |

**前置条件**：4 角色 + 全量数据

**测试步骤**：
1. sales01 调 `GET /api/leads?scope=sales` → 仅返回自己
2. staff01 调 `GET /api/leads?scope=staff` → 仅返回自己录入
3. admin 调 `GET /api/leads?scope=all` → 全部
4. owner 调同 admin

**预期结果**：
- 4 角色 × scope 全部正确过滤
- 不会出现"销售看到他人 lead"

---

### TC-PERF-004：越权 HTTP 状态码（401/403/404）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 |
| 修复前行为 | 越权返 200 |
| 修复后行为 | 未登录 401；无权限 403；归属不符 404 |
| 自动化建议 | 必须自动化 |

**前置条件**：全套角色

**测试步骤**：
1. 无 token 调任意接口 → 401
2. sales01 调 `/api/users`（admin only）→ 403
3. sales01 调 `/api/leads/:id-other` → 404
4. sales01 调 `/api/orders/:id-other` → 404

**预期结果**：
- HTTP 状态码语义化
- 不暴露存在性（404 not found 而非 403 forbidden）

---

### TC-PERF-005：主管/admin 全局查询也走 service 过滤

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 |
| 修复前行为 | admin/owner 旁路过滤 |
| 修复后行为 | 仍走 applyLeadScope，scope=all 走全量 |
| 自动化建议 | 必须自动化 |

**前置条件**：admin/owner 登录

**测试步骤**：
1. admin 调 `GET /api/leads` → 全量
2. admin 调 `GET /api/leads?scope=all` → 同上
3. owner 调同 admin

**预期结果**：
- admin/owner 默认 scope=all
- 仍走 service 过滤（不会越过 service 层）

---

### TC-PERF-006：fixture 预置数据

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 + F-P1-01 ~ 05 |
| 修复前行为 | 17 个魔数 ID 不存在 |
| 修复后行为 | fixture_legacy_magic_ids.sql 等 5 个 fixture 补充 |
| 自动化建议 | 必须自动化 |

**前置条件**：5 个 fixture 全部跑

**测试步骤**：
1. 跑 5 个 fixture
2. 校验行数增量
3. 校验 17 个魔数 ID 存在

**预期结果**：
- leads 143 → 157（+14 magic IDs）
- orders 25 → 29（+4）
- notifications 40 → 42（+2）
- operation_logs 35 → 37（+2）
- order_follow_records 12 → 13（+1）
- accounts +1 / posts +1 / employees +1

---

### TC-PERF-007：role 7 值枚举

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 |
| 修复前行为 | role 字段无统一枚举 |
| 修复后行为 | 7 值：admin/staff/owner/sales/academic/operation/supervisor |
| 自动化建议 | 必须自动化 |

**前置条件**：users 表

**测试步骤**：
1. 跑 `SELECT DISTINCT role FROM users`
2. 校验值在 7 值内
3. 跑 fixture 跑后续 `INSERT INTO users (role) VALUES ('xxx')` 试错

**预期结果**：
- 实际 role 值 ∈ 7 值
- 非法 role 写入失败（DB CHECK 约束或 service 校验）

---

### TC-PERF-008：跨端口越权（3000→3001）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 |
| 修复前行为 | sales 在端口 3000 登录，可访问 3001 接口 |
| 修复后行为 | 3001 端口仅 owner 角色可登录 |
| 自动化建议 | 必须自动化 |

**前置条件**：sales01 / staff01 / admin / owner

**测试步骤**：
1. sales01 在端口 3000 登录 → 200
2. sales01 访问 3001 接口 → 403
3. owner 在 3001 登录 → 200
4. owner 访问 3000 接口 → 403（仅 admin/owner 在 3000 也允许）

**预期结果**：
- 跨端口越权严格隔离
- 端口 3001 仅 owner 可登录

---

### TC-PERF-009：token 伪造

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 |
| 修复前行为 | token 可伪造 |
| 修复后行为 | token 验证严格（HMAC + 过期） |
| 自动化建议 | 必须自动化 |

**前置条件**：真实 token

**测试步骤**：
1. 修改 token 第 10 字符（HMAC 校验失败）→ 401
2. 修改 token 过期时间（篡改 payload）→ 401
3. 用 admin 的 token 伪造成 owner → 401（role 字段从 DB 二次校验）

**预期结果**：
- 所有伪造尝试返 401
- 不接受任何篡改

---

### TC-PERF-010：停用账号的 token 失效

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B3 + R-P1-01（账号锁定） |
| 修复前行为 | 账号锁定后 token 仍可用 |
| 修复后行为 | token 与 user.status 联动，status=locked/inactive 立即失效 |
| 自动化建议 | 必须自动化 |

**前置条件**：sales01 已登录

**测试步骤**：
1. sales01 登录，记 token
2. 5 次输错密码，账号锁定
3. 用 token 调任意接口
4. admin 解锁 sales01
5. 用 token 再调

**预期结果**：
- 步骤 3：HTTP 401 或 403 "account locked"
- 步骤 4：admin `POST /api/users/staff` 改 status=active
- 步骤 5：admin 重新登录后可用

---

## 五、修复项 B4：状态机+文档 P1 修复

**对应修复文档**：`doc/B端-P1修复-状态机+文档.md`
**修复要点**：
- S-P1-01：closeDeal 写 `process_status='deal_done'`，`status='in_followup'`（不再写 `deal_closed`）
- S-P1-02：handleNote 非空校验（trim 后必填）
- S-P1-03：acceptHandover rejected 状态校验（由 P0-03 覆盖）
- S-P1-04：collab close 回退 lead.status（in_collaboration → in_followup）
- C-P1-01/02：状态机文档化（部分完成）

### TC-STMV-001：客资 status 8 个状态

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 |
| 修复前行为 | 旧实现写 `deal_closed` 等非法值 |
| 修复后行为 | 8 状态枚举：new/assigned/in_followup/in_collaboration/operation_handled/added_success/deal_closed/invalid |
| 自动化建议 | 必须自动化 |

**前置条件**：8 状态各 1 条 lead

**测试步骤**：
1. 准备 8 条 lead 状态分别对应枚举
2. 调 `GET /api/leads?status=<each>`
3. 校验响应

**预期结果**：
- 8 状态全部能过滤
- 非法状态值返 0 行（无报错）

**DB 核对 SQL**：
```sql
SELECT DISTINCT status FROM leads;
-- 期望值在 8 值内
```

---

### TC-STMV-002：add_status 5 个状态

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 |
| 修复前行为 | add_status 枚举不清晰 |
| 修复后行为 | 5 状态：未添加/已添加/拒绝添加/添加中/添加成功 |
| 自动化建议 | 必须自动化 |

**前置条件**：5 状态各 1 条 lead

**测试步骤**：
1. 准备 5 条 add_status
2. 过滤

**预期结果**：
- 5 状态全部能过滤

---

### TC-STMV-003：process_status 7 个状态

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 |
| 修复前行为 | 旧实现无 process_status 或值漂移 |
| 修复后行为 | 7 状态：沟通中/已报价/已签约/已添加/已成交/已流失/无效 |
| 自动化建议 | 必须自动化 |

**前置条件**：7 状态各 1 条 lead

**测试步骤**：
1. 准备 7 条
2. 过滤 + closeDeal 测试

**预期结果**：
- 7 状态全部能过滤
- closeDeal 后 process_status='已成交'（与 S-P1-01 对齐）

---

### TC-STMV-004：deal_status 4 个状态

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 |
| 修复前行为 | deal_status 状态机不完整 |
| 修复后行为 | 4 状态：未成交/已成交/已流失/已退款 |
| 自动化建议 | 必须自动化 |

**前置条件**：4 状态各 1 条 lead

**测试步骤**：
1. 准备 4 条
2. 过滤

**预期结果**：
- 4 状态全部能过滤

---

### TC-STMV-005：collaboration_tasks 4 个状态（pending/handling/handled/closed）+ timeout

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 |
| 修复前行为 | 状态机遗漏 timeout |
| 修复后行为 | 5 状态：pending/handling/handled/closed/timeout |
| 自动化建议 | 必须自动化 |

**前置条件**：5 状态各 1 条协同任务

**测试步骤**：
1. 准备 5 条
2. 过滤

**预期结果**：
- 5 状态全部能过滤

---

### TC-STMV-006：状态机非法流转拒绝

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 |
| 修复前行为 | 状态可乱跳 |
| 修复后行为 | 非法流转返 400 |
| 自动化建议 | 必须自动化 |

**前置条件**：5 状态各 1 条协同任务

**测试步骤**：
1. pending → closed（合法：sales close）
2. closed → pending（非法）
3. handled → handling（非法）

**预期结果**：
- 合法流转 200
- 非法流转 400

---

### TC-STMV-007：状态机旁路流转

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 |
| 修复前行为 | 旁路流转可绕过 |
| 修复后行为 | 旁路流转受控 |
| 自动化建议 | 必须自动化 |

**前置条件**：pending 协同

**测试步骤**：
1. sales01 调 `PATCH /api/collaboration-tasks/:id` body=`{status:'closed'}`（旁路 close 路由）

**预期结果**：
- HTTP 400/403
- DB status 不变
- 只能通过 PUT/PATCH :id/close 走

---

### TC-STMV-008：状态机逆向流转

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 |
| 修复前行为 | 逆向流转可绕过 |
| 修复后行为 | 拒绝逆向（除 S-P1-04 特殊路径） |
| 自动化建议 | 必须自动化 |

**前置条件**：handled 协同

**测试步骤**：
1. sales01 调 handle（pending → handled）
2. staff01 调 handle（handled → handling 逆向）

**预期结果**：
- HTTP 400
- 状态保持 handled

---

### TC-STMV-009：文档 vs 实际 schema 一致性

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 + C-P1-01/02 |
| 修复前行为 | 文档与 schema 不一致 |
| 修复后行为 | 文档明确状态机 |
| 自动化建议 | 必须自动化 |

**前置条件**：schema.sql + 状态机文档

**测试步骤**：
1. 对照 schema.sql 表字段 + 文档
2. 校验枚举值是否匹配

**预期结果**：
- leads.status 字段定义与文档一致
- leads.process_status / add_status / deal_status 一致
- collaboration_tasks.status 包含 timeout

---

### TC-STMV-010：文档 vs 实际 type_code 字符串一致性

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B4 + B5 + B6 |
| 修复前行为 | typeCode 字符串漂移 |
| 修复后行为 | 文档与代码常量一致 |
| 自动化建议 | 必须自动化 |

**前置条件**：N-P1-01 修复后 NOTIFICATION_TYPES 统一

**测试步骤**：
1. 对照 12 个通知 typeCode（`lead_assigned`/`collaboration_requested`/...）
2. 校验代码常量、DB typeCode、文档三处一致

**预期结果**：
- 12 个 typeCode 三处一致
- 无 `supervisor_suggestion` / `lead_deal_done`（已 deprecated）被新代码写入

---

## 六、修复项 B5：通知 1-5 P1 修复

**对应修复文档**：`doc/B端-P1修复-通知1-5.md`
**修复要点**：
- N-P1-01：enum 一致性（notification-types.ts 与 notifications.ts 统一）
- N-P1-02：order_updated 通知触发（emitOrderUpdated + 30s 去重）
- N-P1-03：portType 修复（abnormal 通知 portType='academic'）
- N-P1-04：deal_closed 拆分（ORDER_CREATED/ORDER_HANDED_OVER/ORDER_ACCEPTED）
- N-P1-05：多 tab race 条件（未完成）

### TC-NOT15-001：lead_assigned 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 |
| 修复前行为 | 通知接收方/routeHint/payload 字段缺失 |
| 修复后行为 | receiverId 为被分配销售；routeHint=`/sales/leads/<id>`；payload 含 leadId |
| 自动化建议 | 必须自动化 |

**前置条件**：admin / sales01 在线

**测试步骤**：
1. admin 调 `PATCH /api/leads/:id/assign` body=`{salesUserId:'user-test-sales-01'}`
2. sales01 查 `GET /api/notifications?type=lead_assigned`
3. 校验 payload

**预期结果**：
- 通知存在 typeCode='lead_assigned'
- receiverId='user-test-sales-01'
- relatedId=leadId
- routeHint='/sales/leads/<leadId>'
- content 含 "新客资已分配"

---

### TC-NOT15-002：collaboration_requested 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 |
| 修复前行为 | 通知可能路由错误 |
| 修复后行为 | receiverId=运营；routeHint=`/operation/collaboration?taskId=<id>` |
| 自动化建议 | 必须自动化 |

**前置条件**：sales01 + staff01 + 1 个 lead

**测试步骤**：
1. sales01 调 `POST /api/collaboration-tasks` body=`{leadId, type:'source_confirm', reason:'x'}`
2. staff01 查 `GET /api/notifications?type=collaboration_requested`

**预期结果**：
- typeCode='collaboration_requested'
- receiverId 为运营（多 receiver）
- routeHint='/operation/collaboration?taskId=<taskId>'
- content 含 "请协助确认客资来源"

---

### TC-NOT15-003：customer_not_passed 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 |
| 修复前行为 | 通知可能路由错误 |
| 修复后行为 | receiverId=运营；routeHint=`/operation/leads?leadId=<id>` |
| 自动化建议 | 必须自动化 |

**前置条件**：sales01 + 1 个 lead

**测试步骤**：
1. sales01 调 `PATCH /api/leads/:id/status` body=`{addStatus:'not_passed', reason:'客户未通过'}`
2. staff01 查 `GET /api/notifications?type=customer_not_passed`

**预期结果**：
- typeCode='customer_not_passed'
- receiverId=运营
- routeHint='/operation/leads?leadId=<leadId>'

---

### TC-NOT15-004：collaboration_handled 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 + N-P1-06（relatedId 修复） |
| 修复前行为 | relatedId 指向 leadId，跳转错误 |
| 修复后行为 | relatedId=taskId，relatedType='collaboration_task' |
| 自动化建议 | 必须自动化 |

**前置条件**：staff01 + 1 个 pending 协同

**测试步骤**：
1. staff01 调 `POST /api/collaboration-tasks/:id/handle` body=`{handledNote:'已与客户沟通'}`
2. sales01 查 `GET /api/notifications?type=collaboration_handled`

**预期结果**：
- typeCode='collaboration_handled'
- relatedId=taskId
- relatedType='collaboration_task'
- routeHint='/sales/collaboration?taskId=<taskId>'
- content 含 "已处理"

---

### TC-NOT15-005：customer_added 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 |
| 修复前行为 | 通知可能路由错误 |
| 修复后行为 | receiverId=运营；routeHint=`/operation/leads?leadId=<id>` |
| 自动化建议 | 必须自动化 |

**前置条件**：sales01 + 1 个 lead

**测试步骤**：
1. sales01 调 `PATCH /api/leads/:id/status` body=`{addStatus:'added_success'}`
2. staff01 查 `GET /api/notifications?type=customer_added`

**预期结果**：
- typeCode='customer_added'
- receiverId=运营
- routeHint='/operation/leads?leadId=<leadId>'

---

### TC-NOT15-006：5 个通知的入表 type_code 正确

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 + N-P1-01 |
| 修复前行为 | typeCode 漂移 |
| 修复后行为 | 5 个 typeCode 统一 |
| 自动化建议 | 必须自动化 |

**前置条件**：5 个 typeCode 各 1 条通知

**测试步骤**：
1. 跑 `SELECT DISTINCT type_code FROM notifications WHERE type_code IN ('lead_assigned','collaboration_requested','customer_not_passed','collaboration_handled','customer_added')`
2. 校验 NOTIFICATION_TYPES 常量

**预期结果**：
- 5 个 typeCode 与代码 NOTIFICATION_TYPES 100% 一致

---

### TC-NOT15-007：5 个通知的已读/未读状态正确

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 |
| 修复前行为 | 通知已读/未读可能错乱 |
| 修复后行为 | 默认 unread=true，markRead 后 false |
| 自动化建议 | 必须自动化 |

**前置条件**：5 个通知各 1 条

**测试步骤**：
1. 查 `GET /api/notifications` 校验 unread=true
2. 调 `POST /api/notifications/:id/read`
3. 再查校验 unread=false
4. 查 unread-count 变化

**预期结果**：
- 默认 unread=true
- markRead 后 unread=false
- unread-count 正确减 1

---

### TC-NOT15-008：5 个通知的批量触发（如批量改派）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 |
| 修复前行为 | 批量改派未发通知 |
| 修复后行为 | 批量改派每个 lead 触发 lead_assigned |
| 自动化建议 | 必须自动化 |

**前置条件**：10 个未分配 lead

**测试步骤**：
1. admin 调批量分配 API（10 个 lead 一次性分配给 sales01）
2. 查 sales01 通知

**预期结果**：
- 10 条 lead_assigned 通知
- 每条 receiverId=sales01, relatedId 不同

---

### TC-NOT15-009：5 个通知的失效场景（客资删除后）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 |
| 修复前行为 | 客资删除后通知 link 失效 |
| 修复后行为 | 软删除 lead，通知 link 仍可点击（"客资已删除"占位） |
| 自动化建议 | 必须自动化 |

**前置条件**：1 个 lead + 1 条 lead_assigned 通知

**测试步骤**：
1. admin 软删除 lead
2. sales01 点击通知

**预期结果**：
- 跳转到 /sales/leads/<id>
- 前端显示"客资已删除"占位
- 不报错

---

### TC-NOT15-010：5 个通知的 socket 推送 + 60s polling 兜底

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B5 + C1（60s polling） |
| 修复前行为 | socket 断线后无通知 |
| 修复后行为 | socket 实时 + 60s polling 兜底 |
| 自动化建议 | 必须自动化 |

**前置条件**：5 个通知场景

**测试步骤**：
1. sales01 断网（关闭 socket）
2. admin 触发 lead_assigned 等 5 个通知
3. sales01 恢复网络
4. 60 秒内 polling 拉到通知

**预期结果**：
- 60s 内 5 条通知全部出现
- typeCode 一致

---

## 七、修复项 B6：通知 6-10 P1 修复

**对应修复文档**：`doc/B端-P1修复-通知6-10.md`
**修复要点**：
- N-P1-06：relatedId 错位（COLLAB_HANDLED relatedId 指向 taskId）
- N-P1-07：节点抄送销售（未完成）
- N-P1-08：buildRouteHint 导出/导入分支
- N-P1-09：markRead 一致性（未完成）
- N-P1-10：unreadCount 跨端口（未完成）

### TC-NOT610-001：deal_closed 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 + N-P1-04（拆分） |
| 修复前行为 | deal_closed 与 handOver/accept 共用 typeCode |
| 修复后行为 | 拆分为 order_created / order_handed_over / order_accepted |
| 自动化建议 | 必须自动化 |

**前置条件**：sales01 + 1 个 lead

**测试步骤**：
1. sales01 调 `POST /api/leads/:id/close-deal` body=`{amount:'5000', serviceType:'x'}`
2. academic02 / admin 查 `GET /api/notifications?type=order_created`

**预期结果**：
- 通知 typeCode='order_created'（不是 deal_closed）
- receiverId 为 academic + admin
- routeHint='/academic/orders/<id>' 或 '/sales/orders/<id>'

---

### TC-NOT610-002：order_created 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 |
| 修复前行为 | 同 deal_closed |
| 修复后行为 | 独立 typeCode='order_created' |
| 自动化建议 | 必须自动化 |

**前置条件**：同 TC-NOT610-001

**测试步骤**：
1. 走 close-deal 完整流程
2. 校验 order_created 通知存在

**预期结果**：
- typeCode='order_created'
- content 含"已创建订单"
- payload 含 orderId, leadId, serviceType, amount

---

### TC-NOT610-003：order_updated 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 + N-P1-02 |
| 修复前行为 | 订单状态更新无通知 |
| 修复后行为 | 30s 内同字段组合去重；发 order_updated |
| 自动化建议 | 必须自动化 |

**前置条件**：归属 sales01 的订单

**测试步骤**：
1. sales01 调 `PATCH /api/orders/:id` body=`{orderStatus:'in_progress'}`
2. sales01 查 `GET /api/notifications?type=order_updated`
3. 立即再 PATCH 一次同字段
4. 查通知数（应不变）

**预期结果**：
- 第一条 PATCH 产生 1 条通知
- 第二条 PATCH 在 30s 内去重，不重复
- receiverId 为 admin/owner（admin/owner 兜底）
- content 含变化字段名

---

### TC-NOT610-004：order_abnormal 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 + N-P1-03（portType 修复） |
| 修复前行为 | portType 错，导致通知无法正确路由 |
| 修复后行为 | portType='academic' |
| 自动化建议 | 必须自动化 |

**前置条件**：academic02 + 1 个归属订单

**测试步骤**：
1. academic02 调 `POST /api/orders/:id/abnormal-feedback`
2. 校验 notifications 表

**预期结果**：
- typeCode='order_abnormal'
- portType='academic'（字段值在 DB）
- receiverId 为 sales01 + admin

---

### TC-NOT610-005：export_finished 通知接收方、routeHint、payload 完整

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 + N-P1-08 |
| 修复前行为 | 导出通知无 routeHint |
| 修复后行为 | routeHint='/academic/exports?taskId=<id>'（唯一可消费页） |
| 自动化建议 | 必须自动化 |

**前置条件**：sales01 + 1 个 lead

**测试步骤**：
1. sales01 调 `POST /api/exports` body=`{exportType:'leads'}`
2. 等待完成
3. sales01 查 `GET /api/notifications?type=export_finished`

**预期结果**：
- typeCode='export_finished'
- routeHint='/academic/exports?taskId=<id>'（虽然 sales 不一定有页面，但不报错）
- payload 含 exportType, rowCount

---

### TC-NOT610-006：5 个通知的入表 type_code 正确

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 |
| 修复前行为 | typeCode 漂移 |
| 修复后行为 | 5 个 typeCode 统一 |
| 自动化建议 | 必须自动化 |

**前置条件**：5 个 typeCode 各 1 条

**测试步骤**：
1. 跑 SQL：SELECT DISTINCT type_code FROM notifications WHERE type_code IN ('deal_closed','order_created','order_updated','order_abnormal','export_finished')

**预期结果**：
- 5 个 typeCode 全部能查
- 与 NOTIFICATION_TYPES 一致

---

### TC-NOT610-007：5 个通知的已读/未读状态正确

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 |
| 修复前行为 | 同 B5 |
| 修复后行为 | 默认 unread=true，markRead 后 false |
| 自动化建议 | 必须自动化 |

**前置条件**：5 个通知各 1 条

**测试步骤**：
1. 校验 unread=true
2. markRead
3. 校验 unread=false

**预期结果**：
- 与 B5 行为一致

---

### TC-NOT610-008：5 个通知的批量触发

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 |
| 修复前行为 | 批量改状态无通知 |
| 修复后行为 | 批量触发独立通知 |
| 自动化建议 | 必须自动化 |

**前置条件**：10 个订单

**测试步骤**：
1. 批量改 10 个订单状态
2. 校验通知数

**预期结果**：
- 10 条 order_updated 通知
- 30s 内去重（10 个不同 orderId 不去重）

---

### TC-NOT610-009：5 个通知的失效场景

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 |
| 修复前行为 | 通知 link 失效 |
| 修复后行为 | 软删除，link 仍可点 |
| 自动化建议 | 必须自动化 |

**前置条件**：1 个订单 + 1 条 order_updated 通知

**测试步骤**：
1. 软删除订单
2. sales01 点击通知

**预期结果**：
- 跳转到 /sales/orders/<id>
- 显示"订单已删除"占位

---

### TC-NOT610-010：5 个通知的 socket 推送 + 60s polling 兜底

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | B6 + C1（60s polling） |
| 修复前行为 | socket 断线无通知 |
| 修复后行为 | socket 实时 + 60s polling 兜底 |
| 自动化建议 | 必须自动化 |

**前置条件**：5 个通知场景

**测试步骤**：
1. sales01 断网
2. 触发 5 个通知
3. 恢复网络
4. 60s 内 polling 拉

**预期结果**：
- 60s 内 5 条通知全部出现
- typeCode 一致

---

## 八、修复项 C1：Redis 适配专项回归

**对应修复文档**：`doc/v1.2-b端-redis适配说明.md`
**修复要点**：
- BullMQ 异步队列
- AOF 持久化（每 1s/每次写）
- key 过期清理
- 60s polling 兜底
- 升级到 Redis Adapter 的推荐做法

### TC-RDS-001：REDIS_URL 配置时 BullMQ 队列初始化

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | REDIS_URL 未配置时启动失败 |
| 修复后行为 | 配置后 BullMQ 队列正常初始化 |
| 自动化建议 | 必须自动化 |

**前置条件**：Redis 服务运行中，REDIS_URL=redis://127.0.0.1:6379

**测试步骤**：
1. 后端启动
2. 查日志 "exports queue initialized (redis: redis://127.0.0.1:6379)"
3. 调 `POST /api/exports`
4. Redis 客户端 `KEYS bull:*` 看到队列

**预期结果**：
- 日志含 queue initialized
- Redis 中存在 bull:exports:* keys
- 导出任务通过 queue.add 进入队列

---

### TC-RDS-002：REDIS_URL 未配置时 setImmediate 降级

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 未配置时启动失败 |
| 修复后行为 | 降级 setImmediate 同步执行 |
| 自动化建议 | 必须自动化 |

**前置条件**：REDIS_URL 留空

**测试步骤**：
1. 后端启动
2. 查日志 "queue not initialized, fallback to setImmediate"
3. 调 `POST /api/exports`
4. 验证任务直接同步执行

**预期结果**：
- 后端启动成功
- 导出任务在 HTTP 响应前完成
- 无 Redis 连接

---

### TC-RDS-003：BullMQ Worker 并发=2 处理

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 + P-P1-02 |
| 修复前行为 | 串行处理 |
| 修复后行为 | concurrency=2 并发 |
| 自动化建议 | 必须自动化 |

**前置条件**：REDIS_URL 已配置

**测试步骤**：
1. 同时触发 4 个导出任务
2. 观察 worker 日志

**预期结果**：
- 同一时间最多 2 个并发
- 4 个任务在 2×t 时间内完成

---

### TC-RDS-004：BullMQ Worker 失败时 status=failed

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 失败时卡 pending |
| 修复后行为 | status=failed，attempts 计数 |
| 自动化建议 | 必须自动化 |

**前置条件**：REDIS_URL 已配置

**测试步骤**：
1. 注入失败数据（如 exportType=invalid）
2. 触发导出
3. 查 status

**预期结果**：
- status='failed'
- exports.errorMessage 含错误信息
- 通知用户 export_finished 通知含 failed 状态

---

### TC-RDS-005：BullMQ Queue 连接失败降级

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | Redis 不可用时全挂 |
| 修复后行为 | Queue 连接失败降级到 setImmediate |
| 自动化建议 | 必须自动化 |

**前置条件**：REDIS_URL 配置但服务宕机

**测试步骤**：
1. 停 Redis
2. 调 `POST /api/exports`
3. 验证降级

**预期结果**：
- HTTP 仍 200
- 任务同步执行（不依赖 Redis）
- 日志含"queue connection failed, fallback to setImmediate"

---

### TC-RDS-006：queue.add 失败降级

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | queue.add 失败时 HTTP 500 |
| 修复后行为 | queue.add 失败 try/catch 降级 |
| 自动化建议 | 必须自动化 |

**前置条件**：Redis 内存满

**测试步骤**：
1. 触发大导出
2. queue.add OOM
3. 验证降级

**预期结果**：
- 不抛 500
- 任务同步执行

---

### TC-RDS-007：AOF 持久化（每 1s/每次写）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 默认 RDB 可能丢失最近 1 秒 |
| 修复后行为 | AOF appendfsync everysec |
| 自动化建议 | 建议自动化 |

**前置条件**：Redis appendonly=yes, appendfsync=everysec

**测试步骤**：
1. 触发 1 个导出任务（写入 Redis）
2. 1 秒后 `kill -9 redis-server`
3. 重启 Redis
4. 查 bull queue

**预期结果**：
- 任务在 Redis 重启后恢复
- 不丢失最近 1 秒写入

---

### TC-RDS-008：Redis 重启后任务恢复

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 重启后任务丢失 |
| 修复后行为 | AOF 持久化下任务恢复 |
| 自动化建议 | 必须自动化 |

**前置条件**：队列有 1 个 pending 任务

**测试步骤**：
1. 触发 1 个任务
2. 在任务执行前 kill -9 redis
3. 重启 redis
4. 查 bull queue 状态

**预期结果**：
- 任务恢复为 pending 或 completed
- 不丢失

---

### TC-RDS-009：key 过期清理（notification TTL）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 通知 key 永驻 |
| 修复后行为 | 通知 TTL 7 天自动过期 |
| 自动化建议 | 必须自动化 |

**前置条件**：缓存服务配置 TTL

**测试步骤**：
1. 写入 1 个通知 key
2. `TTL <key>` 验证 TTL > 0
3. 等待 7 天（或 mock 时间）

**预期结果**：
- TTL = 7×24×3600
- 过期后 key 自动删除

---

### TC-RDS-010：Redis cluster 模式兼容

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 不兼容 cluster |
| 修复后行为 | ioredis cluster 模式正常 |
| 自动化建议 | 建议自动化 |

**前置条件**：Redis cluster（3 节点）

**测试步骤**：
1. REDIS_URL=redis-cluster://node1:6379,node2:6379,node3:6379
2. 后端启动
3. 跑导出

**预期结果**：
- 集群模式正常
- key 按 hash slot 分布

---

### TC-RDS-011：Redis 哨兵模式

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 不兼容哨兵 |
| 修复后行为 | 哨兵模式自动 failover |
| 自动化建议 | 建议自动化 |

**前置条件**：Redis Sentinel（3 哨兵 + 1 主 + 1 从）

**测试步骤**：
1. REDIS_URL 配哨兵
2. 主 Redis 故障
3. 哨兵选新主

**预期结果**：
- 后端自动重连到新主
- 队列不中断

---

### TC-RDS-012：Redis 内存淘汰策略

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 内存满时拒绝写入 |
| 修复后行为 | 配置 LRU/allkeys-lru 淘汰 |
| 自动化建议 | 建议自动化 |

**前置条件**：maxmemory 256mb, maxmemory-policy=allkeys-lru

**测试步骤**：
1. 写入大量 key 占满内存
2. 新写入触发 LRU 淘汰
3. 验证老 key 被淘汰

**预期结果**：
- 内存不溢出
- LRU 淘汰生效

---

### TC-RDS-013：Redis 缓存雪崩

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 大量 key 同时过期致 DB 压力 |
| 修复后行为 | TTL 加随机抖动 |
| 自动化建议 | 建议自动化 |

**前置条件**：缓存开启

**测试步骤**：
1. 同时写入 1000 个缓存 key，TTL=300s
2. 观察 TTL 分布

**预期结果**：
- TTL 分布在 280~320s（带 5% 抖动）
- 不会同时过期

---

### TC-RDS-014：Redis 缓存穿透

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 不存在的 key 反复打 DB |
| 修复后行为 | 缓存空值（NULL）+ 短 TTL |
| 自动化建议 | 必须自动化 |

**前置条件**：缓存服务

**测试步骤**：
1. 查不存在的 lead id
2. 第二次查同 id

**预期结果**：
- 第二次命中空值缓存，不打 DB
- 短 TTL（如 30s）后过期

---

### TC-RDS-015：Redis 缓存击穿

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 热点 key 过期瞬间并发打 DB |
| 修复后行为 | 互斥锁（setNX）防止击穿 |
| 自动化建议 | 必须自动化 |

**前置条件**：缓存服务

**测试步骤**：
1. 100 并发查同一 key
2. key 恰好过期

**预期结果**：
- 仅 1 个请求回源
- 其他 99 个等待缓存重建
- DB 不会被击穿

---

### TC-RDS-016：Redis 缓存预热

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 冷启动首请求慢 |
| 修复后行为 | 启动时预热关键 key |
| 自动化建议 | 建议自动化 |

**前置条件**：缓存服务

**测试步骤**：
1. 后端启动
2. 立即查 dashboard summary

**预期结果**：
- 响应 < 100ms（已预热）
- 不再走 DB

---

### TC-RDS-017：Redis 分布式一致性

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 |
| 修复前行为 | 多副本时缓存不一致 |
| 修复后行为 | 单一 Redis 共享 |
| 自动化建议 | 建议自动化 |

**前置条件**：2 个后端实例

**测试步骤**：
1. 实例 A 写缓存
2. 实例 B 读

**预期结果**：
- 实例 B 命中实例 A 写的缓存
- 一致性 100%

---

### TC-RDS-018：Redis 与 DB 的一致性

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | C1 + P-P1-03 |
| 修复前行为 | 缓存与 DB 可能不一致 |
| 修复后行为 | 写 DB 后失效缓存 |
| 自动化建议 | 必须自动化 |

**前置条件**：缓存 + DB

**测试步骤**：
1. 读 dashboard summary（缓存 miss → DB → 缓存）
2. 直接改 DB（绕过 service）
3. 再读 summary

**预期结果**：
- 步骤 3 仍返回步骤 1 的旧值（缓存未失效）
- 通过 service 写 DB 后会失效缓存

---

## 九、修复项 D1：收尾批次回归用例（25→60+）

**对应修复文档**：`doc/B端-v1.2-收尾批次回归测试用例.md`
**修复要点**：
- M16 迁移 + 两新字段读写（已覆盖 TC-LEAD-01~05）
- 销售详情页 4 字段展示（已覆盖 TC-SALES-01~05）
- 节点超时扫描器 7 天阈值（已覆盖 TC-ORDER-01~05）
- 主管总览 abnormalOrders（已覆盖 TC-STATS-01~05）
- M17 性能索引（已覆盖 TC-INDEX-01~05）
- **本次补充**：扫描器边界、迁移回滚、字段历史快照等 15+ 条

### TC-LEAD-01 ~ 05：M16 迁移幂等 + 字段读写（已覆盖，见收尾文档）

> 详见 `doc/B端-v1.2-收尾批次回归测试用例.md` §1 的 TC-LEAD-01~05
> 本节不重复，标记为已覆盖

---

### TC-LEAD-06：节点超时扫描器无跟进记录按创建时间

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1（收尾-节点超时扫描器） |
| 修复前行为 | 无跟进记录时无法判断超时 |
| 修复后行为 | 扫描器在无跟进记录时按订单 created_at 判断 |
| 自动化建议 | 必须自动化 |

**前置条件**：1 个订单 order_status='in_progress', order_follow_records 为空，created_at < NOW()-7d

**测试步骤**：
1. 准备测试数据
2. admin 调 `POST /api/orders/scan-node-timeouts`
3. 校验响应

**预期结果**：
- 响应 notified >= 1
- DB notifications 新增 typeCode='order_node_overdue', relatedId=<orderId>

---

### TC-LEAD-07：节点超时扫描器幂等

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | 重复扫描产生重复通知 |
| 修复后行为 | recentlyNotified Map 7 天内不重复 |
| 自动化建议 | 必须自动化 |

**前置条件**：上一步已通知的订单

**测试步骤**：
1. 立即再调 `POST /api/orders/scan-node-timeouts`
2. 校验响应

**预期结果**：
- skipped >= 1
- DB 通知数不增加

---

### TC-LEAD-08：节点超时扫描器手动 trigger（POST /api/orders/scan-node-timeouts）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | 仅 cron 触发 |
| 修复后行为 | admin/owner 可手动 trigger |
| 自动化建议 | 必须自动化 |

**前置条件**：admin/owner 登录

**测试步骤**：
1. admin 调 `POST /api/orders/scan-node-timeouts`
2. sales01 调同 URL

**预期结果**：
- admin 200, {scanned, notified, skipped, failed}
- sales01 403

---

### TC-LEAD-09：节点超时扫描器通知对象（admin/owner）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | 通知可能发给所有用户 |
| 修复后行为 | 仅 admin/owner 收到 |
| 自动化建议 | 必须自动化 |

**前置条件**：1 个超时订单

**测试步骤**：
1. 触发扫描
2. 查 notifications 表

**预期结果**：
- receiverId ∈ {admin, owner} 用户
- sales01 不在 receiverId 列表
- academic01 不在

---

### TC-LEAD-10：主管总览 abnormalOrders

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | dashboard summary 无 abnormalOrders 字段 |
| 修复后行为 | summary 返 abnormalOrders = COUNT(*) WHERE order_status='abnormal' |
| 自动化建议 | 必须自动化 |

**前置条件**：admin 登录

**测试步骤**：
1. admin 调 `GET /api/dashboard/summary`
2. 校验 abnormalOrders 字段

**预期结果**：
- 字段存在，类型 number
- 数值 = `SELECT COUNT(*) FROM orders WHERE order_status='abnormal'`

---

### TC-LEAD-11：M17 性能索引 EXPLAIN 验证

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1（收尾-M17 索引） |
| 修复前行为 | 9 个索引可能未生效 |
| 修复后行为 | EXPLAIN 走索引 |
| 自动化建议 | 必须自动化 |

**前置条件**：M17 已执行

**测试步骤**：
1. 跑 9 个 EXPLAIN
2. 校验 type=ref/range

**预期结果**：
- 全部不为 ALL
- 详见收尾文档 TC-INDEX-04

---

### TC-LEAD-12：主管 total 缓存 5 分钟 TTL

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 + P-P1-03 |
| 修复前行为 | dashboard total 实时查 |
| 修复后行为 | 5 分钟缓存 |
| 自动化建议 | 必须自动化 |

**前置条件**：dashboard 已触发

**测试步骤**：
1. 第一次 GET /api/dashboard/summary
2. 改 DB（直接 INSERT）
3. 立即再 GET

**预期结果**：
- 第二次返回与第一次相同（缓存命中）
- 5 分钟后过期

---

### TC-LEAD-13：主管跨端口 dashboard 同步

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | admin 与 owner 端口数据不同步 |
| 修复后行为 | 两端口共享 DB，summary 相同 |
| 自动化建议 | 必须自动化 |

**前置条件**：admin (3000) + owner (3001)

**测试步骤**：
1. admin 调 `GET /api/dashboard/summary` (3000)
2. owner 调同 URL (3001)

**预期结果**：
- 响应内容一致
- abnormalOrders 数值相同

---

### TC-LEAD-14：节点超时扫描器边界（订单未到 7 天不通知）

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | 任何时间都通知 |
| 修复后行为 | 仅 >7 天通知 |
| 自动化建议 | 必须自动化 |

**前置条件**：1 个订单 created_at < NOW()-3d（不到 7 天）

**测试步骤**：
1. 触发扫描
2. 校验

**预期结果**：
- 不通知
- DB 无新增

---

### TC-LEAD-15：节点超时扫描器并发安全

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | 并发触发产生重复通知 |
| 修复后行为 | recentlyNotified Map 线程安全 |
| 自动化建议 | 建议自动化 |

**前置条件**：1 个超时订单

**测试步骤**：
1. 同时 10 个 HTTP 请求调 `POST /api/orders/scan-node-timeouts`

**预期结果**：
- 通知数 = 1（不重复）
- recentlyNotified 保证幂等

---

### TC-LEAD-16：M16 迁移回滚

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | 无回滚脚本 |
| 修复后行为 | M16__down.sql 删除两字段 |
| 自动化建议 | 必须自动化 |

**前置条件**：M16 已执行

**测试步骤**：
1. 跑 M16__lead_requirement_supervisor_note.down.sql
2. 校验字段已删

**预期结果**：
- `SHOW COLUMNS FROM leads WHERE Field IN ('requirement_note','supervisor_note')` 返 0 行
- 业务 GET /api/leads/:id 不再含 requirementNote/supervisorNote 字段

---

### TC-LEAD-17：M16 迁移失败回滚

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | 失败可能半完成 |
| 修复后行为 | 事务保证全部成功或全失败 |
| 自动化建议 | 必须自动化 |

**前置条件**：干净 DB

**测试步骤**：
1. 在 M16 中插入一个故意失败的 ALTER（如 `ALTER TABLE` 一个不存在的列）
2. 跑迁移
3. 校验

**预期结果**：
- 整次迁移回滚
- 两字段都不存在

---

### TC-LEAD-18：M17 索引创建失败回滚

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 |
| 修复前行为 | 索引创建可能部分成功 |
| 修复后行为 | 全部成功或全部回滚 |
| 自动化建议 | 必须自动化 |

**前置条件**：干净 DB

**测试步骤**：
1. 在 M17 中故意失败（如索引列超长）
2. 跑迁移

**预期结果**：
- 9 个索引全部回滚
- INFORMATION_SCHEMA.STATISTICS 无这些索引

---

### TC-LEAD-19：requirementNote 字段历史快照

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 + A1 operation-logs |
| 修复前行为 | 字段历史无快照 |
| 修复后行为 | 字段变更写 operation_logs，从 / 到 记录 |
| 自动化建议 | 必须自动化 |

**前置条件**：1 个 lead 已有 requirementNote

**测试步骤**：
1. sales01 改 lead.requirementNote
2. 查 operation_logs

**预期结果**：
- 日志存在 action='update'
- detail.from.requirementNote / detail.to.requirementNote 正确

---

### TC-LEAD-20：supervisorNote 字段历史快照

| 字段 | 内容 |
|------|------|
| 优先级 | P1 |
| 对应修复项 | D1 + A1 |
| 修复前行为 | 字段历史无快照 |
| 修复后行为 | 字段变更写 operation_logs |
| 自动化建议 | 必须自动化 |

**前置条件**：1 个 lead 已有 supervisorNote

**测试步骤**：
1. admin 改 lead.supervisorNote
2. 查 operation_logs

**预期结果**：
- 日志存在，detail.from / detail.to 记录

---

## 十、回归执行指南

### 10.1 准备阶段

1. 后端启动：
   - REDIS_URL 配置（BullMQ 测试需要）
   - MySQL 数据库
   - 跑 M16/M17 migration + 5 个 fixture
2. 前端启动
3. 准备 11 个 fixture 账号

### 10.2 执行顺序

1. **B1 字符集**：与导出测试同时跑
2. **B2 性能**：依赖 M17 索引 + Redis
3. **B3 权限**：与 A1~A5 协同
4. **B4 状态机**：与 P0 handover 协同
5. **B5 + B6 通知**：依赖所有 P0/P1 触发点
6. **C1 Redis**：可独立
7. **D1 收尾**：与 M16/M17 同步

### 10.3 自动化建议

| 用例范围 | 自动化方式 |
| --- | --- |
| TC-CHRS-001/005/009 | CSV 解析 + Excel 验证 |
| TC-PERFV-001/002 | k6 压测 |
| TC-PERFV-005 | 10000+ 数据量生成 + 异步导出 |
| TC-PERF-001~010 | supertest 矩阵 |
| TC-NOT15-001~005/006~010 | socket.io-client + polling mock |
| TC-NOT610-001~005 | 同上 |
| TC-RDS-001~018 | ioredis 客户端 + 超时/降级 mock |
| TC-LEAD-01~20 | 已覆盖在收尾文档 + 本次补充 |

### 10.4 失败兜底

- REDIS_URL 未配置：B2/C1 部分用例标记降级路径
- 后端未启动：所有 HTTP 用例标记 `⚠️ 跳过（需后端启动）`
- 通知 N-P1-05/07/09/10 未完成：相关 TC 标记 `⚠️ 跳过（agent 断连）`

---

## 十一、关联文档

- 修复依据 1：`doc/B端-P1修复-导出+字符集.md`
- 修复依据 2：`doc/B端-P1修复-性能+数据.md`
- 修复依据 3：`doc/B端-P1修复-权限+Fixture.md`
- 修复依据 4：`doc/B端-P1修复-状态机+文档.md`
- 修复依据 5：`doc/B端-P1修复-通知1-5.md`
- 修复依据 6：`doc/B端-P1修复-通知6-10.md`
- 修复依据 7：`doc/v1.2-b端-redis适配说明.md`
- 收尾依据：`doc/B端-v1.2-收尾批次回归测试用例.md`（25 条已覆盖）
- 通知 TC 风格参考：`doc/test-cases/07-通知和WebSocket测试用例.md`
- 性能 TC 风格参考：`doc/test-cases/08-导出和性能测试用例.md`
- 权限 TC 风格参考：`doc/test-cases/06-权限和数据安全测试用例.md`

---

**合计：B1~B6 + C1 + D1 共 8 大 P1 修复项，108 条专项回归用例（B1 10 + B2 10 + B3 10 + B4 10 + B5 10 + B6 10 + C1 18 + D1 20 = 108）。**
