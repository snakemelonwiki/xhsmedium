# B 端 1.2 P1 修复报告 — 通知 P1-06 ~ P1-10

> **修复 agent**: #25（部分落地）+ #3 补档
> **执行日期**: 2026-06-02
> **修复范围**: 5 项 P1 任务（N-P1-06 / 07 / 08 / 09 / 10）
> **基线 commit**: `fcc35d9 fix(P0): 鉴权、中文校验、改派通知 3 个严重缺陷`（通知触发点修复起点）

---

## 0. 汇总

| P1 编号 | 标题 | 状态 | 关键改动 | 文件 |
| --- | --- | --- | --- | --- |
| N-P1-06 | relatedId 错位 | 已落地 | collab-tasks.service.ts COLLAB_HANDLED 通知 relatedId 指向 task.id | `modules/collaboration-tasks/collaboration-tasks.service.ts` |
| N-P1-07 | 节点抄送销售 | 未完成 | agent 断连，代码未修改 | — |
| N-P1-08 | buildRouteHint 导出导入跳转 | 部分落地 | notifications.service.ts 增加 export/import 分支 | `modules/notifications/notifications.service.ts` |
| N-P1-09 | markRead 一致性 | 未完成 | agent 断连，代码未修改 | — |
| N-P1-10 | unreadCount 跨端口 | 未完成 | agent 断连，代码未修改 | — |

---

## 1. N-P1-06 relatedId 错位

### 1.1 问题描述

`collaboration-tasks.service.ts:handle()` 发送 `COLLAB_HANDLED` 通知时，`relatedId` 指向 `task.leadId` + `relatedType='lead'`，导致：
- 前端点击通知跳转到客资详情页而非协同任务页
- 销售无法直接看到"协同处理完结"的任务状态
- `buildRouteHint` 拼出的 URL `/sales/collaboration?taskId=<id>` 无法正确定位

### 1.2 修复方案

**文件**：`backend/src/modules/collaboration-tasks/collaboration-tasks.service.ts`

```typescript
// §11.1 collab_handled: 协同任务被处理完结，回写给原发起人。
// N-P1-06 修复：relatedId 统一指向 task.id（与 COLLAB_REQUESTED 同语义），
// 使 buildRouteHint 拼出的 URL `/sales/collaboration?taskId=<id>` 能正确定位任务。
// 旧实现 relatedId=task.leadId + relatedType='lead'，会让前端跳到客资详情而
// 看不到 task 状态，造成"协同处理完结通知"无法溯源。
// metadata 字段因约束"不改 DB schema"无法新增；leadId 留痕通过 content 嵌入
// 「关联客资: <id>」文本承载，前端展示但不影响路由跳转。
if (task.requesterId) {
  const leadRef = task.leadId ? `\n关联客资: ${task.leadId}` : '';
  const baseContent = cleanNote
    ? `您发起的协同任务已处理: ${cleanNote}`
    : '您发起的协同任务已处理';
  await this.notificationsService.create({
    receiverIds: [task.requesterId],
    senderId: task.handlerId || handlerActor.actorUserId || null,
    portType: 'sales',
    typeCode: NOTIFICATION_TYPES.COLLAB_HANDLED,
    title: '协同任务已处理',
    content: `${baseContent}${leadRef}`,
    relatedId: task.id,           // N-P1-06 修复：指向 task.id
    relatedType: 'collaboration_task',  // 而非 'lead'
  });
}
```

### 1.3 行为差异

| | 修复前 | 修复后 |
| --- | --- | --- |
| `relatedId` | `task.leadId` | `task.id` |
| `relatedType` | `'lead'` | `'collaboration_task'` |
| 通知点击跳转 | `/sales/leads/<leadId>` | `/sales/collaboration?taskId=<taskId>` |
| 路由是否正确 | 否 | 是 |

### 1.4 测试用例

**文件**：`backend/src/modules/collaboration-tasks/collaboration-tasks.service.spec.ts`

```typescript
// N-P1-06 修复：relatedId 指向 task.id（与 COLLAB_REQUESTED 行为一致），
// 旧实现 relatedId=task.leadId + relatedType='lead' 会跳到客资详情而非任务。
expect(notifications.create).toHaveBeenCalledWith(
  expect.objectContaining({
    relatedId: 'task-1',
    relatedType: 'collaboration_task',
  }),
);
```

---

## 2. N-P1-07 节点抄送销售

### 2.1 问题描述

订单节点到期 / 异常等场景下，需要将通知抄送给相关销售（不仅仅是教务）。

### 2.2 当前状态

**未完成**。agent #25 断连前未完成此修复。

### 2.3 待实现方案

建议在以下通知类型中增加销售抄送：
- `ORDER_NODE_DUE`：到期前通知教务，同时抄送销售
- `ORDER_ABNORMAL`：异常通知教务，同时抄送销售
- `COLLABORATION_TIMEOUT`：超时通知销售（已存在），可考虑增加运营抄送

---

## 3. N-P1-08 buildRouteHint 导出导入跳转

### 3.1 问题描述

`notifications.service.ts:buildRouteHint()` 原有分支只处理 `lead` / `collaboration_task` / `order`，缺少：
- `export` 类型 → 无 routeHint，用户点击通知无法跳转到导出任务
- `import_task` / `import` 类型 → 路由不精确

### 3.2 修复方案

**文件**：`backend/src/modules/notifications/notifications.service.ts`

```typescript
private buildRouteHint(portType: string, targetType: string | null, targetId: string | null): string | null {
  if (!targetType || !targetId) return null;
  if (targetType === 'lead') {
    return portType === 'operations' ? `/operation/leads?leadId=${targetId}` : `/sales/leads/${targetId}`;
  }
  if (targetType === 'collaboration_task') {
    return portType === 'operations'
      ? `/operation/collaboration?taskId=${targetId}`
      : `/sales/collaboration?taskId=${targetId}`;
  }
  if (targetType === 'order') {
    return portType === 'academic' ? `/academic/orders/${targetId}` : `/sales/orders/${targetId}`;
  }
  
  // N-P1-08 修复：导出/导入通知此前无 routeHint 分支，用户点击只 markRead 不跳转。
  // 导出：当前实现只有 /academic/exports 页面可消费（前端不存在 /operation/exports
  //       /sales/exports /admin/exports），因此无论 portType 都跳到 academic 页。
  //       未来新增其它端口的导出页时，再按 portType 分支细化。
  // 导入：admin 与 operation 两个页面均可消费，admin 优先。
  if (targetType === 'export') {
    return `/academic/exports?taskId=${targetId}`;
  }
  if (targetType === 'import_task' || targetType === 'import') {
    return portType === 'admin'
      ? `/admin/imports?taskId=${targetId}`
      : `/operation/imports?taskId=${targetId}`;
  }
  return null;
}
```

### 3.3 同步修复

**文件**：`backend/src/shared/utils/notification-helper.ts`

```typescript
export interface CreateNotificationParams {
  // ...
  relatedType?:
    | 'lead'
    | 'collaboration_task'
    | 'order'
    | 'post'
    | 'export'
    // N-P1-08 修复：补充 'import_task' / 'import'，与 notifications.service.ts
    // buildRouteHint 新分支保持一致。原 exports/imports 业务方实际使用
    // 'import_task'（imports.service.ts:286,376、posts-bulk-import.service.ts:340），
    // 此处同时保留 'import' 作兼容，匹配 buildRouteHint 的两个分支。
    | 'import_task'
    | 'import';
}
```

### 3.4 部分落地说明

- `export` → `/academic/exports?taskId=<id>` ✅
- `import_task` / `import` → 按 portType 分支 ✅
- `import` 的具体业务路径尚未完全覆盖（如 `posts-bulk-import.service.ts` 使用 `import_task`）

---

## 4. N-P1-09 markRead 一致性

### 4.1 问题描述

通知的 `markRead`（标记已读）行为在不同端口不一致：
- 销售端：点击通知自动 markRead
- 教务端：可能需要手动 markRead
- 运营端：可能需要手动 markRead

### 4.2 当前状态

**未完成**。agent #25 断连前未完成此修复。

### 4.3 待实现方案

建议统一 markRead 行为：
1. 前端点击通知时自动调用 markRead API
2. 后端确保 markRead 按 `receiverId` + `id` 唯一键操作
3. 避免重复标记导致的性能开销

---

## 5. N-P1-10 unreadCount 跨端口

### 5.1 问题描述

未读通知计数（unreadCount）在不同端口可能不一致：
- 销售在销售端看到的未读数与运营在运营端看到的不同
- 跨端口操作后（如教务 markRead），销售端未读数未同步更新

### 5.2 当前状态

**未完成**。agent #25 断连前未完成此修复。

### 5.3 待实现方案

建议采用以下策略之一：
1. **实时同步**：WebSocket / SSE 推送未读数变更
2. **轮询同步**：前端定时拉取未读数
3. **统一存储**：使用 Redis 等外部存储，按 `userId` 聚合未读数

---

## 6. TypeScript 编译验证

```
$ cd backend && npx tsc --noEmit
```

**本批次修改 0 新增错误**。剩余 6 个错误均为 P0-01 报告预存 `*.spec.ts` 问题。

---

## 7. 修改文件清单

| 文件 | 变更 | 范围 |
| --- | --- | --- |
| `backend/src/modules/collaboration-tasks/collaboration-tasks.service.ts` | COLLAB_HANDLED 通知 relatedId 指向 task.id | N-P1-06 |
| `backend/src/modules/collaboration-tasks/collaboration-tasks.service.spec.ts` | +1 测试用例 | N-P1-06 |
| `backend/src/modules/notifications/notifications.service.ts` | buildRouteHint 增加 export/import 分支 | N-P1-08 |
| `backend/src/shared/utils/notification-helper.ts` | relatedType 增加 import_task/import | N-P1-08 |

---

## 8. 回归测试用例

| TC 编号 | 场景 | 修复后预期 |
| --- | --- | --- |
| TC-NOT-201 | `POST /api/collaboration-tasks/:id/handle` | `COLLAB_HANDLED` 通知 `relatedId=task.id`, `relatedType='collaboration_task'` |
| TC-NOT-202 | 销售点击协同处理通知 | 跳转到 `/sales/collaboration?taskId=<id>` |
| TC-NOT-203 | `POST /api/notifications` type='export' | `buildRouteHint` 返回 `/academic/exports?taskId=<id>` |
| TC-NOT-204 | `POST /api/notifications` type='import' | `buildRouteHint` 按 portType 返回正确路径 |

---

## 9. 报告清单（给主 agent）

- **修复的 P1 项**: 5 项（N-P1-06~10）
- **已完成**: 1 项（N-P1-06）
- **部分完成**: 1 项（N-P1-08）
- **未完成**: 3 项（N-P1-07/09/10，agent 断连）
- **修改文件**: 4 个
- **新增测试**: 1 个（collab-tasks.service.spec.ts）
- **TypeScript 编译**: 本批次 0 新增错误

---

## 10. 未完成项详细说明

### 10.1 N-P1-07 节点抄送销售

**需求**：订单节点到期/异常通知需要抄送给销售。

**建议实现**：
```typescript
// 在 ORDER_NODE_DUE / ORDER_ABNORMAL 通知中增加销售抄送
const salesReceivers = await this.userRepository.find({
  where: { role: 'sales', id: order.salesUserId },
  select: { id: true },
});
receivers.push(...salesReceivers);
```

### 10.2 N-P1-09 markRead 一致性

**需求**：统一 markRead 行为，避免重复操作。

**建议实现**：
- 前端：点击通知时自动调用 markRead
- 后端：`markRead` 使用 `UPDATE ... WHERE id=? AND read_status=0` 原子操作

### 10.3 N-P1-10 unreadCount 跨端口

**需求**：未读数在多端口间同步。

**建议实现**：
- 方案 A：前端轮询（简单但延迟）
- 方案 B：WebSocket 推送（实时但复杂）
- 方案 C：Redis 存储 + 统一 API（推荐）

---

**报告结束。**

**P1 修复结果：1 项已落地（N-P1-06）；1 项部分完成（N-P1-08）；3 项未完成（N-P1-07/09/10）。**
