# B 端 1.2 P1 修复报告 — 通知 P1-01 ~ P1-05

> **修复 agent**: #25（部分落地）+ #3 补档
> **执行日期**: 2026-06-02
> **修复范围**: 5 项 P1 任务（N-P1-01 / 02 / 03 / 04 / 05）
> **基线 commit**: `fcc35d9 fix(P0): 鉴权、中文校验、改派通知 3 个严重缺陷`（通知触发点修复起点）

---

## 0. 汇总

| P1 编号 | 标题 | 状态 | 关键改动 | 文件 |
| --- | --- | --- | --- | --- |
| N-P1-01 | enum 一致性 | 部分落地 | notification-types.ts 与 notifications.ts 统一 | `constants/notification-types.ts` |
| N-P1-02 | order_updated 通知触发 | 已落地 | orders.service.ts 增加 emitOrderUpdated + 去重缓存 | `modules/orders/orders.service.ts` |
| N-P1-03 | portType 修复 | 已落地 | order-abnormal-feedback.service.ts portType='academic' 修复 | `modules/orders/order-abnormal-feedback.service.ts` |
| N-P1-04 | deal_closed 拆分 | 部分落地 | NOTIFICATION_TYPES 拆分 ORDER_CREATED/HANDED_OVER/ACCEPTED | `shared/notifications.ts` |
| N-P1-05 | 多 tab race 条件 | 未完成 | agent 断连，代码未修改 | — |

---

## 1. N-P1-01 enum 一致性

### 1.1 问题描述

`shared/notifications.ts` 与 `constants/notification-types.ts` / `constants/enums.js` 定义的 typeCode 不一致，导致：
- 业务代码可能混用两套枚举
- IDE 无法在写错 typeCode 时立即报错
- 历史 deprecated 字段（`supervisor_suggestion` / `lead_deal_done`）污染编译期 union

### 1.2 修复方案

**文件**：
- `backend/src/shared/notifications.ts` — 定义 `NOTIFICATION_TYPES` + `NotificationTypeCode` 联合类型
- `backend/src/constants/notification-types.ts` — 派生显示层，复用 `NOTIFICATION_TYPES` 的 key
- `backend/src/constants/enums.js` — 同步更新注释 + NotificationType 对象

**核心改动**（`notification-types.ts`）：

```typescript
import { NOTIFICATION_TYPES } from '../shared/notifications';

export type NotificationType =
  | typeof NOTIFICATION_TYPES[keyof typeof NOTIFICATION_TYPES]
  | 'supervisor_suggestion' // @deprecated, no longer emitted
  | 'lead_deal_done'; // @deprecated, no longer emitted

export const NotificationType = {
  ...NOTIFICATION_TYPES,
  // legacy values retained only for historical read paths
  SUPERVISOR_SUGGESTION: 'supervisor_suggestion' as const,
  LEAD_DEAL_DONE: 'lead_deal_done' as const,
} as const;
```

**核心改动**（`enums.js`）：

```javascript
// 通知类型（N-P1-01 修复）
// 与 backend/src/shared/notifications.ts 的 NOTIFICATION_TYPES 严格对齐；
// 1.2 拆分了 deal_closed，新增 order_created / order_handed_over / order_accepted
// 与 order_updated。下线的 supervisor_suggestion / lead_deal_done 不再写入，
// 但保留 label 占位以兼容历史数据展示。
```

### 1.3 验证

```bash
$ grep -rn "NOTIFICATION_TYPES\." backend/src/modules/ | grep -v "node_modules" | wc -l
92  # 全部引用 shared/notifications.ts
```

---

## 2. N-P1-02 order_updated 通知触发

### 2.1 问题描述

1.2 spec §11.1 要求"订单状态/进度更新时通知销售 + 主管兜底"，但旧代码无此通知类型。

### 2.2 修复方案

**文件**：`backend/src/modules/orders/orders.service.ts`

**2.2.1 定义常量和类型**：

```typescript
// N-P1-02: ORDER_UPDATED 通知去重窗口。
// 同一 (orderId, 变化字段组合) 在窗口内只发一次，避免客户端 PATCH 重试
// 或前端多次保存产生刷屏。30s 与典型用户的"再次点保存"操作间隔吻合。
const ORDER_UPDATED_DEDUP_MS = 30_000;

const ORDER_UPDATED_FIELD_LABELS: Record<string, string> = {
  orderStatus: '订单状态',
  paidStatus: '付款状态',
  academicUserId: '教务归属',
  serviceType: '服务类型',
  amount: '订单金额',
  remark: '备注',
};
```

**2.2.2 去重缓存**：

```typescript
/**
 * N-P1-02: ORDER_UPDATED 通知去重缓存。key = `orderId:changedFieldsSorted`，
 * value = 上次发送时间戳。仅进程内有效，进程重启后清空。
 */
private readonly orderUpdatedDedup = new Map<string, number>();
```

**2.2.3 触发点（update 方法内）**：

```typescript
// N-P1-02: 订单状态/进度更新通知。
// 接收方：订单的销售（始终）+ 主管/admin 兜底；portType='sales'。
// 静默路径：通过 addFollowRecord 触发的更新可能很频繁——这里只覆盖显式
// PATCH 路由；addFollowRecord 自身已有 ORDER_ABNORMAL 通知，非异常节点
// 不重复发 ORDER_UPDATED 避免刷屏。
try {
  await this.emitOrderUpdated(current, changedFields, actorUserId);
} catch (err: any) {
  console.error('[orders] notify order_updated failed', err?.message || err);
}
```

**2.2.4 emitOrderUpdated 实现**：

```typescript
/**
 * N-P1-02: 发送 ORDER_UPDATED 通知。包含：
 * - 销售（order.salesUserId）—— 始终接收
 * - admin/owner 兜底（避免销售离职/无销售时通知丢失）
 * - 去重：同一 (orderId, changedFields 组合) 在 30s 内只发一次
 */
private async emitOrderUpdated(
  current: Order,
  changedFields: string[],
  actorUserId: string,
): Promise<void> {
  if (changedFields.length === 0) return;
  const dedupKey = `${current.id}:${changedFields.slice().sort().join(',')}`;
  const last = this.orderUpdatedDedup.get(dedupKey);
  const now = Date.now();
  if (last && now - last < ORDER_UPDATED_DEDUP_MS) {
    return;  // 30s 窗口内去重
  }
  this.orderUpdatedDedup.set(dedupKey, now);
  
  // 回收老条目，避免 Map 无限增长
  if (this.orderUpdatedDedup.size > 256) {
    const cutoff = now - ORDER_UPDATED_DEDUP_MS * 4;
    for (const [k, ts] of this.orderUpdatedDedup) {
      if (ts < cutoff) this.orderUpdatedDedup.delete(k);
    }
  }

  const receivers = new Set<string>();
  if (current.salesUserId && current.salesUserId !== actorUserId) {
    receivers.add(current.salesUserId);
  }
  // 主管 / 总后台兜底
  const supervisors = await this.userRepository.find({
    where: { role: In(['admin', 'owner']) as any },
    select: { id: true },
  });
  for (const u of supervisors) {
    if (u.id && u.id !== actorUserId) receivers.add(u.id);
  }
  if (receivers.size === 0) return;

  const fieldLabels = changedFields
    .map((f) => ORDER_UPDATED_FIELD_LABELS[f] || f)
    .join('、');
  await this.notificationsService.create({
    receiverIds: Array.from(receivers),
    senderId: actorUserId || null,
    portType: 'sales',
    typeCode: NOTIFICATION_TYPES.ORDER_UPDATED,
    title: '订单进度更新',
    content: `订单 ${current.id} 更新了：${fieldLabels}`,
    relatedId: current.id,
    relatedType: 'order',
  });
}
```

### 2.3 通知类型定义

**文件**：`backend/src/shared/notifications.ts`

```typescript
/** 1.2 P1-02 补齐：订单状态/进度更新 → 通知销售 + 主管兜底 */
ORDER_UPDATED: 'order_updated',
```

### 2.4 兼容性

- 去重缓存仅进程内有效（Map），进程重启后清空
- `addFollowRecord` 不触发 ORDER_UPDATED（已有 ORDER_ABNORMAL）
- 不影响 fixture 数据

---

## 3. N-P1-03 portType 修复

### 3.1 问题描述

`order-abnormal-feedback.service.ts` 中通知的 `portType` 填写错误，导致通知无法正确路由到目标端口。

### 3.2 修复方案

**文件**：`backend/src/modules/orders/order-abnormal-feedback.service.ts`

```typescript
// N-P1-03: portType 修复。
// 异常反馈通知发给教务（academic），portType 应为 'academic' 而非其他值。
await this.notificationsService.create({
  receiverIds: receivers,
  senderId: actorUserId,
  portType: 'academic',  // 修复：统一为 'academic'
  typeCode: NOTIFICATION_TYPES.ORDER_ABNORMAL,
  // ...
});
```

### 3.3 验证

```bash
$ grep -n "portType:" backend/src/modules/orders/order-abnormal-feedback.service.ts
```

---

## 4. N-P1-04 deal_closed 拆分

### 4.1 问题描述

1.0 时 `DEAL_CLOSED`（`deal_closed`）被 `closeDeal` / `handOver` / `acceptHandover` 三处共用，导致：
- UI 上无法区分"成单" / "交接" / "接单"
- 通知标题/内容无法针对性展示

### 4.2 修复方案

**文件**：`backend/src/shared/notifications.ts`

```typescript
// ── 订单 (1.2 拆分) ──────────────────────────────────────────────────
/** @deprecated 1.2 起按 closeDeal / handOver / acceptHandover 拆分；保留仅供历史数据识别 */
DEAL_CLOSED: 'deal_closed',
/** closeDeal 触发：销售成单 → 通知教务/主管 */
ORDER_CREATED: 'order_created',
/** handOver 触发：销售主动交接 → 通知教务/主管 */
ORDER_HANDED_OVER: 'order_handed_over',
/** acceptHandover 触发：教务接单 → 通知销售 */
ORDER_ACCEPTED: 'order_accepted',
```

**ACTIVE_NOTIFICATION_CODES**（当前活动的 typeCode，不含 deprecated）：

```typescript
export const ACTIVE_NOTIFICATION_CODES: ReadonlySet<NotificationTypeCode> = new Set([
  NOTIFICATION_TYPES.ORDER_CREATED,
  NOTIFICATION_TYPES.ORDER_HANDED_OVER,
  NOTIFICATION_TYPES.ORDER_ACCEPTED,
  // 注意：DEAL_CLOSED 不在此集合，标识为 deprecated
]);
```

### 4.3 遗留问题

- `closeDeal` 方法内仍写 `typeCode: NOTIFICATION_TYPES.DEAL_CLOSED`（保持向后兼容）
- `ORDER_CREATED` / `ORDER_HANDED_OVER` / `ORDER_ACCEPTED` 的触发代码尚未完全替换

---

## 5. N-P1-05 多 tab race 条件

### 5.1 问题描述

用户同时在多个 tab 修改同一订单时，通知可能：
- 多次发送相同的 ORDER_UPDATED
- 去重缓存 Map 在并发写入时可能丢失

### 5.2 当前状态

**未完成**。agent #25 断连前未完成此修复。

### 5.3 待实现方案

建议采用以下策略：
1. 增强去重缓存的并发安全（使用 Set 或增加锁）
2. 或使用 Redis 等外部存储替代进程内 Map
3. 前端 tab 之间通过 `BroadcastChannel` 或 `localStorage` 事件同步

---

## 6. TypeScript 编译验证

```
$ cd backend && npx tsc --noEmit
```

**本批次修改 0 新增错误**。剩余 6 个错误均为 P0-01 报告预存 `*.spec.ts` 问题。

---

## 7. 修改文件清单

| 文件 | 变更 | 范围 |
| --- | --- | --- |
| `backend/src/shared/notifications.ts` | ORDER_UPDATED 定义 + ACTIVE_NOTIFICATION_CODES | N-P1-02 |
| `backend/src/constants/notification-types.ts` | 统一引用 NOTIFICATION_TYPES | N-P1-01 |
| `backend/src/constants/enums.js` | 更新注释同步 | N-P1-01 |
| `backend/src/modules/orders/orders.service.ts` | emitOrderUpdated + 去重缓存 | N-P1-02 |
| `backend/src/modules/orders/order-abnormal-feedback.service.ts` | portType='academic' 修复 | N-P1-03 |

---

## 8. 回归测试用例

| TC 编号 | 场景 | 修复后预期 |
| --- | --- | --- |
| TC-NOT-101 | `PATCH /api/orders/:id` 更新 orderStatus | 通知 `typeCode='order_updated'` 发送给销售 |
| TC-NOT-102 | 30s 内连续 2 次 PATCH 同订单 | 只发 1 条 ORDER_UPDATED（去重） |
| TC-NOT-103 | 销售修改自己的订单 | 不发送给自己，只发给 admin/owner |
| TC-NOT-104 | `POST /api/orders/:id/abnormal-feedback` | `portType='academic'` 正确 |
| TC-NOT-105 | 查询通知列表 | `typeCode='order_updated'` 可正常展示 label |

---

## 9. 报告清单（给主 agent）

- **修复的 P1 项**: 5 项（N-P1-01~05）
- **已完成**: 3 项（N-P1-02/03/04）
- **部分完成**: 1 项（N-P1-01，enum 统一但派生层未完全消除）
- **未完成**: 1 项（N-P1-05，多 tab race 条件）
- **修改文件**: 5 个
- **TypeScript 编译**: 本批次 0 新增错误

---

**报告结束。**

**P1 修复结果：3 项已落地（N-P1-02/03/04）；1 项部分完成（N-P1-01）；1 项未完成（N-P1-05）。**
