# B 端 1.2 验收问题跟踪

> 创建日期：2026-06-01
> 依据：`doc/开发里程碑-1.2-完整交付版.md` 第二节范围
> 验收方式：分 4 个 agent 并行验收四端口（运营/销售/教务/主管），每个 agent 做接口探测 + 简单页面元素验证
> 服务地址：后端 http://localhost:8089 ，前端 http://localhost:3302
> 测试账号（均为密码 `test123`）：staff `youlunrong`、sales `sales01`、academic `academic02`、admin `youlun`

---

## 验收结论速览

| 端 | 模块数 | 通过 | 有问题 | 待补充 |
| --- | --- | --- | --- | --- |
| 运营端 | 10（+协同/消息/导入/稳定性 4 项附加） | 4 | 10 | 0 |
| 销售端 | 6（+被动添加+稳定性） | 1 | 7 | 0 |
| 教务端 | 6（+稳定性） | 0 | 7 | 0 |
| 主管端 | 8（+协同/导入/订单/稳定性 4 项附加） | 2 | 10 | 0 |
| **合计** | 30 + 12 附加 | 7 | 34 | 0 |

> P0 总数：6（4 端均有"后端崩溃 + import-tasks 500/exports 越权 + 订单池过滤"等阻断项）
> P1 总数：13；P2/P3 总数：15

### 重点 P0（修了它们其余才好继续验）

1. **后端反复崩溃无自愈** — 【🟡 部分修复 2026-06-01】`backend/src/main.ts` 已加 `process.on('unhandledRejection'/'uncaughtException')` 兜底，未捕获异常仅 log 不退进程；崩溃源头 #2 修复后大概率不再触发，仍需长跑回归。
2. **`GET /api/import-tasks` 全部 500** — 【✅ 已修复 2026-06-01】`entities/import-task.entity.ts` `@CreateDateColumn` 由 `create_time` 改为 `created_at`，schema.sql 列名同步；admin 调 `GET /api/import-tasks?limit=5` 实测 200。
3. **`POST /api/exports` 越权** — 【✅ 已修复 2026-06-01】`exports.controller.ts` 加 `ROLE_EXPORT_WHITELIST`（academic 仅 orders、sales 仅 leads/orders/collab、staff 仅 leads/posts/rankings/collab）；强制覆盖客户端传的 role/currentUserId/scope；`exports.service.ts` orders SQL 按角色 + scope 注入 where；`findOne` 加创建者归属校验。academic 导 leads → 403、导 orders → 200，sales 导 posts → 403。
4. **教务订单池永远为空** — 【✅ 已修复 2026-06-01】`orders.service.ts` 抽出 `applyOrdersScope`：academic 默认看池单+自己已认领；新增 `scope=pool/assigned/mine` 三档；list/listPaged/exports 共用同一口径。`orders.controller.findOne` 同步加角色归属校验：他人订单 → 404、池单详情 → 200。
5. **节点提醒未实现** — 【✅ 已修复 2026-06-01】① `app.module.ts` 接入 `ScheduleModule.forRoot()`；② 新增 `backend/src/modules/orders/reminders.service.ts`：`@Cron(EVERY_MINUTE)` 扫 `next_remind_at <= NOW AND reminder_sent_at IS NULL`，给跟进人 + 订单当前教务发 `ORDER_NODE_DUE` 通知后写回 `reminder_sent_at` 保证幂等；③ 新增字段 `order_follow_records.reminder_sent_at`（M11 迁移，含 idx_order_follow_remind 复合索引，schema.sql 同步）；④ 控制器加 `GET /api/orders/reminders/pending`（教务"节点提醒"页消费）+ `POST /api/orders/reminders/scan`（admin 手动 trigger）；⑤ 前端 `frontend/src/app/academic/reminders/page.tsx` 节点提醒页 + 菜单"节点提醒"入口。冒烟实测：过期记录 scan=1 sent=1，二次 scan=0 验证幂等，listPending 返回 isOverdue 红黄分级。
6. **作品/客资导入模板下载 404** — 【✅ 已修复 2026-06-01】把模板路由从 ImportsController 挪到 LeadsController / PostsController，放在 `@Get(':id')` 之前。`/api/leads/import-template.xlsx`、`/api/posts/import-template.xlsx` 两个端点实测 200。

---

## 一、运营端（B 端验收）

> 范围：今日任务、运营排行榜、学习榜单、个人看板、作品录入、客资录入、客资看板、我的作品、作品广场、账号管理
> 验收人：B端验收 agent #1（2026-06-01）；账号 `youlunrong / test123`（staff）
> 验收方式：curl 后端 API + curl 前端页面 + 前后端源码核对

| # | 模块 | 接口 | 页面 | 问题摘要 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 今日任务 | 无独立接口 | `/operation/dashboard` HTTP 200 | 1.2 文档要求"今日任务"为独立模块，但运营端菜单（`frontend/src/shared/layout/menu.tsx`）及 `app/operation/` 下无 today-task 页面/路由，仅以 dashboard 内"今日作品/客资/成交"卡片体现；后端无 today-tasks 接口；期望：补充独立"今日任务"列表（含待处理协同/线索/作品提醒）或在文档中收敛范围 | P1 | 待修复 |
| 2 | 运营排行榜 | `GET /api/rankings` HTTP 200，返回 22 员工聚合数据 | `/operation/rankings` HTTP 200 | 接口与页面通过；但所有员工 `todayPosts/todayLeads/todayTraffic/todayDeals` 均为 0（仅 `accountCount/postCount` 有值），需确认是否数据原因或日期过滤逻辑错误（`dashboard.service.ts` 用 `publishedAt = today`，注意 publishedAt 是 DATE 格式与时区） | P2 | 待修复 |
| 3 | 学习榜单 | `GET /api/rankings/learning-posts` HTTP 200 返回 `[]` | 复用 rankings 页面 | 接口通了但返回空数组；`rankings.service.ts:getLearningPosts(days=7)` 默认查近7天，当前最新作品 publishedAt 为 2026-04-29（已超 7 天），导致前端永远看不到学习作品；期望：默认 days 拉长或文档明确仅按 publishedAt 近 N 天 | P2 | 待修复 |
| 4 | 个人看板 | `GET /api/dashboard/summary` HTTP 200；`GET /api/dashboard/post-type-distribution` HTTP 200 | `/operation/dashboard` HTTP 200 | summary 全部计数为 0（updatedEmployees/xhsPosts/douyinPosts/todayLeads=0），但 `/api/posts?limit=2` 显示个人确有 73 条作品；判断 dashboard 按 today 日期过滤导致全 0，日期默认为今天 (2026-06-01) 而样本数据 publishedAt 在 4 月，看板可读性差；期望：增加近 7/30 天切换或合理空态文案 | P2 | 待修复 |
| 5 | 作品录入 | `POST /api/posts`（代码存在，未压测）；`GET /api/posts?limit=2` HTTP 200 | `/operation/posts/new` HTTP 200 | 单条录入接口存在；批量导入模板下载 `GET /api/posts/import-template.xlsx` 被 `posts/:id` 路由抢占，返回 `{"message":"作品不存在"}` HTTP 404（路由顺序冲突，应将 `import-template.xlsx` 路由提前注册或迁入 posts.controller） | P1 | 待修复 |
| 6 | 客资录入 | `POST /api/leads`（存在）；草稿 `GET /api/lead-drafts` HTTP 200 返回 3 条；`POST /api/leads/passive/bind` 等被动客资接口存在 | `/operation/leads/new` HTTP 200 | 接口与页面通过；导入模板 `GET /api/leads/import-template.xlsx` 同样被 `leads/:id` 抢占返回 `{ok:false,message:'not found'}` HTTP 404，与作品录入同一根因 | P1 | 待修复 |
| 7 | 客资看板 | `GET /api/leads?limit=2` HTTP 200 返回空；`GET /api/leads/stats` HTTP 200；`GET /api/leads/tomorrow-followups` HTTP 200 返回 `[]` | `/operation/leads` HTTP 200 | 接口结构完整；当前 staff `youlunrong` 自有客资为 0（scope=self），需主管端确认是否数据问题。功能层面通过 | P3 | 已通过 |
| 8 | 我的作品 | `GET /api/posts?limit=2` HTTP 200，total=73 | `/operation/posts` HTTP 200 | 接口与页面均通过；返回数据中 `traffic/likes/comments/favorites` 全为 0，且 `metricsUpdatedAt: null`——指标抓取链路未跑通或样本数据未刷新，影响"我的作品"展示价值 | P2 | 待修复 |
| 9 | 作品广场 | `GET /api/posts/plaza?limit=2` HTTP 200，返回 5 条优秀作品（`view=excellent`） | `/operation/gallery` HTTP 200 | 接口与页面均通过；同样所有 traffic/likes=0，`isFavorited=false`，依赖指标抓取与收藏链路；功能可用，数据不丰富 | P3 | 已通过 |
| 10 | 账号管理 | `GET /api/accounts?limit=2` HTTP 200，total=178 | 运营端无入口 | 后端接口齐全（增删改查 + posting-plan），但 `frontend/src/shared/layout/menu.tsx` 中"账号管理"仅出现在 admin 菜单（`/admin/accounts`），运营端无菜单项也无 `app/operation/accounts/` 页面；1.2 文档第 48 行将"账号管理"列入运营端范围；期望：补建 `/operation/accounts` 页面并加入 operation 菜单（或与主管端共用） | P1 | 待修复 |
| 11 | 协同申请（关联） | `GET /api/collaboration-tasks` HTTP 200 total=10 | `/operation/collaboration` HTTP 200 | 接口/页面均通过 | P3 | 已通过 |
| 12 | 消息中心（关联） | `GET /api/notifications` HTTP 200 unreadCount=3；`GET /api/notifications/unread-count` HTTP 200 | `/operation/messages` HTTP 200 | 接口/页面均通过 | P3 | 已通过 |
| 13 | 导入任务列表 | `GET /api/import-tasks?limit=5&offset=0` 及 `GET /api/import-tasks` 均 HTTP 500 `Internal server error` | `/operation/imports` HTTP 200 | 接口稳定 500，影响导入历史与失败回看；`imports.controller.ts:99` `actorUserId = session?.userId || session?.id || 'anonymous'`，session 中字段可能为 `sub`（JWT 标准）导致下游 SQL 异常；期望：与 notifications 一致兼容 `user?.sub`，并加 try/catch + 详细日志 | P0 | 待修复 |
| 14 | 服务稳定性 | 验收过程中 `localhost:8089` 在测试到第 14 个接口（favorites/operation-logs/employees/exports 顺序请求）时整体宕机（netstat 无 LISTENING），后续所有接口连接被拒（HTTP 000），未自恢复 | - | 后端进程崩溃；疑似无未捕获异常处理或并发资源问题；P0 阻断后续验收，且生产环境极易触发；期望：排查崩溃栈并加进程守护（PM2 已配置但 dev 模式未生效） | P0 | 待修复 |

---

## 二、销售端（B 端验收）

> 范围：我的客资、待跟进、客资详情、协同申请、订单跟进、消息中心
> 验收账号：sales01 / test123（user-sales-1）
> 验收时间：2026-06-01
> 验收方式：登录获取 JWT 后逐一调核心 GET 接口（部分 POST 验证因后端运行中崩溃未完成）+ 前端页面 HTML 200 校验 + 关键源码走查

| # | 模块 | 接口 | 页面 | 问题摘要 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 我的客资 | `GET /api/leads`、`GET /api/leads/stats` 均 200，分页/筛选可用 | `/sales/leads` 200，使用 `LeadCard` 渲染、含状态/添加状态筛选 | 数据层有脏数据：被动添加客资 `fd8525cd-f7c5-48d5-a158-dcd6a4cb38c7` 的 `nickname`/`note` 含 `�` 替换字符（GBK→UTF-8 误转），前端显示为乱码；怀疑被动新建接口在某历史调用中字符集错误 | P2 | 待修复 |
| 2 | 待跟进 / 明日待跟进 | `GET /api/leads/tomorrow-followups` 200（空数组）；`GET /api/leads?needFollowup=true` 200 | `/sales/leads/followups` 200，含全部/新分配/未通过/协同后/到期 5 队列 Segmented | followups 页面前端一次性并发 5 个 `listSalesLeads` + `listTomorrowFollowups` 调用做客户端聚合（page.tsx L37-44），数据量大时性能差且分页失真；建议后端提供聚合接口或队列参数 | P3 | 待修复 |
| 3 | 客资详情 | `GET /api/leads/:id` 200；`GET /api/leads/:id/follow-records` 200（返回 4 条历史跟进） | `/sales/leads/[id]/page.tsx` 存在，HTTP 200 | 通过 | P3 | 已通过 |
| 4 | 协同申请 | `GET /api/collaboration-tasks?scope=mine` 200（5 条，正确）；但**默认无 scope 与 `scope=outgoing`/`scope=incoming` 均返回全表 10 条**，含 `user-test-sales-01`/`user-test-collab-fix` 等他人记录 | `/sales/collaboration` 200，前端使用 `scope:'mine'` 故页面侧暂未泄露 | 后端 `collaboration-tasks.service.ts` `normalizeScope` 仅认 `mine/inbox/all/requester/handler/sales/operations`；`outgoing/incoming` 落到 `all` 不做用户过滤，越权可读全部协同任务。任何调用方传错 scope 即数据泄露 | P1 | 待修复 |
| 5 | 订单跟进 | `GET /api/orders` 200（1 单）；`GET /api/orders/:id` 200；`GET /api/orders/:id/follow-records` 200 | `/sales/orders` 200，复用 `academic/orders/OrderTable`（scope=sales, actionMode=sales） | 标记成交 `POST /api/leads/:id/close-deal` 因后端中途崩溃未做端到端验证；从 controller 看路由存在且鉴权 OK | P2 | 待修复 |
| 6 | 消息中心 | `GET /api/notifications` 200（3 条，含 collab_handled / export_done）；`GET /api/notifications/unread-count` 200 (unreadCount:0) | `/sales/messages` 200 | `POST /api/notifications/:id/read` 因后端崩溃未验，code review 路由与 service 存在；建议补端到端 | P3 | 待修复 |
| 7 | 被动添加客资 | `GET /api/leads/passive/candidates?nickname=最终` 200，返回 1 条带 `score:35` 评分（URL 需正确 encode 中文） | `/sales/passive-leads` 200，含查询表单 + 新建表单 + 绑定按钮 | 已发现一条历史被动新建客资 `note` 为 `[被动添加新建] ����`、`nickname` 为 `���ղ���`，确认被动新建写入路径存在字符集问题（与 #1 同源） | P2 | 待修复 |
| 8 | 后端稳定性（跨模块观察） | 在销售验收用例第 11 个 GET 之后 8089 端口停止 LISTENING，连续 POST 全部返回 HTTP 000，未自行恢复（等待 30s+ 后仍未起） | — | NestJS 后端在 6-10 个常规调用后崩溃退出，按要求未重启；需排查内存/未捕获异常/Playwright 占用；阻断后续 POST 验收（标记成交/请求协同/标记已读未能闭环） | P0 | 待修复 |

> 备注：本节验收基于 JWT 令牌 `sub=user-sales-1`，所有 GET 用例均在后端崩溃前完成；POST/PUT/PATCH 用例未能完成端到端验证，已在代码层确认路由与权限分支存在。

---

## 三、教务端（B 端验收）

> 范围：订单池、订单详情、进度跟进、节点提醒、异常反馈、导出
> 验收账号：academic02 / test123（user-test-academic-02, role=academic）
> 验收时间：2026-06-01 由 agent #3 执行；中段后端 `localhost:8089` 崩溃（与销售端 #8、主管端 #12 同源），剩余项以代码审查补足
> 验收方式：JWT 登录后逐一调核心 GET / POST 接口（带过滤参数）+ 前端 `/academic/*` HTML 200 校验 + 关键源码走查

| # | 模块 | 接口 | 页面 | 问题摘要 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 订单池 | `GET /api/orders?scope=academic&limit=20` 200 但 `items=[]`；`role=academic` 默认查询同样返回 `[]` | `/academic/orders` 200，标题"订单池"、列"领取/状态"渲染正常 | 后端 `backend/src/modules/orders/orders.service.ts:133-214` 仅识别 `role=academic+scope=all`（限 admin/owner）和"已分配给我"两种分支；academic02 访问"订单池"落入"非 admin"分支 → `WHERE sales_user_id=:uid OR academic_user_id=:uid`，永远查不到 `academic_user_id IS NULL` 的池单。库内有 3 单（admin 全量查得证），其中 `bc026ade…` 的 `academicUserId=null` 属真正池单，academic02 接口侧实际查到 0。需新增 `scope=pool` 过滤 `academic_user_id IS NULL`，或当 `session.role==='academic'+scope=academic` 时默认列池单 | P0 | 待修复 |
| 2 | 订单详情 | `GET /api/orders/:id` 200 返回详情；测试 `bc026ade…`（不属于 academic02）可读，无角色/归属校验 | **缺失**：`frontend/src/app/academic/orders/` 下无 `[id]/page.tsx`，列表里订单 ID 仅 `copyable` 无跳转 | (1) 后端 `orders.controller.ts:81-90` findOne 不做角色/归属过滤，任意登录用户可查任意订单详情（含金额/客资 ID/销售 ID），数据越权；(2) 教务端没有详情页路由（销售端有 `sales/orders/[id]/page.tsx`），里程碑 1.2 "订单详情"在教务端实际未落地；(3) 详情接口 mapOrder 未返回 sales/academic name，前端 displayUser 只能 fallback userId | P1 | 待修复 |
| 3 | 进度跟进 | `GET /api/orders/:id/follow-records` 200 返回 `{items:[],total:0,limit:20,offset:0}` 正常分页；`POST /api/orders/:id/follow-records` 控制器存在 | **缺失**：教务端无独立 follow-records UI（既无新增节点表单，也无历史列表） | 后端 `orders.controller.ts:110-146` 已齐备并对"异常"字样命中走 ORDER_ABNORMAL 通知；但 `frontend/src/app/academic/` 下无任何进度跟进面板，教务实际无法新增/查看节点跟进，里程碑 1.2 教务端"进度跟进"功能页缺失 | P1 | 待修复 |
| 4 | 节点提醒 | 后端无定时任务消费 `order_follow_records.next_remind_at`；`backend/src` 全文 grep 仅有字段声明，无 `@Cron` / `node-cron` / `schedule` / `setInterval` | **缺失**：教务端无"待提醒/今日节点"页面或入口 | 【✅ 已修复 2026-06-01】`ScheduleModule.forRoot()` + `RemindersService.@Cron(EVERY_MINUTE)` 扫到期未发记录发 ORDER_NODE_DUE；新增 `order_follow_records.reminder_sent_at` 字段做幂等；新增 `GET /api/orders/reminders/pending` + `POST /api/orders/reminders/scan`；前端 `academic/reminders/page.tsx` + 菜单"节点提醒"入口；端到端冒烟通过（scan/二次 scan 幂等、listPending isOverdue 分级）。 | P0 | 已修复 |
| 5 | 异常反馈 | 后端可用：跟进节点含"异常"字样会向销售发 ORDER_ABNORMAL 通知（`orders.service.ts:293-307`）；`GET /api/orders?status=abnormal` 走通；`PATCH /api/orders/:id` 可改状态 | `/academic/abnormal` 200，复用 OrderTable（scope=academic, status=abnormal, actionMode='abnormal'），"处理"按钮把状态改回 in_progress | (1) "处理"按钮仅 `PATCH order_status=in_progress`，不写一条 follow-record 也不通知销售，闭环不完整；(2) 列表 `scope=academic` 同样走入 #1 过滤分支，academic 用户只能看自己名下的异常单，看不到全池异常单（与"异常反馈集中处理"需求相悖） | P2 | 待修复 |
| 6 | 导出 | `POST /api/exports {exportType:'orders'}` 返回 `{ok:true,id:…,status:'processing'}`；后续 `GET /api/exports/:id` 在调用时后端崩溃 | **缺失**：教务端无任何 `/academic/exports/` 页面；首页 nav 也无导出入口 | (1) **权限泄漏 P0**：`exports.service.ts:394` 仅在 filter 含 `role==='academic'+currentUserId+scope!='all'` 时按教务过滤；但 `exports.controller.ts:22-48` 不会把 session.role / userId 自动注入 filter，academic02 直接 POST 不带 filter 即可下载全公司订单 CSV；(2) 同理 `exportType` 未做角色白名单，academic 可创建 `leads/posts/rankings` 任意导出；(3) 教务端无导出 UI，里程碑 1.2 "导出（教务可见）"完全没落地 | P0 | 待修复 |
| 7 | 后端稳定性（跨模块观察） | 在调用 `GET /api/exports/:id` 检查异步任务状态时 8089 进程消失，10+ 分钟未恢复（与销售端 #8、主管端 #12 同源） | — | 怀疑 `setImmediate(runExport)` 内未捕获异常或 CSV 生成 OOM；需补 `process.on('unhandledRejection')` 兜底与 PM2 重启策略，否则 1.2 验收完全无法跑端到端 | P0 | 待修复 |

> 备注：本节验收基于 JWT 令牌 `sub=user-test-academic-02, role=academic`；GET 用例完成 + POST `/api/exports` 成功创建任务，但状态查询时后端崩溃。最严重 3 项：#6 导出接口无权限隔离导致教务可下载全公司订单 CSV（P0）；#1 订单池过滤逻辑错误导致教务永远看不到池单（P0）；#4 节点提醒功能完全未实现（P0）。

---

## 四、主管端（B 端验收）

> 范围：总览、运营排行榜、个人看板选择器、作品看板、客资看板、员工管理、账号管理、基础分析看板
> 验收账号：youlun / test123（user-admin-1, role=admin）
> 验收时间：2026-06-01
> 验收方式：登录获取 JWT 后逐一调核心 GET 接口 + 前端 `/admin/*` HTML 200 校验 + 关键源码走查

| # | 模块 | 接口 | 页面 | 问题摘要 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 总览（admin dashboard / overview） | `GET /api/dashboard/summary` 200 返回 14 个聚合字段（todayLeads/todayDeals/xhs+douyin 流量等，均为 0）；`GET /api/dashboard/post-type-distribution` 200 返回素人/话题/获客三档 ratio "0%" | `/admin` 200，使用 `frontend/src/app/admin/page.tsx`（仅 61 行）作为"主管首页"展示 4 个统计 + 5 个入口卡片 | 总览页面功能极简：只有 4 个 Statistic 卡和 5 个入口卡片，缺少里程碑文档要求的"总览看板"图表与时间筛选；接口数据正常但页面没有可视化 | P2 | 待修复 |
| 2 | 运营排行榜（rankings 主管视角） | `GET /api/rankings` 200 返回 23 个员工聚合行（accountCount/postCount/xhsPostCount/douyinPostCount 等）；`GET /api/rankings/learning-posts` 200（空数组） | **缺失**：`/admin/` 下无 `rankings/` 目录与 page.tsx，主管端无独立运营排行榜入口 | (1) 主管端前端缺失运营排行榜专属页面；(2) 控制器仅接受 `type/date/limit/offset`，对 `platform=xhs`/`period=30d` 等查询参数静默忽略（rankings.controller.ts L10-33），筛选维度不足；(3) 学习榜数组为空，无法验证 isFavorited 逻辑 | P1 | 待修复 |
| 3 | 个人看板选择器 | 无独立接口（依赖 `/api/employees` 列表 + 现有 staff personal API 透传） | **缺失**：`/admin/` 下无 `personal/` 或 employee-dashboard 选择器页面 | 里程碑文档要求"主管可挑选员工查看其个人面板"，前端 admin 路由完全没有此入口，admin/page.tsx 也无相关跳转，功能未实现 | P1 | 待修复 |
| 4 | 作品看板（posts board，跨员工聚合） | `GET /api/posts?limit=3&offset=0&employeeId=...` 200 返回 {items,total,limit,offset} 分页结构，按 employeeId 过滤生效；`GET /api/posts/plaza?view=all` 200 返回 rows + employeeName/accountName 等聚合字段 | **缺失**：`/admin/` 下无 `posts/` 目录与 page.tsx，主管端无作品看板入口 | 里程碑文档要求"作品看板（跨员工聚合）"，前端 admin 缺少独立页面；接口本身正常（含 plaza 视图） | P1 | 待修复 |
| 5 | 客资看板（leads board，跨员工聚合 + stats） | `GET /api/leads?limit=3` 200 返回 items；`GET /api/leads/stats` 200 返回 total:126/byStatus/byIntention/byProcess/byAddStatus 完整聚合；`GET /api/leads/passive/candidates` 200（空） | `/admin/leads` 200，使用 `frontend/src/app/admin/leads/page.tsx`（101 行），仅展示客资列表 + 分页 | (1) 前端未消费 `/api/leads/stats`，缺少头部统计卡片（按状态/意向/添加状态分布）；(2) 前端无筛选/搜索区（employeeId/platform/status 等接口已支持）；(3) leads 接口数据存在乱码（见销售端 #1，跨端共性） | P2 | 待修复 |
| 6 | 员工管理（employees） | `GET /api/employees` 200 返回 23 个员工数组（含 test-staff-01~15 + 8 个真实员工，字段 employeeCode/name/phone/hireDate/status 齐全） | `/admin/employees` 200，使用 page.tsx（107 行） | 通过；接口结构与字段完整 | P3 | 已通过 |
| 7 | 账号管理（accounts） | `GET /api/accounts?limit=3&offset=0` 200 返回 {items,total:178,limit,offset}；无分页时 `GET /api/accounts` 200 返回数组（兼容旧前端） | `/admin/accounts` 200，使用 page.tsx（109 行） | 通过；接口聚合 178 条账号、字段含 platform/profileUrl/accountUid/postingPlan/status | P3 | 已通过 |
| 8 | 基础分析看板（analytics snapshots） | `GET /api/analytics/snapshots` 200 但响应固定为 `{"snapshots":{}}` | `/admin/analytics` 200，使用 page.tsx（55 行）调 `getAdminDashboardSummary`+`listAdminPostTypeDistribution`+`listAdminRankings` | analytics.controller.ts L9-20 仍是 Legacy：仅读 `daily-snapshots.json` 同级文件（实际路径 `backend/src/../../daily-snapshots.json` 不存在），始终返回空对象；snapshots 数据源未迁移到 MySQL，前端基础分析页面其实不依赖该接口（用 dashboard/rankings 兜底） | P1 | 待修复 |
| 9 | 协同处理 inbox / 待确认来源（附加） | `GET /api/collaboration-tasks` 路由存在，admin 调用因后端崩溃未能完整闭环；销售端验收已证实 `scope=outgoing/incoming` 落到 `all` 不做过滤（跨端共性） | **缺失**：`/admin/` 下无协同任务/inbox 页面 | 主管端无协同处理 inbox 入口；后端 scope 过滤越权漏洞同销售端 #4 | P1 | 待修复 |
| 10 | 导入历史（imports 主管视角，附加） | `GET /api/import-tasks` / `GET /api/import-tasks?limit=5` 均 **HTTP 500**；日志显示 `QueryFailedError: Unknown column 'ImportTask.create_time' in 'field list'` | **缺失**：`/admin/` 下无 imports/导入历史页面 | 后端实体声明 `import_tasks.create_time`，但实际 DB 列名为 `created_at`（见 `migrations/M8__favorites_imports_metrics_compat.up.sql`），`backend/src/entities/import-task.entity.ts` L34 `@CreateDateColumn({ name: 'create_time' })` 与库不一致，导致接口全部 500、列表与详情均不可用；前端无相应页面 | P0 | 待修复 |
| 11 | 订单看板（admin orders，附加） | `GET /api/orders?limit=3&offset=0` 200 返回 {items,total:3,limit,offset}；订单字段 paidStatus/orderStatus 正常 | `/admin/orders` 200，复用 `academic/orders/OrderTable`（scope=all, actionMode=admin） | 返回数据存在编码乱码：`serviceType:"������ѵ"`、`remark:"�˵������ղ���"`（GBK→UTF-8 误转，跨端共性，与销售端 #1/#7 同源）；建议排查写入路径字符集 | P2 | 待修复 |
| 12 | 后端稳定性（跨模块观察） | 主管端验收过程中后端 8089 端口在 ~12 次 GET 调用后再次停止 LISTENING（与销售端 #8 现象一致，未自行恢复），import-tasks 500 error stack 后即下线 | — | 后端崩溃前最后一次 QueryFailedError 来自 imports（见 #10），可能为同一未捕获异常导致进程退出，与销售端同步发现；需排查 ExceptionsHandler 是否吞掉 stack 并补全 process.on('unhandledRejection') 兜底 | P0 | 待修复 |

> 备注：本节验收基于 JWT 令牌 `sub=user-admin-1, role=admin`，所有 GET 用例在后端崩溃前完成。CREATE/UPDATE/DELETE 类 POST/PUT 未做端到端验证（依据 code review 路由与权限分支均存在）。最严重 3 项：#10 导入任务接口 500 + 实体列名不匹配（P0）、#12 后端反复崩溃无自愈（P0）、#2/#3/#4 主管端 rankings/personal/posts 三大业务页面前端缺失（P1）。

---

## 五、跨端共性问题

| # | 类别 | 描述 | 涉及端 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- |
| C1 | 服务稳定性 | NestJS 后端在 10-14 次常规接口调用后停止 LISTENING（HTTP 000）且不自恢复，4 端验收均观察到、且阻断各自的 POST/PUT 端到端用例。可能由未捕获异常（含 import-tasks QueryFailedError 与 exports 异步任务）触发；建议补 `process.on('unhandledRejection'/'uncaughtException')` 兜底 + 调整 PM2 守护策略。详见 运营#14、销售#8、教务#7、主管#12 | 运营/销售/教务/主管 | P0 | 待修复 |
| C2 | 数据库实体列名错位 | `backend/src/entities/import-task.entity.ts` `@CreateDateColumn({ name: 'create_time' })` 与库内 `import_tasks.created_at` 不一致，所有 `GET /api/import-tasks*` 接口稳定 500，影响运营/主管端的"导入历史"模块；且高度怀疑为后端崩溃源（C1） | 运营/主管 | P0 | 待修复 |
| C3 | 字符集脏数据（GBK→UTF-8 误转） | `leads.nickname/note` 与 `orders.serviceType/remark` 存在 `�` 替换字符（如 `���ղ���`、`������ѵ`），前端显示为乱码；疑被动新建客资 / 旧订单写入路径字符集错位。需排查写入入口，并对历史脏数据做 backfill 修复 | 销售/主管（教务订单也会读到） | P2 | 待修复 |
| C4 | 协同任务 scope 越权 | 【✅ 已修复 2026-06-01】`collaboration-tasks.service.ts` 重写 `normalizeScope`：未识别 scope（空/outgoing/未知）一律落到 mine；新增 `applyCollabScope` 区分 admin/owner 才放行 `scope=all`，其它角色强制降级到 mine。controller 透传 session.role。实测 sales 调 `scope=outgoing/all` 都只返回自己 5 条，admin 调 `scope=all` 返回全表 10 条。 | 销售/主管/运营（接口共享） | P1 | 已修复 |
| C5 | 看板按 today 默认日期聚合全 0 | `dashboard.service.ts` 的 summary/post-type-distribution 与 `rankings.service.ts` 的 today* 字段默认按 `publishedAt = today` 过滤；当前样本最新 `publishedAt` 在 2026-04-29，导致今日所有计数永远 0、学习榜空数组、个人看板全部为 0；建议默认改成近 7/30 天或在前端加合理空态文案 | 运营/主管 | P2 | 待修复 |
| C6 | 主管端业务页面未落地 | 【✅ 已修复 2026-06-01】补建 `frontend/src/app/admin/{rankings,posts,collaboration,imports,personal}/page.tsx`，菜单 `menu.tsx` 新增对应 5 个条目；rankings 含 platform+period 双筛选；collaboration 走 `scope=all`；personal 是员工选择器→展示该员工累计 posts/leads。 | 主管 | P1 | 已修复 |
| C7 | 订单/导出权限未在接口层强制隔离 | `orders.controller.ts:81-90` findOne 不校验归属、`exports.controller.ts:22-48` 不强制注入 session 角色/userId/scope，academic 可越权读任意订单详情与下载全公司订单 CSV；建议在 controller/guard 层统一注入并白名单 exportType | 销售/教务/主管 | P0 | 待修复 |
| C8 | 指标抓取链路未跑通 | `posts.traffic/likes/comments/favorites` 全为 0、`metricsUpdatedAt:null`，运营"我的作品/作品广场"与主管"作品看板"数据无意义；需确认 Playwright 抓取定时任务与样本数据是否运行 | 运营/主管 | P2 | 待修复 |

---

## 优先级与状态规范

- 优先级：P0（阻断）/ P1（高）/ P2（中）/ P3（低）
- 状态：待修复 / 修复中 / 已修复 / 已验证 / 暂缓
- 问题登记格式建议：`接口/页面 路径 - 现象 - 期望 - 截图或日志位置`
